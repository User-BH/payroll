# وضعیت پروژه — سامانه حقوق و دستمزد

آخرین به‌روزرسانی: ۲۱ مرداد ۱۴۰۵

سند فنی وضعیت. برای معرفی غیرفنی سامانه، فایل پروپوزال جداگانه است.

---

## خلاصه در یک نگاه

| | |
|---|---|
| وضعیت | دموی کارآمد، روی سرور مستقر شده |
| آدرس | `http://tamin.justup.ir` |
| مخزن | `github.com/User-BH/payroll` (عمومی) |
| حجم کد | ۵٬۲۶۳ خط پایتون · ۲۶ قالب · ۲۷ مدل داده |
| موتور محاسبه | ۲۰ قاعده ثبت‌شده |
| داده نمونه | ۱۴۶ پرسنل · ۲ دوره · ۲۹۲ فیش |
| **آمادگی تولید** | **خیر — فاز ۰ هنوز باز است** |

سامانه از نظر فنی کار می‌کند، ولی تا وقتی اعداد قانونی واقعی وارد نشده و دورهٔ
موازی اجرا نشده، **نباید مبنای پرداخت واقعی قرار بگیرد**.

---

## ۱. انجام‌شده

### زیرساخت

- Django ۵ روی MySQL ۸ — فقط MySQL، بدون هیچ موتور جایگزین
- کولیشن `utf8mb4_persian_ci`، حالت `STRICT_TRANS_TABLES`
- رابط RTL کامل، فونت وزیرمتن و HTMX و Alpine همگی لوکال (بدون وابستگی به CDN)
- استقرار: nginx + gunicorn + systemd — کانفیگ‌ها نسخه‌بندی‌شده در `deploy/`
- تنظیمات محیطی از `.env`؛ هیچ کلید یا رمزی در مخزن نیست

### مدل داده — ۲۷ مدل در ۸ اپ

`accounts` · `org` · `employees` · `payroll_config` · `attendance` · `payroll` ·
`loans` · `portal`

سه اصلی که کل مدل روی آن‌ها بنا شده:

1. **هیچ داده‌ای «جاری» نیست.** حقوق پایه روی قرارداد تاریخ‌دار است، نه روی
   پرسنل. پارامترهای قانونی و پلکان مالیات هم بازهٔ اعتبار دارند.
2. **فیش قفل‌شده تغییرناپذیر است.** کل پارامترهای مؤثر در لحظهٔ محاسبه داخل خود
   فیش snapshot می‌شوند.
3. **هر عدد توضیح خودش را دارد.** هر سطر فیش، متن محاسبه‌اش را همراه دارد.

### موتور محاسبه — ۲۰ قاعده

منطق در کد، پارامتر در دیتابیس. اجرا در سه پاس: مزایا → کسور → هزینهٔ کارفرما،
داخل یک `transaction.atomic`.

- مزایا: حقوق پایه، پایه سنوات، حق مسکن، بن، حق اولاد، فوق‌العاده شغل،
  اضافه‌کاری، شب‌کاری، جمعه‌کاری، تعطیل‌کاری، نوبت‌کاری، پورسانت
- کسور: بیمه سهم کارگر (با سقف)، مالیات پلکانی، اقساط وام، تعدیل گرد کردن
- هزینهٔ کارفرما: بیمه سهم کارفرما، بیمه بیکاری
- **مبنای بیمه و مبنای مالیات دو عدد مستقل‌اند** — پورسانت مشمول مالیات و معاف
  از بیمه است و این تفاوت در محاسبه واقعاً اعمال می‌شود

### رابط کاربری — ۹ بخش

داشبورد · پرسنل و تاریخچهٔ قرارداد · دوره‌های حقوقی · کارکرد ماهانه (ویرایش
درجا) · فیش و نسخهٔ چاپی · اقلام حقوقی · تنظیمات سال مالی · گزارش‌ها با خروجی
اکسل · پرتال کارکنان

### ابزار عملیاتی

- `python manage.py seed_demo [--reset]` — ساخت داده نمونه
- `python manage.py doctor` — بررسی سلامت نصب (دیتابیس، کاربر، پیکربندی، استاتیک)

---

## ۲. تست‌شده

روی MySQL ۸٫۰٫۴۶ واقعی با کولیشن فارسی:

| مورد | نتیجه |
|---|---|
| ۲۴ مسیر برنامه | همه ۲۰۰ |
| مهاجرت‌ها از صفر | بدون خطا |
| قید «یک فیش اصلی برای هر پرسنل در هر دوره» | درج تکراری با خطای ۱۰۶۲ رد شد |
| فیش اصلاحی با `revision=1` | پذیرفته شد |
| ماشین حالت دوره تا قفل | پیشروی درست |
| محاسبهٔ مجدد پس از قفل | رد شد با پیام درست |
| برگشت وضعیت پس از قفل | رد شد با پیام درست |
| دسترسی پرتال به فیش دیگران | ۴۰۴ |
| دسترسی پرسنل به بخش مالی | هدایت به پرتال |
| ذخیرهٔ درجای کارکرد (HTMX) | در دیتابیس نشست |
| خروجی اکسل (۴ گزارش) | تولید شد |
| متن فارسی و مبالغ DECIMAL | سالم |

### باگ‌هایی که در همین مسیر پیدا و رفع شدند

1. **قید یکتایی فیش روی MySQL بی‌اثر بود.** با
   `UniqueConstraint(condition=...)` نوشته شده بود؛ MySQL ایندکس شرطی ندارد و
   جنگو چنین قیدی را بی‌صدا نادیده می‌گیرد. یعنی امکان ثبت دو فیش تکراری برای یک
   نفر در یک دوره باز بود. با ستون `revision` بازنویسی شد.
2. **کوکی امن روی سایت HTTP.** با `DEBUG=0` کوکی `Secure` می‌شد و روی HTTP ورود
   بدون هیچ پیام خطایی شکست می‌خورد. حالا با `SECURE_COOKIES` کنترل می‌شود.
3. **ویرایش درجای کارکرد کار نمی‌کرد** — نحو نامعتبر HTMX.

---

## ۳. باقی‌مانده

### فاز ۰ — مسدودکننده‌ها

تا این چهار مورد روشن نشود، مدل داده نهایی نیست:

- [ ] یک **نمونه فیش حقوقی واقعی** و یک **نمونه لیست بیمهٔ ارسالی** از واحد مالی
- [ ] منطق دقیق **پورسانت سه‌سطحی** و اینکه ورودی‌اش از کدام سیستم می‌آید
- [ ] **چند شرکت یا شعبه؟** (تعیین می‌کند چندشرکتی لازم است یا نه)
- [ ] **انواع قرارداد**: فقط تمام‌وقت یا ساعتی و روزمزد و پیمانی هم هست

### فاز ۱ — اعداد و اقلام واقعی

- [ ] جایگزینی پارامترهای قانونی نمونه با ارقام واقعی ۱۴۰۵
- [ ] جایگزینی پلکان مالیات نمونه با پلکان رسمی سال
- [ ] بازتعریف اقلام حقوقی مطابق فیش واقعی شرکت
- [ ] تأیید تفسیر «کسر بیمهٔ سهم کارگر از درآمد مشمول مالیات» با حسابدار

### فاز ۲ — ورود کارکرد

- [ ] ورود کارکرد از فایل اکسل (مدل `TimesheetImport` ساخته شده، منطقش نه)
- [ ] اتصال به دستگاه حضور و غیاب — نیازمند مشخصات دستگاه

### فاز ۳ — خروجی‌های قانونی

- [ ] فایل DSK تأمین اجتماعی با فرمت واقعی
- [ ] لیست مالیات با فرمت سامانهٔ مالیاتی
- [ ] فایل پرداخت گروهی بانک با فرمت بانک مقصد
- [ ] ثبت کد رهگیری و زمان ارسال

### فاز ۴ — تعهدات سالانه

- [ ] عیدی · سنوات · بازخرید مرخصی · تسویه‌حساب
- [ ] فیش اصلاحی (ستون `revision` آماده است، فرایندش نه)

### فاز ۵ — تکمیل

- [ ] درخواست مرخصی آنلاین
- [ ] احراز هویت دومرحله‌ای برای نقش‌های مالی
- [ ] لاگ کامل تغییرات (`django-simple-history`)
- [ ] محدودسازی تلاش ورود (`django-axes`)

### فاز ۶ — اعتبارسنجی (حیاتی)

- [ ] **اجرای موازی ۲ تا ۳ دوره** کنار روش فعلی و مقایسهٔ ریال‌به‌ریال

هیچ سامانهٔ حقوقی بدون این مرحله قابل اعتماد نیست. اگر یک ریال اختلاف بود، تا
پیدا کردن علتش نباید جلو رفت.

---

## ۴. کارهای زیرساختی باقی‌مانده

- [ ] **نصب SSL** — الان روی HTTP است و رمز عبور پرسنل متن ساده در شبکه می‌رود
- [ ] **بکاپ خودکار روزانه + تست بازیابی** — بکاپ بدون تست بازیابی، بکاپ نیست
- [ ] بررسی خصوصی‌کردن مخزن (الان عمومی است)
- [ ] `SECRET_KEY` اختصاصی روی سرور

---

## ۵. ریسک‌های شناخته‌شده

| ریسک | اثر | کاهش |
|---|---|---|
| مدل داده بدون دیدن فیش واقعی ساخته شده | ممکن است اقلام یا مبناها اشتباه باشند | فاز ۰ |
| اعداد قانونی نمونه‌اند | محاسبات فعلی معتبر نیستند | فاز ۱ |
| تفسیر کسر بیمه از مالیات تأیید نشده | مبلغ مالیات مستقیماً تحت تأثیر است | تأیید حسابدار |
| سایت روی HTTP | افشای رمز عبور | نصب SSL |
| بدون بکاپ خودکار | از دست رفتن داده حقوق | راه‌اندازی بکاپ |
