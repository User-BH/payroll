# کارهای باقی‌مانده

آخرین به‌روزرسانی: ۲۵ مرداد ۱۴۰۵

گروه ۱ کامل شد. آنچه مانده به ترتیب اولویت:

---

## گروه ۲ — لازم برای کار روزمره

| # | کار | چرا لازم است |
|---|---|---|
| ۱ | **ورود کارکرد از اکسل** | ثبت دستی کارکرد ۱۴۶ نفر هر ماه، هر ماه. مدل `TimesheetImport` ساخته شده ولی منطقش نوشته نشده |
| ۲ | **مدیریت وام و اقساط** | موتور اقساط سررسیدشده را کسر می‌کند، ولی راهی برای ثبت وام جدید در سامانه نیست |
| ۳ | **مدیریت ساختار سازمانی** | افزودن واحد، مرکز هزینه و پست سازمانی فقط با دستور خط فرمان ممکن است |
| ۴ | **ویرایش اطلاعات شرکت** | کد کارگاه بیمه و شماره پرونده مالیاتی روی گزارش‌ها چاپ می‌شوند ولی جایی وارد نمی‌شوند — الان خالی‌اند |
| ۵ | **خروجی PDF دسته‌ای فیش‌ها** | فیش فعلی شرکت یک PDF چندصفحه‌ای است؛ الان باید تک‌تک چاپ شوند |
| ۶ | گردش خروج پرسنل | تاریخ خاتمه از فرم ویرایش قابل ثبت است ولی تسویه‌حساب و توقف خودکار محاسبه ندارد |

## گروه ۳ — خروجی‌های قانونی

| # | کار |
|---|---|
| ۷ | فایل DSK تأمین اجتماعی (الان فقط اکسل) |
| ۸ | لیست مالیات با فرمت سامانه مالیاتی |
| ۹ | فایل پرداخت با فرمت بانک رسالت |
| ۱۰ | ثبت کد رهگیری و زمان ارسال |

## گروه ۴ — تعهدات سالانه

| # | کار | یادداشت |
|---|---|---|
| ۱۱ | عیدی | تا اسفند لازم می‌شود |
| ۱۲ | سنوات | |
| ۱۳ | بازخرید مرخصی | مانده مرخصی از قبل محاسبه می‌شود |
| ۱۴ | تسویه‌حساب پایان کار | |
| ۱۵ | فیش اصلاحی | ستون `revision` آماده است، فرایندش نه |

## گروه ۵ — امنیت و نگهداری

| # | کار | وضعیت |
|---|---|---|
| ۱۶ | **بکاپ خودکار روزانه + تست بازیابی** | هیچ بکاپی تنظیم نشده |
| ۱۷ | **نصب SSL** | سایت روی HTTP؛ رمز پرسنل متن ساده در شبکه می‌رود |
| ۱۸ | **لاگ تغییرات** | مدل `AuditLog` ساخته شده ولی **هیچ‌جا نوشته نمی‌شود** |
| ۱۹ | محدودسازی تلاش ورود | `django-axes` نصب نشده |
| ۲۰ | احراز هویت دومرحله‌ای | برای نقش‌های مالی |
| ۲۱ | خصوصی کردن مخزن گیت‌هاب | الان عمومی است |

## گروه ۶ — اعتبارسنجی

| # | کار |
|---|---|
| ۲۲ | **اجرای موازی ۲ تا ۳ دوره** کنار روش فعلی و مقایسه ریال‌به‌ریال |

## سؤال‌های باز

- **مابه التفاوت** فعلاً دستی وارد می‌شود. اگر قاعده‌ای دارد، خودکار می‌شود.
- **طلب ماه قبل** و **وجه مرخصی** در فیش‌های نمونه صفر بودند؛ مشمولیت بیمه و
  مالیاتشان حدسی است (فعلاً مشمول هر دو) و باید تأیید شود.
- **پله‌های مالیات ۱۴۰۴** اگر دوره موازی از ۱۴۰۴ شروع شود.

---

# آنچه ساخته و تست شده

## قواعد استخراج‌شده از فیش‌های واقعی
روی **چهار فیش تیر ۱۴۰۵** ریال‌به‌ریال تطبیق داده شد:

    مبنای بیمه   = جمع پرداختی‌ها − حق ماموریت − حق اولاد      (۷٪)
    مبنای مالیات = جمع پرداختی‌ها − حق ماموریت                 (پلکانی)
    بیمه سهم کارگر از مبنای مالیات کسر نمی‌شود
    بدون گرد کردن — خالص، رقم دقیق ریالی است
    هر روز مرخصی = ۴۴۰ دقیقه (۷ ساعت و ۲۰ دقیقه)

پلکان ۱۴۰۵: معاف تا ۴۰ م.ت، سپس ۱۰ / ۱۵ / ۲۰ / ۲۵ / ۳۰ درصد

## زیرساخت
Django ۵ روی MySQL ۸ · RTL کامل · فونت و کتابخانه‌ها لوکال ·
nginx + gunicorn + systemd با کانفیگ نسخه‌بندی‌شده · دستور `doctor`

## عملیات
داشبورد · پرسنل (ثبت، ویرایش، قرارداد، حساب بانکی، تحت تکفل، **ورود گروهی از
اکسل**) · دوره‌ها (ساخت، ماشین حالت، قفل برگشت‌ناپذیر) · کارکرد ماهانه با ویرایش
درجا و مانده مرخصی زنده · مبالغ دستی · موتور محاسبه · فیش A5 با لوگو و ریز
محاسبات · گزارش بیمه/مالیات/بانک با خروجی اکسل

## پیکربندی
ویرایش پارامترهای سال · ویرایش پلکان مالیات · ساخت و ویرایش اقلام حقوقی با
دامنه شمول · سهمیه مرخصی (ثبت یکجا و تکی)

## پرتال کارکنان
ساخت گروهی حساب با رمز یک‌بارمصرف · اجبار به تغییر رمز در اولین ورود ·
مشاهده فیش · **بارگذاری امضا** · تأیید فیش (مشروط به بارگذاری امضا) ·
ثبت اعتراض با ذکر علت · صفحه بررسی و پاسخ برای واحد مالی

## ریسپانسیو
روی گوشی جدول‌ها به کارت تبدیل می‌شوند · سایدبار کشویی ·
در ۳۷۵ پیکسل هیچ صفحه‌ای سرریز افقی ندارد
