# کارهای باقی‌مانده

آخرین به‌روزرسانی: ۲۴ مرداد ۱۴۰۵

فهرست کارهای باقی‌مانده تا سامانه کامل شود، به ترتیب اولویت.
برای فهرست آنچه ساخته شده، انتهای همین فایل را ببینید.

---

## گروه ۱ — مسدودکننده: بدون این‌ها حقوق مرداد قابل صدور نیست

| # | کار | چرا مسدودکننده است |
|---|---|---|
| ۱ | **ویرایش پارامترهای سال مالی** | حداقل دستمزد، حق اولاد و پایه سنوات صفرند و **هیچ راهی برای واردکردنشان نیست** — نه در سامانه، نه در پنل ادمین (هیچ `admin.py` وجود ندارد) |
| ۲ | **ویرایش اقلام حقوقی** | فقط فعال/غیرفعال ممکن است. مبلغ ثابت حق مسکن، بن، حق تاهل و پایه سنوات جایی وارد نمی‌شود |
| ۳ | **ثبت سهمیه مرخصی** | انتقالی و استحقاقی هر نفر باید وارد شود، وگرنه مانده مرخصی روی همه فیش‌ها صفر می‌آید |
| ۴ | **ساخت حساب پرتال پرسنل** | راهی برای دادن دسترسی به ۱۴۶ نفر وجود ندارد؛ اتصال `Employee.user` فقط دستی است |

هر چهارتا کار کوتاهی‌اند و با هم یک روز کار دارند.

## گروه ۲ — لازم برای استفاده روزمره

| # | کار | یادداشت |
|---|---|---|
| ۵ | ورود کارکرد از اکسل | مدل `TimesheetImport` ساخته شده، منطقش نوشته نشده |
| ۶ | مدیریت وام و اقساط | مدل کامل است و موتور اقساط را کسر می‌کند، ولی ثبت وام رابط کاربری ندارد |
| ۷ | مدیریت ساختار سازمانی | افزودن واحد، مرکز هزینه و پست سازمانی فقط با دستور خط فرمان |
| ۸ | ویرایش اطلاعات شرکت | کد کارگاه بیمه و شماره پرونده مالیاتی روی گزارش‌ها چاپ می‌شوند ولی جایی وارد نمی‌شوند |
| ۹ | تغییر رمز عبور | نه برای کارکنان مالی، نه برای پرسنل در پرتال |
| ۱۰ | خروجی PDF دسته‌ای فیش‌ها | فیش فعلی شرکت یک PDF چندصفحه‌ای است؛ الان باید تک‌تک چاپ شوند |
| ۱۱ | حذف و غیرفعال‌سازی پرسنل | خروج از شرکت (تاریخ خاتمه) فقط از فرم ویرایش، بدون گردش کار |

## گروه ۳ — خروجی‌های قانونی

| # | کار | یادداشت |
|---|---|---|
| ۱۲ | فایل DSK تأمین اجتماعی | الان فقط اکسل تولید می‌شود؛ فرمت واقعی DSK لازم است |
| ۱۳ | لیست مالیات با فرمت سامانه مالیاتی | همین‌طور |
| ۱۴ | فایل پرداخت با فرمت بانک رسالت | فرمت واقعی بانک لازم است |
| ۱۵ | ثبت کد رهگیری و زمان ارسال | مدل‌هایش در طراحی هست، ساخته نشده |

## گروه ۴ — تعهدات سالانه

| # | کار |
|---|---|
| ۱۶ | عیدی |
| ۱۷ | سنوات |
| ۱۸ | بازخرید مرخصی |
| ۱۹ | تسویه‌حساب پایان کار |
| ۲۰ | فیش اصلاحی (ستون `revision` آماده است، فرایندش نه) |

## گروه ۵ — امنیت و نگهداری

| # | کار | وضعیت |
|---|---|---|
| ۲۱ | **لاگ تغییرات** | مدل `AuditLog` ساخته شده ولی **هیچ‌جا نوشته نمی‌شود** — یعنی عملاً لاگ تغییری وجود ندارد |
| ۲۲ | **نصب SSL** | سایت روی HTTP است و رمز پرسنل متن ساده در شبکه می‌رود |
| ۲۳ | **بکاپ خودکار روزانه + تست بازیابی** | هیچ بکاپی تنظیم نشده |
| ۲۴ | محدودسازی تلاش ورود | برای جلوگیری از حدس رمز پرسنل |
| ۲۵ | احراز هویت دومرحله‌ای برای نقش‌های مالی | |
| ۲۶ | خصوصی کردن مخزن گیت‌هاب | الان عمومی است |

## گروه ۶ — اعتبارسنجی

| # | کار |
|---|---|
| ۲۷ | **اجرای موازی ۲ تا ۳ دوره** کنار روش فعلی و مقایسه ریال‌به‌ریال |

بدون این مرحله هیچ سامانه حقوقی قابل اعتماد نیست.

## سؤال باز

- **مابه التفاوت** فعلاً دستی وارد می‌شود. اگر قاعده‌ای دارد، خودکارسازی‌اش هم ممکن است.
- **طلب ماه قبل**، **وجه مرخصی** و **هزینه تلفن** در فیش‌های نمونه صفر بودند؛ مشمولیت بیمه و مالیاتشان حدسی است و باید تأیید شود.

---

# آنچه ساخته و تست شده

## زیرساخت
Django ۵ روی MySQL ۸ (فقط MySQL) · کولیشن `utf8mb4_persian_ci` · رابط RTL کامل ·
فونت و کتابخانه‌ها لوکال بدون CDN · nginx + gunicorn + systemd با کانفیگ
نسخه‌بندی‌شده در `deploy/` · دستور `doctor` برای تشخیص مشکلات نصب

## مدل داده — ۲۸ مدل
سه اصل: همه‌چیز تاریخ‌دار · فیش قفل‌شده تغییرناپذیر با snapshot · هر عدد توضیح خودش را دارد

## موتور محاسبه — ۲۰ قاعده
سه پاس (مزایا → کسور → هزینه کارفرما) داخل یک تراکنش.
**قواعد استخراج‌شده از فیش‌های واقعی تیر ۱۴۰۵ و تأییدشده ریال‌به‌ریال:**

    مبنای بیمه   = جمع پرداختی‌ها − حق ماموریت − حق اولاد        (۷٪)
    مبنای مالیات = جمع پرداختی‌ها − حق ماموریت                   (پلکانی)
    بیمه سهم کارگر از مبنای مالیات کسر نمی‌شود

پلکان مالیات ۱۴۰۵: معاف تا ۴۰ م.ت، سپس ۱۰ / ۱۵ / ۲۰ / ۲۵ / ۳۰ درصد

## رابط کاربری
داشبورد · پرسنل و قرارداد (ثبت، ویرایش، ورود گروهی از اکسل) · دوره‌ها (ساخت،
ماشین حالت، قفل) · کارکرد ماهانه با ویرایش درجا و مانده مرخصی زنده · مبالغ دستی ·
فیش A5 با لوگو و ریز محاسبات · اقلام حقوقی · تنظیمات سال مالی (خواندنی) ·
گزارش بیمه/مالیات/بانک با خروجی اکسل · پرتال کارکنان با گردش تأیید و اعتراض

## مرخصی
واحد پایه دقیقه (روز = ۴۴۰ دقیقه، از خود فیش شرکت استخراج شد).
مانده هرگز ذخیره نمی‌شود و همیشه از روی کارکرد ماه‌ها ساخته می‌شود.

## گردش تأیید فیش
در انتظار → دیده شد → (تأیید | اعتراض با ذکر علت) → بررسی و پاسخ واحد مالی
