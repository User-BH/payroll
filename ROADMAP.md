# نقشه راه — فازبندی درخواست‌های جدید

این فایل، ده درخواستِ سند به‌روزرسانی را به فازهای قابل اجرا تقسیم می‌کند.
ترتیب فازها تصادفی نیست: هر فاز یا پیش‌نیاز فاز بعد است، یا از فاز بعد کم‌ریسک‌تر
است و زودتر به کار می‌آید.

**قاعدهٔ کار:** هر فاز یک شاخه، یک بار محاسبهٔ آزمایشی روی دورهٔ واقعی، و
مقایسهٔ ریال‌به‌ریال با ماه قبل پیش از رفتن به فاز بعد. هیچ دو فازی هم‌زمان
روی محاسبه دست نمی‌برند.

| فاز | موضوع | بند سند | ریسک محاسباتی |
|---|---|---|---|
| ۰ | حساب بانکی، فهرست بانک‌ها، شبا | — (درخواست جداگانه) | ندارد |
| ۱ ✅ | فیش و بیمهٔ بازنشسته | ۷، ۹ | کم |
| ۲ | کارکرد و حقوق ثابت | ۴، ۲، ۱ | **زیاد** |
| ۳ | تفکیک سالانه از ماهانه | ۶ | کم |
| ۴ | پورسانت، مأموریت و اضافه‌کاری | ۱۰ | **زیاد** |
| ۵ | کارتابل مرخصی | ۵ | کم |
| ۶ | ساختار سازمانی چندسطحی | ۸ | متوسط (مهاجرت داده) |
| ۷ | اقلام حقوقی بدون کدنویسی | ۳ | متوسط |

---

## فاز ۰ — حساب بانکی و شبا ✅ انجام شد

خارج از سند، ولی چون به فرم افزودن پرسنل مربوط بود همین حالا انجام شد.

- حساب بانکی در همان صفحهٔ افزودن پرسنل پرسیده می‌شود، نه یک بخش جدا.
- نام بانک از **فهرست بانک‌های کشور** انتخاب می‌شود، نه متن آزاد.
- شماره شبا اختیاری است، فاصله و حروف کوچک را می‌پذیرد، رقم کنترلی‌اش کنترل
  می‌شود، و **در صورت وجود روی فیش چاپ می‌شود**.
- کد بانک دیگر پرسیده نمی‌شود؛ از روی نام بانک پر می‌شود.

---

## فاز ۱ — فیش و بیمهٔ بازنشسته ✅ انجام شد

کوچک‌ترین فاز و بیشترین اثر فوری. هیچ‌کدام از این دو، فرمول حقوق را عوض
نمی‌کنند، پس می‌توانند سریع و بی‌دغدغه روی همین دورهٔ جاری بروند.

### بند ۷ — اصلاح فیش حقوقی
- حذف **مبنای بیمه** و **مبنای مالیات** از فیش (A5، صفحهٔ فیش، پرتال کارکنان).
- افزودن متن تأیید کنار محل امضا:
  «موارد محاسباتی حقوق و مزایای مندرج در این فیش به‌صورت الکترونیکی رؤیت گردید و
  واریز خالص پرداخت به حساب شماره مندرج در فیش مورد تأیید قرار گرفت.»

فقط قالب است؛ `Payslip.insurable_base` و `taxable_base` سر جایشان می‌مانند چون
گزارش بیمه و مالیات به آن‌ها تکیه دارند — فقط دیگر روی فیش چاپ نمی‌شوند.

### بند ۹ — پرسنل بازنشسته
- فیلد `is_retired` روی `Employee`.
- در موتور: قاعده‌های `insurance_employee`، `insurance_employer` و
  `unemployment_insurance` برای این افراد نتیجه‌ای تولید نکنند.
- در گزارش بیمه، جمع بیمهٔ ماه، و **فایل خروجی تأمین اجتماعی**، این افراد اصلاً
  نیایند و در تعداد نفرات هم شمرده نشوند.

کنترل باید **یک جا** پیاده شود (یک متد روی بستر محاسبه، مثلاً
`ctx.insurance_applies`) و بقیهٔ جاها همان را صدا بزنند — وگرنه یکی از پنج مسیر
جا می‌ماند و مغایرت با تأمین اجتماعی همان‌جا ساخته می‌شود.

**تمام‌شده یعنی:** یک پرسنل بازنشستهٔ نمونه در فیشش هیچ ردیف بیمه‌ای ندارد، در
فایل DSK نیست، و جمع بیمهٔ ماه دقیقاً به اندازهٔ سهم او کم شده است.

### آنچه انجام شد

تصمیم بیمه در `PayrollContext.insurance_applies` متمرکز شد (قرارداد مشمول باشد
**و** پرسنل بازنشسته نباشد) و هر پنج مسیر از همان یک نقطه می‌آیند: اجرای دوره،
سه قاعدهٔ بیمه، گزارش بیمه، جمع ماه، و فایل DSK.

نتیجه در دادهٔ نمونه: فیش بازنشسته هیچ ردیف بیمه‌ای ندارد، خالصش دقیقاً به اندازهٔ
سهم کارگر بالا رفت، گزارش بیمه ۱۴۵ نفر از ۱۴۶ شد و نامِ کنارگذاشته‌شده را در
کادر هشدار نشان می‌دهد، و فایل DSK با ۱۴۵ رکورد ساخته شد بدون کد ملی آن فرد.

`Payslip.insurance_applies` هم مثل بقیهٔ مبناها در لحظهٔ محاسبه عکس‌برداری می‌شود:
اگر بعداً تیک بازنشستگی عوض شود، لیستی که قبلاً به تأمین اجتماعی رفته تغییر
نمی‌کند. مهاجرت، فیش‌های گذشته را از روی `contract.is_insured` پر می‌کند.

> **یادداشت خارج از این فاز:** فیش A5 در دادهٔ نمونه **دو صفحه** می‌شود (۹۷۶
> پیکسل در برابر ۷۹۴ پیکسل ارتفاع A5). این از قبل هم همین‌طور بود — با برگرداندن
> قالب به نسخهٔ قبل هم دو صفحه شد — و ربطی به تغییرات این فاز ندارد. اگر
> فیش‌های واقعی هم دو صفحه در می‌آیند، بگویید تا در یک کار جدا فشرده‌اش کنیم.

---

## فاز ۲ — کارکرد و حقوق ثابت

**پرریسک‌ترین فاز.** هر سه بند یک زنجیرهٔ واحدند و جدا کردنشان یعنی دو بار
دست بردن در همان فرمول:

```
کارکرد اولیه (از تاریخ‌های استخدام/خاتمه/شروع مجدد)
  − غیبت − بیماری − مرخصی بدون حقوق
  = کارکرد نهایی قابل پرداخت
  × دستمزد روزانه (واقعی، وگرنه حداقل)
  = حقوق ثابت
```

### بند ۴ — تاریخ خاتمه کار و شروع به کار مجدد
- `Employee.termination_date` هست؛ `rehire_date` اضافه شود.
- کارکرد اولیهٔ هر ماه از تقاطع «بازهٔ اشتغال» با «بازهٔ دوره» حساب شود، نه از
  طول کامل ماه. مثال سند: خاتمه ۱۶ مرداد ۱۴۰۵ → کارکرد اولیه ۱۵ روز، نه ۳۱.
- همین منطق تعیین کند چه کسی اصلاً ردیف کارکرد بگیرد.

### بند ۲ — کارکرد قابل پرداخت و نمایش روزی
- `Timesheet.paid_days` بازتعریف شود: کارکرد اولیه منهای غیبت، بیماری و مرخصی
  بدون حقوق.
- غیبت، بیماری و مرخصی بدون حقوق در فیش **فقط تعداد روز** نشان بدهند و مبلغ
  ریالی نداشته باشند. لازمه‌اش یک فیلد «واحد نمایش» روی `SalaryComponent`
  (ریال / روز / ساعت) است تا این رفتار در کد سیم‌کشی نشود.

### بند ۱ — دستمزد واقعی با پیش‌فرض حداقل دستمزد
- `EmploymentContract.base_salary` همان «دستمزد واقعی» است. خالی یا صفر یعنی
  «از حداقل دستمزد همان سال استفاده کن».
- روی فیش پیدا باشد که حقوق ثابت با کدام مبنا حساب شده است.
- هر قلمی که مبنایش «دستمزد فرد» است از همین عدد استفاده کند، و هر قلمی که
  مبنایش «حداقل دستمزد» است از پارامتر سال — این دو نباید قاطی شوند.

**تمام‌شده یعنی:** سه پروندهٔ نمونه (کارکرد کامل، خروج وسط ماه، دارای غیبت و
بیماری) دستی حساب شوند و ریال‌به‌ریال با خروجی سامانه بخوانند.

---

## فاز ۳ — تفکیک آیتم‌های سالانه از ماهانه

پیش‌نیاز فاز ۴ است و به‌تنهایی هم ارزش دارد.

### بند ۶
- آنچه سالانه ثابت است (حداقل دستمزد، پایه سنوات، حق مسکن، بن، حق اولاد) همان‌جا
  که هست می‌ماند: تنظیمات سال مالی.
- برای آنچه ماهانه عوض می‌شود (مبنای حق مأموریت، مبنای اضافه‌کاری و …) یک مدل
  «پارامتر ماهانه» و صفحهٔ **«آیتم‌های محاسباتی ماهانه»** ساخته شود: هر دوره،
  هر قلم، یک مقدار.
- ترتیب خواندن در موتور: **مقدار ماهانهٔ همان دوره، وگرنه مقدار سالانه.**
- مقدار به‌کاررفته مثل بقیهٔ پارامترها داخل `params_snapshot` فیش ثبت شود.

---

## فاز ۴ — پورسانت، حق مأموریت و اضافه‌کاری

بعد از فاز ۳ می‌آید چون بدون «مبنای ماهانه» اصلاً قابل پیاده‌سازی نیست.

### بند ۱۰
- سقف مأموریت (فعلاً ۲۵ روز) و سقف‌های جابه‌جایی **در تنظیمات** باشند، نه در کد.
- ظرفیت مأموریت ماه = مبنای مأموریت همان ماه × سقف روز.
- پورسانت تا ظرفیت مجاز به حق مأموریت منتقل شود؛ مازاد تا سقف مجاز به
  اضافه‌کاری؛ و **باقی‌مانده در خود پورسانت بماند، نه اینکه حذف شود**.
- یک رکورد ردیابی نگه داشته شود تا همیشه بشود پرسید این عدد از کجا آمد:
  مبلغ اولیهٔ پورسانت · منتقل‌شده به مأموریت · منتقل‌شده به اضافه‌کاری ·
  باقی‌مانده · مبنای مأموریت ماه · تعداد روز مأموریت · مبنای اضافه‌کاری ماه.

**تمام‌شده یعنی:** برای یک پرسنل با پورسانت بزرگ‌تر از ظرفیت، هر سه عدد (مأموریت،
اضافه‌کاری، ماندهٔ پورسانت) جمعشان دقیقاً برابر پورسانت اولیه باشد.

---

## فاز ۵ — کارتابل مرخصی

### بند ۵
- مدل درخواست/ثبت مرخصی با تاریخ، نوع و مقدار (روز/ساعت/دقیقه).
- ثبت مستقیم توسط کارگزینی، و اثر خودکار روی `Timesheet` همان ماه — بدون ورود
  دوباره در جدول کارکرد.
- مانده مرخصی از همین‌جا هم به‌روز شود (زیرساختش هست: مانده هرگز ذخیره نمی‌شود و
  همیشه محاسبه می‌شود).

از فازهای ۳ و ۴ مستقل است و در صورت نیاز می‌تواند موازی جلو برود.

---

## فاز ۶ — ساختار سازمانی چندسطحی

### بند ۸
سطوح: **سازمان → مرکز هزینه → واحد/قسمت → پرسنل**، با کد مستقل برای هر گره.

- `Department` از قبل `parent` دارد؛ `CostCenter` ندارد و به واحد هم وصل نیست.
- گزارش‌ها و چاپ فیش باید بتوانند روی هر سطح تجمیع کنند.
- **دادهٔ فعلی نباید بشکند:** «فروش» و «پشتیبانی» همان‌طور می‌مانند و فیش‌های
  مرداد ۱۴۰۵ بدون تغییر قابل چاپ‌اند. مهاجرت باید ساختار فعلی را به‌عنوان یک
  درخت تک‌سطحی ببیند، نه اینکه بازنویسی‌اش کند.

بعد از فازهای محاسباتی می‌آید چون مهاجرت داده است و بهتر است وقتی انجام شود که
محاسبه پایدار شده باشد.

---

## فاز ۷ — تعریف اقلام حقوقی بدون کدنویسی

### بند ۳
زیرساختش تا نیمه هست: `SalaryComponent` با `engine_rule_key` و صفحهٔ ساخت و
ویرایش قلم. آنچه نیست، انتخاب **مبنای محاسبه** بدون کد است.

پیشنهاد: به‌جای یک زبان فرمولِ باز (که تست و ممیزی‌اش سخت و خطرش زیاد است)، یک
قاعدهٔ پارامتری:

```
مبلغ = [ حداقل دستمزد | دستمزد فرد | مبلغ ثابت | درصدی از قلم دیگر ]
       × [ روز کارکرد | ثابت ]   × ضریب
```

با همین چهار مبنا، اکثر اقلام واقعی بدون کدنویسی ساخته می‌شوند و هر عدد
همچنان توضیح خودش را دارد.

آخر از همه می‌آید چون بهترین طراحی‌اش وقتی روشن می‌شود که مبناهای فازهای ۱ تا ۴
تثبیت شده باشند — ساختنش پیش از آن، یعنی ساختن آن دو بار.

---

## اصولی که در همهٔ فازها رعایت می‌شود

۱. **فیش قفل‌شده تغییر نمی‌کند.** هر پارامتر تازه‌ای که وارد محاسبه شود باید در
   `params_snapshot` ثبت شود.
۲. **دادهٔ قبلی نمی‌شکند.** هر مهاجرت، مقدار قدیمی را منتقل می‌کند نه اینکه
   صفر بگذارد.
۳. **هیچ سقف و ضریبی در کد ثابت نمی‌شود.** جایش تنظیمات سال یا پارامتر ماهانه است.
۴. **هر عدد توضیح خودش را دارد** — `PayslipLine.explanation` برای اقلام تازه هم
   پر می‌شود.
