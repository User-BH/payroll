from django.apps import AppConfig


class PayrollConfigApp(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.payroll"
    label = "payroll"
    verbose_name = "دوره و فیش حقوقی"
