# فرمول‌های فایل اکسل حقوق و دستمزد

مرجع: `1405.xlsx`

این سند خودکار ساخته شده است:

```bash
python tools/dump_excel_formulas.py <فایل> > docs/EXCEL-FORMULAS.md
```

در هر ستون، شمارهٔ سطر با `#` جایگزین شده تا فرمول سطرهای مختلف
قابل مقایسه باشد. اگر ستونی بیش از یک شکل دارد، یعنی چند سطرش
**دستی عوض شده‌اند** — و همان‌ها معمولاً مهم‌ترین نکتهٔ ستون‌اند.


---

## شیت «فروردین 1405 » — 58 سطر

### `A` · رديف

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `B` · شماره پرسنلی

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `C` · واحد

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `D` · آقا / خانم

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `E` · نام ونام خانوادگي

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `F` · تاریخ استخدام

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `G` · شغل

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `H` · تاريخ تهيه

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `I` · ماه

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `J` · سال

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `K` · روز های ماه

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `L` · غیبت

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `M` · بدون حقوق

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `N` · بیماری

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `O` · کارکرد واقعی ماهانه

- **همهٔ سطرها**

  ```
  =K#-(L#+M#+N#)
  ```

### `P` · تاخیر ساعت

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `Q` · تاخیر دقیقه

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `R` · مزدروزانه

- **57 سطر**

  ```
  =ROUND((CA#*1.45)+(519549),0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `S` · پایه سنوات روزانه

- **50 سطر**

  ```
  =ROUND((5000000/30),0)
  ```

- 8 سطر به‌جای فرمول، عددِ ثابت دارند.

### `T` · جمع مزد وسنوات روزانه

- **همهٔ سطرها**

  ```
  =S#+R#
  ```

### `U` · حقوق ماهانه

- **همهٔ سطرها**

  ```
  =R#*30
  ```

### `V` · پایه سنوات ماهانه

- **همهٔ سطرها**

  ```
  =S#*30
  ```

### `W` · جمع حقوق وسنوات ماهانه

- **همهٔ سطرها**

  ```
  =V#+U#
  ```

### `X` · پایه سنوات تجمیعی روزانه

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `Y` · پایه سنوات تجمیعی ماهانه

- **همهٔ سطرها**

  ```
  =X#*O#
  ```

### `Z` · حقوق قابل پرداخت

- **همهٔ سطرها**

  ```
  =R#*O#
  ```

### `AA` · سنوات قابل پرداخت

- **همهٔ سطرها**

  ```
  =S#*O#
  ```

### `AB` · جمع حقوق وسنوات قابل پرداخت

- **همهٔ سطرها**

  ```
  =AA#+Z#
  ```

### `AC` · مابه التفاوت پایه سنوات تجمیعی قابل پرداخت

- **همهٔ سطرها**

  ```
  =ROUND(IF(OR(Y#>=AA#),Y#-AA#,IF(OR(Y#<=AA#),0)),0)
  ```

### `AD` · پایه سنوات تجمیعی قابل پرداخت

- **همهٔ سطرها**

  ```
  =AC#+AA#
  ```

### `AE` · حق مسكن قابل پرداخت

- **همهٔ سطرها**

  ```
  =ROUND(IF(OR(O#=30,O#=31),CF#,IF(OR(O#<30,O#<31),CF#/30*O#)),0)
  ```

### `AF` · حق اولاد قابل پرداخت

- **همهٔ سطرها**

  ```
  =ROUND(IF(OR(O#=30,O#=31),CH#,IF(OR(O#<30,O#<31),CH#/30*O#)),0)
  ```

### `AG` · حق تاهل قابل پرداخت

- **همهٔ سطرها**

  ```
  =ROUND(IF(OR(O#=30,O#=31),CJ#,IF(OR(O#<30,O#<31),CJ#/30*O#)),0)
  ```

### `AH` · وجه بن قابل پرداخت

- **همهٔ سطرها**

  ```
  =ROUND(IF(OR(O#=30,O#=31),CK#,IF(OR(O#<30,O#<31),CK#/30*O#)),0)
  ```

### `AI` · اضافه کاری عادی دقیقه

- **32 سطر**

  ```
  =DD#
  ```

- **26 سطر**

  ```
  =CT#
  ```

### `AJ` · اضافه کاری عادی ساعت

- **32 سطر**

  ```
  =DE#
  ```

- **26 سطر**

  ```
  =CU#
  ```

### `AK` · مبلغ اضافه كاري کل

- **32 سطر**

  ```
  =ROUND((W#/220*1.4*AJ#)+(W#/13200*1.4*AI#),0)
  ```

- **26 سطر**

  ```
  =ROUND((CS#/220*1.4*AJ#)+(CS#/13200*1.4*AI#),0)
  ```

### `AL` · وجه مرخصی

- **2 سطر**

  ```
  =ROUND((CL#/7.333*16/30*DL#),0)
  ```

- 56 سطر به‌جای فرمول، عددِ ثابت دارند.

### `AM` · پایانکار قابل پرداخت

- **2 سطر**

  ```
  =ROUND((T#*2.5/30*30),0)
  ```

- 56 سطر به‌جای فرمول، عددِ ثابت دارند.

### `AN` · عیدی وپاداش قابل پرداخت

- **2 سطر**

  ```
  =ROUND((CL#*5/30*30),0)
  ```

- 56 سطر به‌جای فرمول، عددِ ثابت دارند.

### `AO` · مدت ماموریت

- **32 سطر**

  ```
  =CY#
  ```

- 26 سطر به‌جای فرمول، عددِ ثابت دارند.

### `AP` · مبلغ ماموریت

- **35 سطر**

  ```
  =AO#*T#
  ```

- **23 سطر**

  ```
  =AO#*T#*2
  ```

### `AQ` · ذخیره پورسانت فروش

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `AR` · پورسانت یک قابل پرداخت

- **23 سطر**

  ```
  =(CN#+CQ#+AL#)-(AC#+AQ#+CM#+AP#)
  ```

- **1 سطر**

  ```
  =(CN#+CQ#+AL#)-(AC#+AQ#+CM#+AP#+CV#)
  ```

- 34 سطر به‌جای فرمول، عددِ ثابت دارند.

### `AS` · پورسانت قابل پرداخت دو

- **23 سطر**

  ```
  =CO#
  ```

- 35 سطر به‌جای فرمول، عددِ ثابت دارند.

### `AT` · پورسانت قابل پرداخت سه

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `AU` · طلب ماه قبل

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `AV` · جمع پرداختيها فیش

- **همهٔ سطرها**

  ```
  =AU#+AT#+AS#+AR#+AP#+AN#+AM#+AL#+AK#+AH#+AG#+AF#+AE#+AC#+AA#+Z#
  ```

### `AW` · علی الحساب واریزی

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `AX` · علی الحساب نقدی

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `AY` · جمع علی الحساب

- **57 سطر**

  ```
  =AW#+AX#
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `AZ` · ماخذ مالیات

- **همهٔ سطرها**

  ```
  =AV#-AP#
  ```

### `BA` · مالیات

- **همهٔ سطرها**

  ```
  =IF(AZ#<=400000000,0, IF(AZ#<=800000000,(AZ#-400000000)*10%, IF(AZ#<=1000000000,(AZ#-800000000)*15%+40000000, IF(AZ#<=1200000000,(AZ#-1000000000)*20%+70000000, IF(AZ#<=1400000000,(AZ#-1200000000)*25%+140000000, IF(AZ#>1400000001,(AZ#-1400000000)*30%+190000000))))))
  ```

### `BB` · روند مالیات

- **56 سطر**

  ```
  =ROUND((BA#),0)
  ```

- 2 سطر به‌جای فرمول، عددِ ثابت دارند.

### `BC` · ماخذ بیمه

- **55 سطر**

  ```
  =AV#-(AP#+AF#)
  ```

- 3 سطر به‌جای فرمول، عددِ ثابت دارند.

### `BD` · سهم کارگر

- **همهٔ سطرها**

  ```
  =ROUND((BC#*7%),0)
  ```

### `BE` · سهم کارفرما

- **همهٔ سطرها**

  ```
  =ROUND((BC#*20%),0)
  ```

### `BF` · بیمه بیکاری

- **همهٔ سطرها**

  ```
  =ROUND((BC#*3%),0)
  ```

### `BG` · جمع کل بیمه

- **37 سطر**

  ```
  =ROUND((BF#+BE#+BD#),0)
  ```

- **21 سطر**

  ```
  =BF#+BE#+BD#
  ```

### `BH` · مبلغ کل وام

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `BI` · تعدادكل قسط

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `BJ` · مبلغ قسط

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `BK` · شماره قسط

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `BL` · مانده قسط

- **55 سطر**

  ```
  =BI#-BK#
  ```

- 3 سطر به‌جای فرمول، عددِ ثابت دارند.

### `BM` · مانده وام

- **54 سطر**

  ```
  =BL#*BJ#
  ```

- 4 سطر به‌جای فرمول، عددِ ثابت دارند.

### `BN` · خرید از شرکت

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `BO` · بدهی از ماه قبل

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `BP` · کسر انبار

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `BQ` · بیمه تکمیلی

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `BR` · جرائم وخلافی خودروها

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `BS` · آزمایش وطب کار

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `BT` · ماخد کسر کار - دیر کرد

- **همهٔ سطرها**

  ```
  =U#+V#+CF#+CH#+CJ#+CK#
  ```

### `BU` · مبلغ کسر کار - دیر کرد

- **57 سطر**

  ```
  =ROUND((BT#/220*P#)+(BT#/11440*Q#),0)
  ```

- **1 سطر**

  ```
  =ROUND((CV#*23%),0)
  ```

### `BV` · جمع كسورات

- **همهٔ سطرها**

  ```
  =BU#+BS#+BR#+BQ#+BP#+BO#+BN#+BJ#+BD#+BB#+AY#
  ```

### `BW` · خالص پرداخت

- **همهٔ سطرها**

  ```
  =AV#-BV#
  ```

### `BX` · شماره کارت

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `BY` · شماره حساب

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `BZ` · بانك عامل

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `CA` · مزدروزانه 1404

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `CB` · پایه سنوات تجمیعی روزانه سال 1405

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `CC` · پایه سنوات تجمیعی ماهانه سال 1405

- **همهٔ سطرها**

  ```
  =CB#*30
  ```

### `CD` · حداقل دستمزد 1405

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `CE` · پایه سنوات روزانه 1405

- **45 سطر**

  ```
  =ROUND((5000000/30),0)
  ```

- 13 سطر به‌جای فرمول، عددِ ثابت دارند.

### `CF` · حق مسکن

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `CG` · تعداد اولاد

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `CH` · حق اولاد

- **همهٔ سطرها**

  ```
  =CG#*CD#*3
  ```

### `CI` · وضعیت تاهل

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `CJ` · حق تاهل

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `CK` · وجه بن

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `CL` · ماخذ پایانکار وعیدی وپاداش ووجه مرخصی وماموریت

- **34 سطر**

  ```
  =T#
  ```

- 24 سطر به‌جای فرمول، عددِ ثابت دارند.

### `CM` · کسر مازاد پرداختی فروش

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `CN` · پورسانت یک

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `CO` · پورسانت دو

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `CP` · پورسانت سه

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `CQ` · مازاد ثابت

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `CR` · مازاد پرداختی ماهانه

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `CS` · ماخذ محاسبه اضافه کاری

- **همهٔ سطرها**

  ```
  =W#
  ```

### `CT` · اضافه کارواقعی دقیقه

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `CU` · اضافه کار واقعی ساعت

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `CV` · مبلغ اضافه کار واقعی

- **همهٔ سطرها**

  ```
  =ROUND((CS#/220*1.4*CU#)+(CS#/13200*1.4*CT#),0)
  ```

### `CW` · جمع مازاد ثابت / مازاد پرداختی ماهانه / اضافه کار واقعی ومرخصی

- **55 سطر**

  ```
  =CV#+CR#+CQ#+AL#
  ```

- **3 سطر**

  ```
  =CV#+CR#+CQ#
  ```

### `CX` · قابل پرداخت مازاد ثابت / اضافه کار ثابت / اضافه کار واقعی

- **32 سطر**

  ```
  =CW#-AC#
  ```

- 26 سطر به‌جای فرمول، عددِ ثابت دارند.

### `CY` · محاسبه مدت ماموریت

- **24 سطر**

  ```
  =ROUNDDOWN((CX#/CL#),0)
  ```

- **1 سطر**

  ```
  =ROUNDDOWN((CR#/CL#),0)
  ```

- 33 سطر به‌جای فرمول، عددِ ثابت دارند.

### `CZ` · محاسبه مبلغ ماموریت

- **31 سطر**

  ```
  =CY#*CL#
  ```

- **1 سطر**

  ```
  =CR#
  ```

- 26 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DA` · مازاد محاسبه یک

- **32 سطر**

  ```
  =CX#-CZ#
  ```

- **1 سطر**

  ```
  =CW#-CZ#
  ```

- **1 سطر**

  ```
  =(CX#-CZ#)+(CZ#-AP#)
  ```

- 24 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DB` · اضافه کار هر ساعت

- **34 سطر**

  ```
  =ROUND((W#/220*1.4*1),0)
  ```

- 24 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DC` · اضافه کار هردقیقه

- **34 سطر**

  ```
  =ROUND((W#/13200*1.4*1),0)
  ```

- 24 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DD` · محاسبه اضافه کار دقیقه

- **34 سطر**

  ```
  =ROUNDUP(IF(OR(DH#<DC#),0,IF(OR(DH#>DC#),DH#/DC#)),0)
  ```

- 24 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DE` · محاسبه اضافه کار ساعت

- **26 سطر**

  ```
  =ROUNDDOWN((DA#/DB#),0)
  ```

- 32 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DF` · مبلغ اضافه کار محاسبه دقیقه

- **32 سطر**

  ```
  =ROUND((W#/13200*1.4*DD#),0)
  ```

- **2 سطر**

  ```
  =ROUND((CS#/13200*1.4*DD#),0)
  ```

- 24 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DG` · مبلغ اضافه کار محاسبه ساعت

- **32 سطر**

  ```
  =ROUND((W#/220*1.4*DE#),0)
  ```

- **2 سطر**

  ```
  =ROUND((CS#/220*1.4*DE#),0)
  ```

- 24 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DH` · مازاد محاسبه دو

- **34 سطر**

  ```
  =DA#-DG#
  ```

- 24 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DI` · مازاد محاسبه سه

- **34 سطر**

  ```
  =DH#-DF#
  ```

- 24 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DJ` · مانده مرخصی قابل استفاده در آخر سال 1404

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `DK` · مرخصی انتقالی از سال 1404 به روز

- **56 سطر**

  ```
  =ROUND(IF(OR(DJ#=9,DJ#>9),9,IF(OR(DJ#<9,),DJ#)),0)
  ```

- 2 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DL` · مرخصی استحقاق سال 1405 به روز

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `DM` · جمع مر خصی قابل استفاده در سال 1405 به روز

- **56 سطر**

  ```
  =DL#+DK#
  ```

- **2 سطر**

  ```
  =ROUND(IF(OR(O#=30,O#=31),DL#,IF(OR(O#<30,O#<31),DL#/30*60)),0)
  ```

### `DN` · مرخصی قابل استفاده سال 1405 به ساعت

- **56 سطر**

  ```
  =ROUNDDOWN((DM#*440)/60,0)
  ```

- **2 سطر**

  ```
  =ROUNDDOWN((DM#/60),0)
  ```

### `DO` · جمع مرخصی قابل استفاده سال 1405 به دقیقه

- **56 سطر**

  ```
  =ROUNDDOWN((DM#*440)-(DN#*60),0)
  ```

- 2 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DP` · مصرفی ماه اعلامی دقیقه

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `DQ` · مصرفی ماه اعلامی ساعت

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `DR` · مصرفی ماه اعلامی روز

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `DS` · مصرفی ماه واقعی دقیقه

- **56 سطر**

  ```
  =ROUNDDOWN((DR#*440+DQ#*60+DP#-(DU#*440+DT#*60)),0)
  ```

- 2 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DT` · مصرفی ماه واقعی ساعت

- **56 سطر**

  ```
  =ROUNDDOWN((DR#*440+DQ#*60+DP#-DU#*440)/60,0)
  ```

- 2 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DU` · مصرفی ماه واقعی روز

- **56 سطر**

  ```
  =ROUNDDOWN((DR#*440+DQ#*60+DP#)/440,0)
  ```

- 2 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DV` · مصرفی اعلامی تا ماه دقیقه

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `DW` · مصرفی اعلامی تا ماه ساعت

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `DX` · مصرفی اعلامی تا ماه روز

عدد یا متنِ ورودی — 58 سطر مقدار دارد.

### `DY` · مصرفی تا ماه دقیقه

- **56 سطر**

  ```
  =ROUNDDOWN((DX#*440+DW#*60+DV#-(EA#*440+DZ#*60)),0)
  ```

- 2 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DZ` · مصرفی تا ماه ساعت

- **56 سطر**

  ```
  =ROUNDDOWN((DX#*440+DW#*60+DV#-EA#*440)/60,0)
  ```

- 2 سطر به‌جای فرمول، عددِ ثابت دارند.

### `EA` · مصرفی تا ماه روز

- **56 سطر**

  ```
  =ROUNDDOWN((DX#*440+DW#*60+DV#)/440,0)
  ```

- 2 سطر به‌جای فرمول، عددِ ثابت دارند.

### `EB` · استفاده شده کل تا ماه دقیقه

- **56 سطر**

  ```
  =ROUNDDOWN((EA#*440+DZ#*60+DY#),0)
  ```

- 2 سطر به‌جای فرمول، عددِ ثابت دارند.

### `EC` · مانده تا آخر سال دقیقه

- **56 سطر**

  ```
  =ROUNDDOWN((DM#*440-EB#-(EE#*440+ED#*60)),0)
  ```

- 2 سطر به‌جای فرمول، عددِ ثابت دارند.

### `ED` · مانده تا آخر سال ساعت

- **56 سطر**

  ```
  =ROUNDDOWN((DM#*440-EB#-EE#*440)/60,0)
  ```

- 2 سطر به‌جای فرمول، عددِ ثابت دارند.

### `EE` · مانده تا آخر سال روز

- **56 سطر**

  ```
  =ROUNDDOWN((DM#*440-EB#)/440,0)
  ```

- 2 سطر به‌جای فرمول، عددِ ثابت دارند.


---

## شیت «اردیبهشت 1405» — 71 سطر

### `A` · رديف

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `B` · شماره پرسنلی

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `C` · واحد

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `D` · آقا / خانم

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `E` · نام ونام خانوادگي

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `F` · تاریخ استخدام

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `G` · شغل

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `H` · تاريخ تهيه

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `I` · ماه

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `J` · سال

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `K` · روز های ماه

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `L` · غیبت

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `M` · بدون حقوق

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `N` · بیماری

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `O` · کارکرد واقعی ماهانه

- **همهٔ سطرها**

  ```
  =K#-(L#+M#+N#)
  ```

### `P` · تاخیر ساعت

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `Q` · تاخیر دقیقه

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `R` · مزدروزانه

- **70 سطر**

  ```
  =ROUND((CA#*1.45)+(519549),0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `S` · پایه سنوات روزانه

- **50 سطر**

  ```
  =ROUND((5000000/30),0)
  ```

- 21 سطر به‌جای فرمول، عددِ ثابت دارند.

### `T` · جمع مزد وسنوات روزانه

- **همهٔ سطرها**

  ```
  =S#+R#
  ```

### `U` · حقوق ماهانه

- **همهٔ سطرها**

  ```
  =R#*30
  ```

### `V` · پایه سنوات ماهانه

- **همهٔ سطرها**

  ```
  =S#*30
  ```

### `W` · جمع حقوق وسنوات ماهانه

- **همهٔ سطرها**

  ```
  =V#+U#
  ```

### `X` · پایه سنوات تجمیعی روزانه

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `Y` · پایه سنوات تجمیعی ماهانه

- **همهٔ سطرها**

  ```
  =X#*O#
  ```

### `Z` · حقوق قابل پرداخت

- **همهٔ سطرها**

  ```
  =R#*O#
  ```

### `AA` · سنوات قابل پرداخت

- **همهٔ سطرها**

  ```
  =S#*O#
  ```

### `AB` · جمع حقوق وسنوات قابل پرداخت

- **همهٔ سطرها**

  ```
  =AA#+Z#
  ```

### `AC` · مابه التفاوت پایه سنوات تجمیعی قابل پرداخت

- **همهٔ سطرها**

  ```
  =ROUND(IF(OR(Y#>=AA#),Y#-AA#,IF(OR(Y#<=AA#),0)),0)
  ```

### `AD` · پایه سنوات تجمیعی قابل پرداخت

- **همهٔ سطرها**

  ```
  =AC#+AA#
  ```

### `AE` · حق مسكن قابل پرداخت

- **همهٔ سطرها**

  ```
  =ROUND(IF(OR(O#=30,O#=31),CF#,IF(OR(O#<30,O#<31),CF#/31*O#)),0)
  ```

### `AF` · حق اولاد قابل پرداخت

- **همهٔ سطرها**

  ```
  =ROUND(IF(OR(O#=30,O#=31),CH#,IF(OR(O#<30,O#<31),CH#/31*O#)),0)
  ```

### `AG` · حق تاهل قابل پرداخت

- **همهٔ سطرها**

  ```
  =ROUND(IF(OR(O#=30,O#=31),CJ#,IF(OR(O#<30,O#<31),CJ#/31*O#)),0)
  ```

### `AH` · وجه بن قابل پرداخت

- **همهٔ سطرها**

  ```
  =ROUND(IF(OR(O#=30,O#=31),CK#,IF(OR(O#<30,O#<31),CK#/31*O#)),0)
  ```

### `AI` · اضافه کاری عادی دقیقه

- **45 سطر**

  ```
  =DD#
  ```

- **26 سطر**

  ```
  =CT#
  ```

### `AJ` · اضافه کاری عادی ساعت

- **45 سطر**

  ```
  =DE#
  ```

- **26 سطر**

  ```
  =CU#
  ```

### `AK` · مبلغ اضافه كاري کل

- **45 سطر**

  ```
  =ROUND((W#/220*1.4*AJ#)+(W#/13200*1.4*AI#),0)
  ```

- **26 سطر**

  ```
  =ROUND((CS#/220*1.4*AJ#)+(CS#/13200*1.4*AI#),0)
  ```

### `AL` · وجه مرخصی

- **2 سطر**

  ```
  =ROUND((CL#/7.333*16/30*DL#),0)
  ```

- 69 سطر به‌جای فرمول، عددِ ثابت دارند.

### `AM` · پایانکار قابل پرداخت

- **2 سطر**

  ```
  =ROUND((T#*2.5/30*30),0)
  ```

- 69 سطر به‌جای فرمول، عددِ ثابت دارند.

### `AN` · عیدی وپاداش قابل پرداخت

- **2 سطر**

  ```
  =ROUND((CL#*5/30*30),0)
  ```

- 69 سطر به‌جای فرمول، عددِ ثابت دارند.

### `AO` · مدت ماموریت

- **44 سطر**

  ```
  =CY#
  ```

- 27 سطر به‌جای فرمول، عددِ ثابت دارند.

### `AP` · مبلغ ماموریت

- **47 سطر**

  ```
  =AO#*T#
  ```

- **24 سطر**

  ```
  =AO#*T#*2
  ```

### `AQ` · ذخیره پورسانت فروش

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `AR` · پورسانت یک قابل پرداخت

- **23 سطر**

  ```
  =(CN#+CQ#+AL#)-(AC#+AQ#+CM#+AP#)
  ```

- **1 سطر**

  ```
  =CN#
  ```

- **1 سطر**

  ```
  =(CN#+CQ#+AL#)-(AC#+AQ#+CM#+AP#+CV#)
  ```

- 46 سطر به‌جای فرمول، عددِ ثابت دارند.

### `AS` · پورسانت قابل پرداخت دو

- **23 سطر**

  ```
  =CO#
  ```

- 48 سطر به‌جای فرمول، عددِ ثابت دارند.

### `AT` · پورسانت قابل پرداخت سه

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `AU` · طلب ماه قبل

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `AV` · جمع پرداختيها فیش

- **همهٔ سطرها**

  ```
  =AU#+AT#+AS#+AR#+AP#+AN#+AM#+AL#+AK#+AH#+AG#+AF#+AE#+AC#+AA#+Z#
  ```

### `AW` · علی الحساب واریزی

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `AX` · علی الحساب نقدی

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `AY` · جمع علی الحساب

- **70 سطر**

  ```
  =AW#+AX#
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `AZ` · ماخذ مالیات

- **همهٔ سطرها**

  ```
  =AV#-AP#
  ```

### `BA` · مالیات

- **همهٔ سطرها**

  ```
  =IF(AZ#<=400000000,0, IF(AZ#<=800000000,(AZ#-400000000)*10%, IF(AZ#<=1000000000,(AZ#-800000000)*15%+40000000, IF(AZ#<=1200000000,(AZ#-1000000000)*20%+70000000, IF(AZ#<=1400000000,(AZ#-1200000000)*25%+140000000, IF(AZ#>1400000001,(AZ#-1400000000)*30%+190000000))))))
  ```

### `BB` · روند مالیات

- **69 سطر**

  ```
  =ROUND((BA#),0)
  ```

- 2 سطر به‌جای فرمول، عددِ ثابت دارند.

### `BC` · ماخذ بیمه

- **66 سطر**

  ```
  =AV#-(AP#+AF#)
  ```

- 5 سطر به‌جای فرمول، عددِ ثابت دارند.

### `BD` · سهم کارگر

- **همهٔ سطرها**

  ```
  =ROUND((BC#*7%),0)
  ```

### `BE` · سهم کارفرما

- **همهٔ سطرها**

  ```
  =ROUND((BC#*20%),0)
  ```

### `BF` · بیمه بیکاری

- **همهٔ سطرها**

  ```
  =ROUND((BC#*3%),0)
  ```

### `BG` · جمع کل بیمه

- **50 سطر**

  ```
  =ROUND((BF#+BE#+BD#),0)
  ```

- **21 سطر**

  ```
  =BF#+BE#+BD#
  ```

### `BH` · مبلغ کل وام

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `BI` · تعدادكل قسط

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `BJ` · مبلغ قسط

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `BK` · شماره قسط

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `BL` · مانده قسط

- **68 سطر**

  ```
  =BI#-BK#
  ```

- 3 سطر به‌جای فرمول، عددِ ثابت دارند.

### `BM` · مانده وام

- **67 سطر**

  ```
  =BL#*BJ#
  ```

- 4 سطر به‌جای فرمول، عددِ ثابت دارند.

### `BN` · خرید از شرکت

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `BO` · بدهی از ماه قبل

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `BP` · سایر بدهی

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `BQ` · بیمه تکمیلی

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `BR` · جرائم وخلافی خودروها

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `BS` · آزمایش وطب کار

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `BT` · ماخد کسر کار - دیر کرد

- **همهٔ سطرها**

  ```
  =U#+V#+CF#+CH#+CJ#+CK#
  ```

### `BU` · مبلغ کسر کار - دیر کرد

- **70 سطر**

  ```
  =ROUND((BT#/220*P#)+(BT#/11440*Q#),0)
  ```

- **1 سطر**

  ```
  =ROUND((CV#*23%),0)
  ```

### `BV` · جمع كسورات

- **همهٔ سطرها**

  ```
  =BU#+BS#+BR#+BQ#+BP#+BO#+BN#+BJ#+BD#+BB#+AW#
  ```

### `BW` · خالص پرداخت

- **همهٔ سطرها**

  ```
  =AV#-BV#
  ```

### `BX` · شماره کارت

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `BY` · شماره حساب

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `BZ` · بانك عامل

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `CA` · مزدروزانه 1404

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `CB` · پایه سنوات تجمیعی روزانه سال 1405

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `CC` · پایه سنوات تجمیعی ماهانه سال 1405

- **همهٔ سطرها**

  ```
  =CB#*30
  ```

### `CD` · حداقل دستمزد 1405

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `CE` · پایه سنوات روزانه 1405

- **45 سطر**

  ```
  =ROUND((5000000/30),0)
  ```

- 26 سطر به‌جای فرمول، عددِ ثابت دارند.

### `CF` · حق مسکن

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `CG` · تعداد اولاد

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `CH` · حق اولاد

- **همهٔ سطرها**

  ```
  =CG#*CD#*3
  ```

### `CI` · وضعیت تاهل

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `CJ` · حق تاهل

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `CK` · وجه بن

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `CL` · ماخذ پایانکار وعیدی وپاداش ووجه مرخصی وماموریت

- **46 سطر**

  ```
  =T#
  ```

- 25 سطر به‌جای فرمول، عددِ ثابت دارند.

### `CM` · کسر مازاد پرداختی فروش

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `CN` · پورسانت یک

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `CO` · پورسانت دو

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `CP` · پورسانت سه

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `CQ` · مازاد ثابت

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `CR` · مازاد پرداختی ماهانه

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `CS` · ماخذ محاسبه اضافه کاری

- **همهٔ سطرها**

  ```
  =W#
  ```

### `CT` · اضافه کارواقعی دقیقه

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `CU` · اضافه کار واقعی ساعت

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `CV` · مبلغ اضافه کار واقعی

- **همهٔ سطرها**

  ```
  =ROUND((CS#/220*1.4*CU#)+(CS#/13200*1.4*CT#),0)
  ```

### `CW` · جمع مازاد ثابت / مازاد پرداختی ماهانه / اضافه کار واقعی ومرخصی

- **68 سطر**

  ```
  =CV#+CR#+CQ#+AL#
  ```

- **3 سطر**

  ```
  =CV#+CR#+CQ#
  ```

### `CX` · قابل پرداخت مازاد ثابت / اضافه کار ثابت / اضافه کار واقعی

- **44 سطر**

  ```
  =CW#-AC#
  ```

- 27 سطر به‌جای فرمول، عددِ ثابت دارند.

### `CY` · محاسبه مدت ماموریت

- **44 سطر**

  ```
  =ROUNDDOWN((CX#/CL#),0)
  ```

- 27 سطر به‌جای فرمول، عددِ ثابت دارند.

### `CZ` · محاسبه مبلغ ماموریت

- **44 سطر**

  ```
  =CY#*CL#
  ```

- 27 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DA` · مازاد محاسبه یک

- **46 سطر**

  ```
  =CX#-CZ#
  ```

- **1 سطر**

  ```
  =CW#-CZ#
  ```

- 24 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DB` · اضافه کار هر ساعت

- **46 سطر**

  ```
  =ROUND((W#/220*1.4*1),0)
  ```

- 25 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DC` · اضافه کار هردقیقه

- **46 سطر**

  ```
  =ROUND((W#/13200*1.4*1),0)
  ```

- 25 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DD` · محاسبه اضافه کار دقیقه

- **46 سطر**

  ```
  =ROUNDUP(IF(OR(DH#<DC#),0,IF(OR(DH#>DC#),DH#/DC#)),0)
  ```

- 25 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DE` · محاسبه اضافه کار ساعت

- **46 سطر**

  ```
  =ROUNDDOWN((DA#/DB#),0)
  ```

- 25 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DF` · مبلغ اضافه کار محاسبه دقیقه

- **44 سطر**

  ```
  =ROUND((W#/13200*1.4*DD#),0)
  ```

- **2 سطر**

  ```
  =ROUND((CS#/13200*1.4*DD#),0)
  ```

- 25 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DG` · مبلغ اضافه کار محاسبه ساعت

- **44 سطر**

  ```
  =ROUND((W#/220*1.4*DE#),0)
  ```

- **2 سطر**

  ```
  =ROUND((CS#/220*1.4*DE#),0)
  ```

- 25 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DH` · مازاد محاسبه دو

- **46 سطر**

  ```
  =DA#-DG#
  ```

- 25 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DI` · مازاد محاسبه سه

- **46 سطر**

  ```
  =DH#-DF#
  ```

- 25 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DJ` · مانده مرخصی قابل استفاده در آخر سال 1404

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `DK` · مرخصی انتقالی از سال 1404 به روز

- **69 سطر**

  ```
  =ROUND(IF(OR(DJ#=9,DJ#>9),9,IF(OR(DJ#<9,),DJ#)),0)
  ```

- 2 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DL` · مرخصی استحقاق سال 1405 به روز

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `DM` · جمع مر خصی قابل استفاده در سال 1405 به روز

- **69 سطر**

  ```
  =DL#+DK#
  ```

- **2 سطر**

  ```
  =ROUND(IF(OR(O#=30,O#=31),DL#,IF(OR(O#<30,O#<31),DL#/30*60)),0)
  ```

### `DN` · مرخصی قابل استفاده سال 1405 به ساعت

- **69 سطر**

  ```
  =ROUNDDOWN((DM#*440)/60,0)
  ```

- **2 سطر**

  ```
  =ROUNDDOWN((DM#/60),0)
  ```

### `DO` · جمع مرخصی قابل استفاده سال 1405 به دقیقه

- **69 سطر**

  ```
  =ROUNDDOWN((DM#*440)-(DN#*60),0)
  ```

- 2 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DP` · مصرفی ماه اعلامی دقیقه

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `DQ` · مصرفی ماه اعلامی ساعت

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `DR` · مصرفی ماه اعلامی روز

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `DS` · مصرفی ماه واقعی دقیقه

- **69 سطر**

  ```
  =ROUNDDOWN((DR#*440+DQ#*60+DP#-(DU#*440+DT#*60)),0)
  ```

- 2 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DT` · مصرفی ماه واقعی ساعت

- **69 سطر**

  ```
  =ROUNDDOWN((DR#*440+DQ#*60+DP#-DU#*440)/60,0)
  ```

- 2 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DU` · مصرفی ماه واقعی روز

- **69 سطر**

  ```
  =ROUNDDOWN((DR#*440+DQ#*60+DP#)/440,0)
  ```

- 2 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DV` · مصرفی اعلامی تا ماه دقیقه

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `DW` · مصرفی اعلامی تا ماه ساعت

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `DX` · مصرفی اعلامی تا ماه روز

عدد یا متنِ ورودی — 71 سطر مقدار دارد.

### `DY` · مصرفی تا ماه دقیقه

- **69 سطر**

  ```
  =ROUNDDOWN((DX#*440+DW#*60+DV#-(EA#*440+DZ#*60)),0)
  ```

- 2 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DZ` · مصرفی تا ماه ساعت

- **69 سطر**

  ```
  =ROUNDDOWN((DX#*440+DW#*60+DV#-EA#*440)/60,0)
  ```

- 2 سطر به‌جای فرمول، عددِ ثابت دارند.

### `EA` · مصرفی تا ماه روز

- **69 سطر**

  ```
  =ROUNDDOWN((DX#*440+DW#*60+DV#)/440,0)
  ```

- 2 سطر به‌جای فرمول، عددِ ثابت دارند.

### `EB` · استفاده شده کل تا ماه دقیقه

- **69 سطر**

  ```
  =ROUNDDOWN((EA#*440+DZ#*60+DY#),0)
  ```

- 2 سطر به‌جای فرمول، عددِ ثابت دارند.

### `EC` · مانده تا آخر سال دقیقه

- **69 سطر**

  ```
  =ROUNDDOWN((DM#*440-EB#-(EE#*440+ED#*60)),0)
  ```

- 2 سطر به‌جای فرمول، عددِ ثابت دارند.

### `ED` · مانده تا آخر سال ساعت

- **69 سطر**

  ```
  =ROUNDDOWN((DM#*440-EB#-EE#*440)/60,0)
  ```

- 2 سطر به‌جای فرمول، عددِ ثابت دارند.

### `EE` · مانده تا آخر سال روز

- **69 سطر**

  ```
  =ROUNDDOWN((DM#*440-EB#)/440,0)
  ```

- 2 سطر به‌جای فرمول، عددِ ثابت دارند.


---

## شیت «اردیبهشت 1405با عدم بیمه» — 65 سطر

### `A` · رديف

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `B` · شماره پرسنلی

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `C` · واحد

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `D` · آقا / خانم

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `E` · نام ونام خانوادگي

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `F` · تاریخ استخدام

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `G` · شغل

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `H` · تاريخ تهيه

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `I` · ماه

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `J` · سال

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `K` · روز های ماه

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `L` · غیبت

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `M` · بدون حقوق

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `N` · بیماری

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `O` · کارکرد واقعی ماهانه

- **همهٔ سطرها**

  ```
  =K#-(L#+M#+N#)
  ```

### `P` · تاخیر ساعت

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `Q` · تاخیر دقیقه

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `R` · مزدروزانه

- **همهٔ سطرها**

  ```
  =ROUND((CA#*1.45)+(519549),0)
  ```

### `S` · پایه سنوات روزانه

- **47 سطر**

  ```
  =ROUND((5000000/30),0)
  ```

- 18 سطر به‌جای فرمول، عددِ ثابت دارند.

### `T` · جمع مزد وسنوات روزانه

- **همهٔ سطرها**

  ```
  =S#+R#
  ```

### `U` · حقوق ماهانه

- **همهٔ سطرها**

  ```
  =R#*30
  ```

### `V` · پایه سنوات ماهانه

- **همهٔ سطرها**

  ```
  =S#*30
  ```

### `W` · جمع حقوق وسنوات ماهانه

- **همهٔ سطرها**

  ```
  =V#+U#
  ```

### `X` · پایه سنوات تجمیعی روزانه

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `Y` · پایه سنوات تجمیعی ماهانه

- **همهٔ سطرها**

  ```
  =X#*O#
  ```

### `Z` · حقوق قابل پرداخت

- **همهٔ سطرها**

  ```
  =R#*O#
  ```

### `AA` · سنوات قابل پرداخت

- **همهٔ سطرها**

  ```
  =S#*O#
  ```

### `AB` · جمع حقوق وسنوات قابل پرداخت

- **همهٔ سطرها**

  ```
  =AA#+Z#
  ```

### `AC` · مابه التفاوت پایه سنوات تجمیعی قابل پرداخت

- **همهٔ سطرها**

  ```
  =ROUND(IF(OR(Y#>=AA#),Y#-AA#,IF(OR(Y#<=AA#),0)),0)
  ```

### `AD` · پایه سنوات تجمیعی قابل پرداخت

- **همهٔ سطرها**

  ```
  =AC#+AA#
  ```

### `AE` · حق مسكن قابل پرداخت

- **همهٔ سطرها**

  ```
  =ROUND(IF(OR(O#=30,O#=31),CF#,IF(OR(O#<30,O#<31),CF#/31*O#)),0)
  ```

### `AF` · حق اولاد قابل پرداخت

- **همهٔ سطرها**

  ```
  =ROUND(IF(OR(O#=30,O#=31),CH#,IF(OR(O#<30,O#<31),CH#/31*O#)),0)
  ```

### `AG` · حق تاهل قابل پرداخت

- **همهٔ سطرها**

  ```
  =ROUND(IF(OR(O#=30,O#=31),CJ#,IF(OR(O#<30,O#<31),CJ#/31*O#)),0)
  ```

### `AH` · وجه بن قابل پرداخت

- **همهٔ سطرها**

  ```
  =ROUND(IF(OR(O#=30,O#=31),CK#,IF(OR(O#<30,O#<31),CK#/31*O#)),0)
  ```

### `AI` · اضافه کاری عادی دقیقه

- **40 سطر**

  ```
  =DD#
  ```

- **25 سطر**

  ```
  =CT#
  ```

### `AJ` · اضافه کاری عادی ساعت

- **40 سطر**

  ```
  =DE#
  ```

- **25 سطر**

  ```
  =CU#
  ```

### `AK` · مبلغ اضافه كاري کل

- **40 سطر**

  ```
  =ROUND((W#/220*1.4*AJ#)+(W#/13200*1.4*AI#),0)
  ```

- **25 سطر**

  ```
  =ROUND((CS#/220*1.4*AJ#)+(CS#/13200*1.4*AI#),0)
  ```

### `AL` · وجه مرخصی

- **2 سطر**

  ```
  =ROUND((CL#/7.333*16/30*DL#),0)
  ```

- 63 سطر به‌جای فرمول، عددِ ثابت دارند.

### `AM` · پایانکار قابل پرداخت

- **2 سطر**

  ```
  =ROUND((T#*2.5/30*30),0)
  ```

- 63 سطر به‌جای فرمول، عددِ ثابت دارند.

### `AN` · عیدی وپاداش قابل پرداخت

- **2 سطر**

  ```
  =ROUND((CL#*5/30*30),0)
  ```

- 63 سطر به‌جای فرمول، عددِ ثابت دارند.

### `AO` · مدت ماموریت

- **39 سطر**

  ```
  =CY#
  ```

- 26 سطر به‌جای فرمول، عددِ ثابت دارند.

### `AP` · مبلغ ماموریت

- **42 سطر**

  ```
  =AO#*T#
  ```

- **23 سطر**

  ```
  =AO#*T#*2
  ```

### `AQ` · ذخیره پورسانت فروش

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `AR` · پورسانت یک قابل پرداخت

- **22 سطر**

  ```
  =(CN#+CQ#+AL#)-(AC#+AQ#+CM#+AP#)
  ```

- **1 سطر**

  ```
  =CN#
  ```

- **1 سطر**

  ```
  =(CN#+CQ#+AL#)-(AC#+AQ#+CM#+AP#+CV#)
  ```

- 41 سطر به‌جای فرمول، عددِ ثابت دارند.

### `AS` · پورسانت قابل پرداخت دو

- **22 سطر**

  ```
  =CO#
  ```

- 43 سطر به‌جای فرمول، عددِ ثابت دارند.

### `AT` · پورسانت قابل پرداخت سه

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `AU` · طلب ماه قبل

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `AV` · جمع پرداختيها فیش

- **همهٔ سطرها**

  ```
  =AU#+AT#+AS#+AR#+AP#+AN#+AM#+AL#+AK#+AH#+AG#+AF#+AE#+AC#+AA#+Z#
  ```

### `AW` · علی الحساب واریزی

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `AX` · علی الحساب نقدی

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `AY` · جمع علی الحساب

- **64 سطر**

  ```
  =AW#+AX#
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `AZ` · ماخذ مالیات

- **همهٔ سطرها**

  ```
  =AV#-AP#
  ```

### `BA` · مالیات

- **همهٔ سطرها**

  ```
  =IF(AZ#<=400000000,0, IF(AZ#<=800000000,(AZ#-400000000)*10%, IF(AZ#<=1000000000,(AZ#-800000000)*15%+40000000, IF(AZ#<=1200000000,(AZ#-1000000000)*20%+70000000, IF(AZ#<=1400000000,(AZ#-1200000000)*25%+140000000, IF(AZ#>1400000001,(AZ#-1400000000)*30%+190000000))))))
  ```

### `BB` · روند مالیات

- **63 سطر**

  ```
  =ROUND((BA#),0)
  ```

- 2 سطر به‌جای فرمول، عددِ ثابت دارند.

### `BC` · ماخذ بیمه

- **همهٔ سطرها**

  ```
  =AV#-(AP#+AF#)
  ```

### `BD` · سهم کارگر

- **همهٔ سطرها**

  ```
  =ROUND((BC#*7%),0)
  ```

### `BE` · سهم کارفرما

- **همهٔ سطرها**

  ```
  =ROUND((BC#*20%),0)
  ```

### `BF` · بیمه بیکاری

- **همهٔ سطرها**

  ```
  =ROUND((BC#*3%),0)
  ```

### `BG` · جمع کل بیمه

- **45 سطر**

  ```
  =ROUND((BF#+BE#+BD#),0)
  ```

- **20 سطر**

  ```
  =BF#+BE#+BD#
  ```

### `BH` · مبلغ کل وام

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `BI` · تعدادكل قسط

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `BJ` · مبلغ قسط

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `BK` · شماره قسط

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `BL` · مانده قسط

- **62 سطر**

  ```
  =BI#-BK#
  ```

- 3 سطر به‌جای فرمول، عددِ ثابت دارند.

### `BM` · مانده وام

- **61 سطر**

  ```
  =BL#*BJ#
  ```

- 4 سطر به‌جای فرمول، عددِ ثابت دارند.

### `BN` · خرید از شرکت

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `BO` · بدهی از ماه قبل

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `BP` · سایر بدهی

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `BQ` · بیمه تکمیلی

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `BR` · جرائم وخلافی خودروها

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `BS` · آزمایش وطب کار

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `BT` · ماخد کسر کار - دیر کرد

- **همهٔ سطرها**

  ```
  =U#+V#+CF#+CH#+CJ#+CK#
  ```

### `BU` · مبلغ کسر کار - دیر کرد

- **64 سطر**

  ```
  =ROUND((BT#/220*P#)+(BT#/11440*Q#),0)
  ```

- **1 سطر**

  ```
  =ROUND((CV#*23%),0)
  ```

### `BV` · جمع كسورات

- **همهٔ سطرها**

  ```
  =BU#+BS#+BR#+BQ#+BP#+BO#+BN#+BJ#+BD#+BB#+AW#
  ```

### `BW` · خالص پرداخت

- **همهٔ سطرها**

  ```
  =AV#-BV#
  ```

### `BX` · شماره کارت

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `BY` · شماره حساب

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `BZ` · بانك عامل

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `CA` · مزدروزانه 1404

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `CB` · پایه سنوات تجمیعی روزانه سال 1405

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `CC` · پایه سنوات تجمیعی ماهانه سال 1405

- **همهٔ سطرها**

  ```
  =CB#*30
  ```

### `CD` · حداقل دستمزد 1405

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `CE` · پایه سنوات روزانه 1405

- **41 سطر**

  ```
  =ROUND((5000000/30),0)
  ```

- 24 سطر به‌جای فرمول، عددِ ثابت دارند.

### `CF` · حق مسکن

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `CG` · تعداد اولاد

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `CH` · حق اولاد

- **همهٔ سطرها**

  ```
  =CG#*CD#*3
  ```

### `CI` · وضعیت تاهل

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `CJ` · حق تاهل

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `CK` · وجه بن

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `CL` · ماخذ پایانکار وعیدی وپاداش ووجه مرخصی وماموریت

- **41 سطر**

  ```
  =T#
  ```

- 24 سطر به‌جای فرمول، عددِ ثابت دارند.

### `CM` · کسر مازاد پرداختی فروش

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `CN` · پورسانت یک

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `CO` · پورسانت دو

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `CP` · پورسانت سه

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `CQ` · مازاد ثابت

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `CR` · مازاد پرداختی ماهانه

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `CS` · ماخذ محاسبه اضافه کاری

- **همهٔ سطرها**

  ```
  =W#
  ```

### `CT` · اضافه کارواقعی دقیقه

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `CU` · اضافه کار واقعی ساعت

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `CV` · مبلغ اضافه کار واقعی

- **همهٔ سطرها**

  ```
  =ROUND((CS#/220*1.4*CU#)+(CS#/13200*1.4*CT#),0)
  ```

### `CW` · جمع مازاد ثابت / مازاد پرداختی ماهانه / اضافه کار واقعی ومرخصی

- **62 سطر**

  ```
  =CV#+CR#+CQ#+AL#
  ```

- **3 سطر**

  ```
  =CV#+CR#+CQ#
  ```

### `CX` · قابل پرداخت مازاد ثابت / اضافه کار ثابت / اضافه کار واقعی

- **39 سطر**

  ```
  =CW#-AC#
  ```

- 26 سطر به‌جای فرمول، عددِ ثابت دارند.

### `CY` · محاسبه مدت ماموریت

- **39 سطر**

  ```
  =ROUNDDOWN((CX#/CL#),0)
  ```

- 26 سطر به‌جای فرمول، عددِ ثابت دارند.

### `CZ` · محاسبه مبلغ ماموریت

- **39 سطر**

  ```
  =CY#*CL#
  ```

- 26 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DA` · مازاد محاسبه یک

- **41 سطر**

  ```
  =CX#-CZ#
  ```

- **1 سطر**

  ```
  =CW#-CZ#
  ```

- 23 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DB` · اضافه کار هر ساعت

- **41 سطر**

  ```
  =ROUND((W#/220*1.4*1),0)
  ```

- 24 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DC` · اضافه کار هردقیقه

- **41 سطر**

  ```
  =ROUND((W#/13200*1.4*1),0)
  ```

- 24 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DD` · محاسبه اضافه کار دقیقه

- **41 سطر**

  ```
  =ROUNDUP(IF(OR(DH#<DC#),0,IF(OR(DH#>DC#),DH#/DC#)),0)
  ```

- 24 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DE` · محاسبه اضافه کار ساعت

- **41 سطر**

  ```
  =ROUNDDOWN((DA#/DB#),0)
  ```

- 24 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DF` · مبلغ اضافه کار محاسبه دقیقه

- **39 سطر**

  ```
  =ROUND((W#/13200*1.4*DD#),0)
  ```

- **2 سطر**

  ```
  =ROUND((CS#/13200*1.4*DD#),0)
  ```

- 24 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DG` · مبلغ اضافه کار محاسبه ساعت

- **39 سطر**

  ```
  =ROUND((W#/220*1.4*DE#),0)
  ```

- **2 سطر**

  ```
  =ROUND((CS#/220*1.4*DE#),0)
  ```

- 24 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DH` · مازاد محاسبه دو

- **41 سطر**

  ```
  =DA#-DG#
  ```

- 24 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DI` · مازاد محاسبه سه

- **41 سطر**

  ```
  =DH#-DF#
  ```

- 24 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DJ` · مانده مرخصی قابل استفاده در آخر سال 1404

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `DK` · مرخصی انتقالی از سال 1404 به روز

- **63 سطر**

  ```
  =ROUND(IF(OR(DJ#=9,DJ#>9),9,IF(OR(DJ#<9,),DJ#)),0)
  ```

- 2 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DL` · مرخصی استحقاق سال 1405 به روز

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `DM` · جمع مر خصی قابل استفاده در سال 1405 به روز

- **63 سطر**

  ```
  =DL#+DK#
  ```

- **2 سطر**

  ```
  =ROUND(IF(OR(O#=30,O#=31),DL#,IF(OR(O#<30,O#<31),DL#/30*60)),0)
  ```

### `DN` · مرخصی قابل استفاده سال 1405 به ساعت

- **63 سطر**

  ```
  =ROUNDDOWN((DM#*440)/60,0)
  ```

- **2 سطر**

  ```
  =ROUNDDOWN((DM#/60),0)
  ```

### `DO` · جمع مرخصی قابل استفاده سال 1405 به دقیقه

- **63 سطر**

  ```
  =ROUNDDOWN((DM#*440)-(DN#*60),0)
  ```

- 2 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DP` · مصرفی ماه اعلامی دقیقه

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `DQ` · مصرفی ماه اعلامی ساعت

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `DR` · مصرفی ماه اعلامی روز

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `DS` · مصرفی ماه واقعی دقیقه

- **63 سطر**

  ```
  =ROUNDDOWN((DR#*440+DQ#*60+DP#-(DU#*440+DT#*60)),0)
  ```

- 2 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DT` · مصرفی ماه واقعی ساعت

- **63 سطر**

  ```
  =ROUNDDOWN((DR#*440+DQ#*60+DP#-DU#*440)/60,0)
  ```

- 2 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DU` · مصرفی ماه واقعی روز

- **63 سطر**

  ```
  =ROUNDDOWN((DR#*440+DQ#*60+DP#)/440,0)
  ```

- 2 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DV` · مصرفی اعلامی تا ماه دقیقه

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `DW` · مصرفی اعلامی تا ماه ساعت

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `DX` · مصرفی اعلامی تا ماه روز

عدد یا متنِ ورودی — 65 سطر مقدار دارد.

### `DY` · مصرفی تا ماه دقیقه

- **63 سطر**

  ```
  =ROUNDDOWN((DX#*440+DW#*60+DV#-(EA#*440+DZ#*60)),0)
  ```

- 2 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DZ` · مصرفی تا ماه ساعت

- **63 سطر**

  ```
  =ROUNDDOWN((DX#*440+DW#*60+DV#-EA#*440)/60,0)
  ```

- 2 سطر به‌جای فرمول، عددِ ثابت دارند.

### `EA` · مصرفی تا ماه روز

- **63 سطر**

  ```
  =ROUNDDOWN((DX#*440+DW#*60+DV#)/440,0)
  ```

- 2 سطر به‌جای فرمول، عددِ ثابت دارند.

### `EB` · استفاده شده کل تا ماه دقیقه

- **63 سطر**

  ```
  =ROUNDDOWN((EA#*440+DZ#*60+DY#),0)
  ```

- 2 سطر به‌جای فرمول، عددِ ثابت دارند.

### `EC` · مانده تا آخر سال دقیقه

- **63 سطر**

  ```
  =ROUNDDOWN((DM#*440-EB#-(EE#*440+ED#*60)),0)
  ```

- 2 سطر به‌جای فرمول، عددِ ثابت دارند.

### `ED` · مانده تا آخر سال ساعت

- **63 سطر**

  ```
  =ROUNDDOWN((DM#*440-EB#-EE#*440)/60,0)
  ```

- 2 سطر به‌جای فرمول، عددِ ثابت دارند.

### `EE` · مانده تا آخر سال روز

- **63 سطر**

  ```
  =ROUNDDOWN((DM#*440-EB#)/440,0)
  ```

- 2 سطر به‌جای فرمول، عددِ ثابت دارند.


---

## شیت «خرداد 1405با بیمه » — 62 سطر

### `A` · رديف

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `B` · شماره پرسنلی

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `C` · واحد

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `D` · آقا / خانم

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `E` · نام ونام خانوادگي

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `F` · تاریخ استخدام

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `G` · شغل

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `H` · تاريخ تهيه

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `I` · ماه

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `J` · سال

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `K` · روز های ماه

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `L` · غیبت

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `M` · بدون حقوق

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `N` · بیماری

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `O` · کارکرد واقعی ماهانه

- **همهٔ سطرها**

  ```
  =K#-(L#+M#+N#)
  ```

### `P` · تاخیر ساعت

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `Q` · تاخیر دقیقه

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `R` · مزدروزانه

- **همهٔ سطرها**

  ```
  =ROUND((CA#*1.45)+(519549),0)
  ```

### `S` · پایه سنوات روزانه

- **45 سطر**

  ```
  =ROUND((5000000/30),0)
  ```

- 17 سطر به‌جای فرمول، عددِ ثابت دارند.

### `T` · جمع مزد وسنوات روزانه

- **همهٔ سطرها**

  ```
  =S#+R#
  ```

### `U` · حقوق ماهانه

- **همهٔ سطرها**

  ```
  =R#*30
  ```

### `V` · پایه سنوات ماهانه

- **همهٔ سطرها**

  ```
  =S#*30
  ```

### `W` · جمع حقوق وسنوات ماهانه

- **همهٔ سطرها**

  ```
  =V#+U#
  ```

### `X` · پایه سنوات تجمیعی روزانه

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `Y` · پایه سنوات تجمیعی ماهانه

- **همهٔ سطرها**

  ```
  =X#*O#
  ```

### `Z` · حقوق قابل پرداخت

- **همهٔ سطرها**

  ```
  =R#*O#
  ```

### `AA` · سنوات قابل پرداخت

- **همهٔ سطرها**

  ```
  =S#*O#
  ```

### `AB` · جمع حقوق وسنوات قابل پرداخت

- **همهٔ سطرها**

  ```
  =AA#+Z#
  ```

### `AC` · مابه التفاوت پایه سنوات تجمیعی قابل پرداخت

- **همهٔ سطرها**

  ```
  =ROUND(IF(OR(Y#>=AA#),Y#-AA#,IF(OR(Y#<=AA#),0)),0)
  ```

### `AD` · پایه سنوات تجمیعی قابل پرداخت

- **همهٔ سطرها**

  ```
  =AC#+AA#
  ```

### `AE` · حق مسكن قابل پرداخت

- **همهٔ سطرها**

  ```
  =ROUND(IF(OR(O#=30,O#=31),CF#,IF(OR(O#<30,O#<31),CF#/31*O#)),0)
  ```

### `AF` · حق اولاد قابل پرداخت

- **همهٔ سطرها**

  ```
  =ROUND(IF(OR(O#=30,O#=31),CH#,IF(OR(O#<30,O#<31),CH#/31*O#)),0)
  ```

### `AG` · حق تاهل قابل پرداخت

- **همهٔ سطرها**

  ```
  =ROUND(IF(OR(O#=30,O#=31),CJ#,IF(OR(O#<30,O#<31),CJ#/31*O#)),0)
  ```

### `AH` · وجه بن قابل پرداخت

- **همهٔ سطرها**

  ```
  =ROUND(IF(OR(O#=30,O#=31),CK#,IF(OR(O#<30,O#<31),CK#/31*O#)),0)
  ```

### `AI` · اضافه کاری عادی دقیقه

- **39 سطر**

  ```
  =DD#
  ```

- **23 سطر**

  ```
  =CT#
  ```

### `AJ` · اضافه کاری عادی ساعت

- **39 سطر**

  ```
  =DE#
  ```

- **23 سطر**

  ```
  =CU#
  ```

### `AK` · مبلغ اضافه كاري کل

- **39 سطر**

  ```
  =ROUND((W#/220*1.4*AJ#)+(W#/13200*1.4*AI#),0)
  ```

- **23 سطر**

  ```
  =ROUND((CS#/220*1.4*AJ#)+(CS#/13200*1.4*AI#),0)
  ```

### `AL` · وجه مرخصی

- **1 سطر**

  ```
  =ROUND((CL#/7.333*16/30*DL#),0)
  ```

- 61 سطر به‌جای فرمول، عددِ ثابت دارند.

### `AM` · پایانکار قابل پرداخت

- **1 سطر**

  ```
  =ROUND((T#*2.5/30*30),0)
  ```

- 61 سطر به‌جای فرمول، عددِ ثابت دارند.

### `AN` · عیدی وپاداش قابل پرداخت

- **1 سطر**

  ```
  =ROUND((CL#*5/30*30),0)
  ```

- 61 سطر به‌جای فرمول، عددِ ثابت دارند.

### `AO` · مدت ماموریت

- **37 سطر**

  ```
  =CY#
  ```

- 25 سطر به‌جای فرمول، عددِ ثابت دارند.

### `AP` · مبلغ ماموریت

- **39 سطر**

  ```
  =AO#*T#
  ```

- **23 سطر**

  ```
  =AO#*T#*2
  ```

### `AQ` · ذخیره پورسانت فروش

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `AR` · پورسانت یک قابل پرداخت

- **22 سطر**

  ```
  =(CN#+CQ#+AL#)-(AC#+AQ#+CM#+AP#)
  ```

- **1 سطر**

  ```
  =CN#
  ```

- **1 سطر**

  ```
  =(CN#+CQ#+AL#)-(AC#+AQ#+CM#+AP#+CV#)
  ```

- 38 سطر به‌جای فرمول، عددِ ثابت دارند.

### `AS` · پورسانت قابل پرداخت دو

- **21 سطر**

  ```
  =CO#
  ```

- 41 سطر به‌جای فرمول، عددِ ثابت دارند.

### `AT` · پورسانت قابل پرداخت سه

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `AU` · طلب ماه قبل

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `AV` · جمع پرداختيها فیش

- **همهٔ سطرها**

  ```
  =AU#+AT#+AS#+AR#+AP#+AN#+AM#+AL#+AK#+AH#+AG#+AF#+AE#+AC#+AA#+Z#
  ```

### `AW` · علی الحساب واریزی

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `AX` · علی الحساب نقدی

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `AY` · جمع علی الحساب

- **61 سطر**

  ```
  =AW#+AX#
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `AZ` · ماخذ مالیات

- **همهٔ سطرها**

  ```
  =AV#-AP#
  ```

### `BA` · مالیات

- **همهٔ سطرها**

  ```
  =IF(AZ#<=400000000,0, IF(AZ#<=800000000,(AZ#-400000000)*10%, IF(AZ#<=1000000000,(AZ#-800000000)*15%+40000000, IF(AZ#<=1200000000,(AZ#-1000000000)*20%+70000000, IF(AZ#<=1400000000,(AZ#-1200000000)*25%+140000000, IF(AZ#>1400000001,(AZ#-1400000000)*30%+190000000))))))
  ```

### `BB` · روند مالیات

- **61 سطر**

  ```
  =ROUND((BA#),0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `BC` · ماخذ بیمه

- **همهٔ سطرها**

  ```
  =AV#-(AP#+AF#)
  ```

### `BD` · سهم کارگر

- **همهٔ سطرها**

  ```
  =ROUND((BC#*7%),0)
  ```

### `BE` · سهم کارفرما

- **همهٔ سطرها**

  ```
  =ROUND((BC#*20%),0)
  ```

### `BF` · بیمه بیکاری

- **همهٔ سطرها**

  ```
  =ROUND((BC#*3%),0)
  ```

### `BG` · جمع کل بیمه

- **43 سطر**

  ```
  =ROUND((BF#+BE#+BD#),0)
  ```

- **19 سطر**

  ```
  =BF#+BE#+BD#
  ```

### `BH` · مبلغ کل وام

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `BI` · تعدادكل قسط

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `BJ` · مبلغ قسط

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `BK` · شماره قسط

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `BL` · مانده قسط

- **59 سطر**

  ```
  =BI#-BK#
  ```

- 3 سطر به‌جای فرمول، عددِ ثابت دارند.

### `BM` · مانده وام

- **59 سطر**

  ```
  =BL#*BJ#
  ```

- 3 سطر به‌جای فرمول، عددِ ثابت دارند.

### `BN` · خرید از شرکت

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `BO` · بدهی از ماه قبل

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `BP` · سایر بدهی

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `BQ` · بیمه تکمیلی

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `BR` · جرائم وخلافی خودروها

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `BS` · آزمایش وطب کار

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `BT` · ماخد کسر کار - دیر کرد

- **همهٔ سطرها**

  ```
  =U#+V#+CF#+CH#+CJ#+CK#
  ```

### `BU` · مبلغ کسر کار - دیر کرد

- **61 سطر**

  ```
  =ROUND((BT#/220*P#)+(BT#/11440*Q#),0)
  ```

- **1 سطر**

  ```
  =ROUND((CV#*23%),0)
  ```

### `BV` · جمع كسورات

- **همهٔ سطرها**

  ```
  =BU#+BS#+BR#+BQ#+BP#+BO#+BN#+BJ#+BD#+BB#+AW#
  ```

### `BW` · خالص پرداخت

- **همهٔ سطرها**

  ```
  =AV#-BV#
  ```

### `BX` · شماره کارت

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `BY` · شماره حساب

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `BZ` · بانك عامل

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `CA` · مزدروزانه 1404

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `CB` · پایه سنوات تجمیعی روزانه سال 1405

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `CC` · پایه سنوات تجمیعی ماهانه سال 1405

- **همهٔ سطرها**

  ```
  =CB#*30
  ```

### `CD` · حداقل دستمزد 1405

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `CE` · پایه سنوات روزانه 1405

- **40 سطر**

  ```
  =ROUND((5000000/30),0)
  ```

- 22 سطر به‌جای فرمول، عددِ ثابت دارند.

### `CF` · حق مسکن

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `CG` · تعداد اولاد

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `CH` · حق اولاد

- **همهٔ سطرها**

  ```
  =CG#*CD#*3
  ```

### `CI` · وضعیت تاهل

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `CJ` · حق تاهل

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `CK` · وجه بن

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `CL` · ماخذ پایانکار وعیدی وپاداش ووجه مرخصی وماموریت

- **38 سطر**

  ```
  =T#
  ```

- 24 سطر به‌جای فرمول، عددِ ثابت دارند.

### `CM` · کسر مازاد پرداختی فروش

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `CN` · پورسانت یک

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `CO` · پورسانت دو

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `CP` · پورسانت سه

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `CQ` · مازاد ثابت

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `CR` · مازاد پرداختی ماهانه

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `CS` · ماخذ محاسبه اضافه کاری

- **همهٔ سطرها**

  ```
  =W#
  ```

### `CT` · اضافه کارواقعی دقیقه

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `CU` · اضافه کار واقعی ساعت

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `CV` · مبلغ اضافه کار واقعی

- **همهٔ سطرها**

  ```
  =ROUND((CS#/220*1.4*CU#)+(CS#/13200*1.4*CT#),0)
  ```

### `CW` · جمع مازاد ثابت / مازاد پرداختی ماهانه / اضافه کار واقعی ومرخصی

- **60 سطر**

  ```
  =CV#+CR#+CQ#+AL#
  ```

- **2 سطر**

  ```
  =CV#+CR#+CQ#
  ```

### `CX` · قابل پرداخت مازاد ثابت / اضافه کار ثابت / اضافه کار واقعی

- **37 سطر**

  ```
  =CW#-AC#
  ```

- 25 سطر به‌جای فرمول، عددِ ثابت دارند.

### `CY` · محاسبه مدت ماموریت

- **37 سطر**

  ```
  =ROUNDDOWN((CX#/CL#),0)
  ```

- 25 سطر به‌جای فرمول، عددِ ثابت دارند.

### `CZ` · محاسبه مبلغ ماموریت

- **37 سطر**

  ```
  =CY#*CL#
  ```

- 25 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DA` · مازاد محاسبه یک

- **38 سطر**

  ```
  =CX#-CZ#
  ```

- **1 سطر**

  ```
  =CW#-CZ#
  ```

- 23 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DB` · اضافه کار هر ساعت

- **38 سطر**

  ```
  =ROUND((W#/220*1.4*1),0)
  ```

- 24 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DC` · اضافه کار هردقیقه

- **38 سطر**

  ```
  =ROUND((W#/13200*1.4*1),0)
  ```

- 24 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DD` · محاسبه اضافه کار دقیقه

- **38 سطر**

  ```
  =ROUNDUP(IF(OR(DH#<DC#),0,IF(OR(DH#>DC#),DH#/DC#)),0)
  ```

- 24 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DE` · محاسبه اضافه کار ساعت

- **38 سطر**

  ```
  =ROUNDDOWN((DA#/DB#),0)
  ```

- 24 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DF` · مبلغ اضافه کار محاسبه دقیقه

- **37 سطر**

  ```
  =ROUND((W#/13200*1.4*DD#),0)
  ```

- **1 سطر**

  ```
  =ROUND((CS#/13200*1.4*DD#),0)
  ```

- 24 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DG` · مبلغ اضافه کار محاسبه ساعت

- **37 سطر**

  ```
  =ROUND((W#/220*1.4*DE#),0)
  ```

- **1 سطر**

  ```
  =ROUND((CS#/220*1.4*DE#),0)
  ```

- 24 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DH` · مازاد محاسبه دو

- **38 سطر**

  ```
  =DA#-DG#
  ```

- 24 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DI` · مازاد محاسبه سه

- **38 سطر**

  ```
  =DH#-DF#
  ```

- 24 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DJ` · مانده مرخصی قابل استفاده در آخر سال 1404

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `DK` · مرخصی انتقالی از سال 1404 به روز

- **60 سطر**

  ```
  =ROUND(IF(OR(DJ#=9,DJ#>9),9,IF(OR(DJ#<9,),DJ#)),0)
  ```

- 2 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DL` · مرخصی استحقاق سال 1405 به روز

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `DM` · جمع مر خصی قابل استفاده در سال 1405 به روز

- **61 سطر**

  ```
  =DL#+DK#
  ```

- **1 سطر**

  ```
  =ROUND(IF(OR(O#=30,O#=31),DL#,IF(OR(O#<30,O#<31),DL#/30*60)),0)
  ```

### `DN` · مرخصی قابل استفاده سال 1405 به ساعت

- **61 سطر**

  ```
  =ROUNDDOWN((DM#*440)/60,0)
  ```

- **1 سطر**

  ```
  =ROUNDDOWN((DM#/60),0)
  ```

### `DO` · جمع مرخصی قابل استفاده سال 1405 به دقیقه

- **61 سطر**

  ```
  =ROUNDDOWN((DM#*440)-(DN#*60),0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DP` · مصرفی ماه اعلامی دقیقه

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `DQ` · مصرفی ماه اعلامی ساعت

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `DR` · مصرفی ماه اعلامی روز

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `DS` · مصرفی ماه واقعی دقیقه

- **61 سطر**

  ```
  =ROUNDDOWN((DR#*440+DQ#*60+DP#-(DU#*440+DT#*60)),0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DT` · مصرفی ماه واقعی ساعت

- **61 سطر**

  ```
  =ROUNDDOWN((DR#*440+DQ#*60+DP#-DU#*440)/60,0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DU` · مصرفی ماه واقعی روز

- **61 سطر**

  ```
  =ROUNDDOWN((DR#*440+DQ#*60+DP#)/440,0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DV` · مصرفی اعلامی تا ماه دقیقه

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `DW` · مصرفی اعلامی تا ماه ساعت

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `DX` · مصرفی اعلامی تا ماه روز

عدد یا متنِ ورودی — 62 سطر مقدار دارد.

### `DY` · مصرفی تا ماه دقیقه

- **61 سطر**

  ```
  =ROUNDDOWN((DX#*440+DW#*60+DV#-(EA#*440+DZ#*60)),0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DZ` · مصرفی تا ماه ساعت

- **61 سطر**

  ```
  =ROUNDDOWN((DX#*440+DW#*60+DV#-EA#*440)/60,0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `EA` · مصرفی تا ماه روز

- **61 سطر**

  ```
  =ROUNDDOWN((DX#*440+DW#*60+DV#)/440,0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `EB` · استفاده شده کل تا ماه دقیقه

- **61 سطر**

  ```
  =ROUNDDOWN((EA#*440+DZ#*60+DY#),0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `EC` · مانده تا آخر سال دقیقه

- **61 سطر**

  ```
  =ROUNDDOWN((DM#*440-EB#-(EE#*440+ED#*60)),0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `ED` · مانده تا آخر سال ساعت

- **61 سطر**

  ```
  =ROUNDDOWN((DM#*440-EB#-EE#*440)/60,0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `EE` · مانده تا آخر سال روز

- **61 سطر**

  ```
  =ROUNDDOWN((DM#*440-EB#)/440,0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.


---

## شیت «اردیبهشت 1405 عدم بیمه» — 6 سطر

### `A` · رديف

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `B` · شماره پرسنلی

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `C` · واحد

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `D` · آقا / خانم

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `E` · نام ونام خانوادگي

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `F` · تاریخ استخدام

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `G` · شغل

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `H` · تاريخ تهيه

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `I` · ماه

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `J` · سال

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `K` · روز های ماه

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `L` · غیبت

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `M` · بدون حقوق

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `N` · بیماری

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `O` · کارکرد واقعی ماهانه

- **همهٔ سطرها**

  ```
  =K#-(L#+M#+N#)
  ```

### `P` · تاخیر ساعت

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `Q` · تاخیر دقیقه

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `R` · مزدروزانه

- **5 سطر**

  ```
  =ROUND((CA#*1.45)+(519549),0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `S` · پایه سنوات روزانه

- **3 سطر**

  ```
  =ROUND((5000000/30),0)
  ```

- 3 سطر به‌جای فرمول، عددِ ثابت دارند.

### `T` · جمع مزد وسنوات روزانه

- **همهٔ سطرها**

  ```
  =S#+R#
  ```

### `U` · حقوق ماهانه

- **همهٔ سطرها**

  ```
  =R#*30
  ```

### `V` · پایه سنوات ماهانه

- **همهٔ سطرها**

  ```
  =S#*30
  ```

### `W` · جمع حقوق وسنوات ماهانه

- **همهٔ سطرها**

  ```
  =V#+U#
  ```

### `X` · پایه سنوات تجمیعی روزانه

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `Y` · پایه سنوات تجمیعی ماهانه

- **همهٔ سطرها**

  ```
  =X#*O#
  ```

### `Z` · حقوق قابل پرداخت

- **همهٔ سطرها**

  ```
  =R#*O#
  ```

### `AA` · سنوات قابل پرداخت

- **همهٔ سطرها**

  ```
  =S#*O#
  ```

### `AB` · جمع حقوق وسنوات قابل پرداخت

- **همهٔ سطرها**

  ```
  =AA#+Z#
  ```

### `AC` · مابه التفاوت پایه سنوات تجمیعی قابل پرداخت

- **همهٔ سطرها**

  ```
  =ROUND(IF(OR(Y#>=AA#),Y#-AA#,IF(OR(Y#<=AA#),0)),0)
  ```

### `AD` · پایه سنوات تجمیعی قابل پرداخت

- **همهٔ سطرها**

  ```
  =AC#+AA#
  ```

### `AE` · حق مسكن قابل پرداخت

- **همهٔ سطرها**

  ```
  =ROUND(IF(OR(O#=30,O#=31),CF#,IF(OR(O#<30,O#<31),CF#/31*O#)),0)
  ```

### `AF` · حق اولاد قابل پرداخت

- **همهٔ سطرها**

  ```
  =ROUND(IF(OR(O#=30,O#=31),CH#,IF(OR(O#<30,O#<31),CH#/31*O#)),0)
  ```

### `AG` · حق تاهل قابل پرداخت

- **همهٔ سطرها**

  ```
  =ROUND(IF(OR(O#=30,O#=31),CJ#,IF(OR(O#<30,O#<31),CJ#/31*O#)),0)
  ```

### `AH` · وجه بن قابل پرداخت

- **همهٔ سطرها**

  ```
  =ROUND(IF(OR(O#=30,O#=31),CK#,IF(OR(O#<30,O#<31),CK#/31*O#)),0)
  ```

### `AI` · اضافه کاری عادی دقیقه

- **5 سطر**

  ```
  =DD#
  ```

- **1 سطر**

  ```
  =CT#
  ```

### `AJ` · اضافه کاری عادی ساعت

- **5 سطر**

  ```
  =DE#
  ```

- **1 سطر**

  ```
  =CU#
  ```

### `AK` · مبلغ اضافه كاري کل

- **5 سطر**

  ```
  =ROUND((W#/220*1.4*AJ#)+(W#/13200*1.4*AI#),0)
  ```

- **1 سطر**

  ```
  =ROUND((CS#/220*1.4*AJ#)+(CS#/13200*1.4*AI#),0)
  ```

### `AL` · وجه مرخصی

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `AM` · پایانکار قابل پرداخت

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `AN` · عیدی وپاداش قابل پرداخت

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `AO` · مدت ماموریت

- **5 سطر**

  ```
  =CY#
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `AP` · مبلغ ماموریت

- **5 سطر**

  ```
  =AO#*T#
  ```

- **1 سطر**

  ```
  =AO#*T#*2
  ```

### `AQ` · ذخیره پورسانت فروش

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `AR` · پورسانت یک قابل پرداخت

- **1 سطر**

  ```
  =(CN#+CQ#+AL#)-(AC#+AQ#+CM#+AP#)
  ```

- 5 سطر به‌جای فرمول، عددِ ثابت دارند.

### `AS` · پورسانت قابل پرداخت دو

- **1 سطر**

  ```
  =CO#
  ```

- 5 سطر به‌جای فرمول، عددِ ثابت دارند.

### `AT` · پورسانت قابل پرداخت سه

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `AU` · طلب ماه قبل

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `AV` · جمع پرداختيها فیش

- **همهٔ سطرها**

  ```
  =AU#+AT#+AS#+AR#+AP#+AN#+AM#+AL#+AK#+AH#+AG#+AF#+AE#+AC#+AA#+Z#
  ```

### `AW` · علی الحساب واریزی

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `AX` · علی الحساب نقدی

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `AY` · جمع علی الحساب

- **همهٔ سطرها**

  ```
  =AW#+AX#
  ```

### `AZ` · ماخذ مالیات

- **همهٔ سطرها**

  ```
  =AV#-AP#
  ```

### `BA` · مالیات

- **همهٔ سطرها**

  ```
  =IF(AZ#<=400000000,0, IF(AZ#<=800000000,(AZ#-400000000)*10%, IF(AZ#<=1000000000,(AZ#-800000000)*15%+40000000, IF(AZ#<=1200000000,(AZ#-1000000000)*20%+70000000, IF(AZ#<=1400000000,(AZ#-1200000000)*25%+140000000, IF(AZ#>1400000001,(AZ#-1400000000)*30%+190000000))))))
  ```

### `BB` · روند مالیات

- **همهٔ سطرها**

  ```
  =ROUND((BA#),0)
  ```

### `BC` · ماخذ بیمه

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `BD` · سهم کارگر

- **همهٔ سطرها**

  ```
  =ROUND((BC#*7%),0)
  ```

### `BE` · سهم کارفرما

- **همهٔ سطرها**

  ```
  =ROUND((BC#*20%),0)
  ```

### `BF` · بیمه بیکاری

- **همهٔ سطرها**

  ```
  =ROUND((BC#*3%),0)
  ```

### `BG` · جمع کل بیمه

- **5 سطر**

  ```
  =ROUND((BF#+BE#+BD#),0)
  ```

- **1 سطر**

  ```
  =BF#+BE#+BD#
  ```

### `BH` · مبلغ کل وام

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `BI` · تعدادكل قسط

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `BJ` · مبلغ قسط

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `BK` · شماره قسط

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `BL` · مانده قسط

- **همهٔ سطرها**

  ```
  =BI#-BK#
  ```

### `BM` · مانده وام

- **همهٔ سطرها**

  ```
  =BL#*BJ#
  ```

### `BN` · خرید از شرکت

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `BO` · بدهی از ماه قبل

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `BP` · سایر بدهی

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `BQ` · بیمه تکمیلی

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `BR` · جرائم وخلافی خودروها

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `BS` · آزمایش وطب کار

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `BT` · ماخد کسر کار - دیر کرد

- **همهٔ سطرها**

  ```
  =U#+V#+CF#+CH#+CJ#+CK#
  ```

### `BU` · مبلغ کسر کار - دیر کرد

- **همهٔ سطرها**

  ```
  =ROUND((BT#/220*P#)+(BT#/11440*Q#),0)
  ```

### `BV` · جمع كسورات

- **همهٔ سطرها**

  ```
  =BU#+BS#+BR#+BQ#+BP#+BO#+BN#+BJ#+BD#+BB#+AW#
  ```

### `BW` · خالص پرداخت

- **همهٔ سطرها**

  ```
  =AV#-BV#
  ```

### `BX` · شماره کارت

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `BY` · شماره حساب

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `BZ` · بانك عامل

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `CA` · مزدروزانه 1404

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `CB` · پایه سنوات تجمیعی روزانه سال 1405

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `CC` · پایه سنوات تجمیعی ماهانه سال 1405

- **همهٔ سطرها**

  ```
  =CB#*30
  ```

### `CD` · حداقل دستمزد 1405

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `CE` · پایه سنوات روزانه 1405

- **4 سطر**

  ```
  =ROUND((5000000/30),0)
  ```

- 2 سطر به‌جای فرمول، عددِ ثابت دارند.

### `CF` · حق مسکن

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `CG` · تعداد اولاد

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `CH` · حق اولاد

- **همهٔ سطرها**

  ```
  =CG#*CD#*3
  ```

### `CI` · وضعیت تاهل

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `CJ` · حق تاهل

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `CK` · وجه بن

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `CL` · ماخذ پایانکار وعیدی وپاداش ووجه مرخصی وماموریت

- **5 سطر**

  ```
  =T#
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `CM` · کسر مازاد پرداختی فروش

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `CN` · پورسانت یک

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `CO` · پورسانت دو

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `CP` · پورسانت سه

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `CQ` · مازاد ثابت

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `CR` · مازاد پرداختی ماهانه

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `CS` · ماخذ محاسبه اضافه کاری

- **همهٔ سطرها**

  ```
  =W#
  ```

### `CT` · اضافه کارواقعی دقیقه

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `CU` · اضافه کار واقعی ساعت

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `CV` · مبلغ اضافه کار واقعی

- **همهٔ سطرها**

  ```
  =ROUND((CS#/220*1.4*CU#)+(CS#/13200*1.4*CT#),0)
  ```

### `CW` · جمع مازاد ثابت / مازاد پرداختی ماهانه / اضافه کار واقعی ومرخصی

- **همهٔ سطرها**

  ```
  =CV#+CR#+CQ#+AL#
  ```

### `CX` · قابل پرداخت مازاد ثابت / اضافه کار ثابت / اضافه کار واقعی

- **5 سطر**

  ```
  =CW#-AC#
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `CY` · محاسبه مدت ماموریت

- **5 سطر**

  ```
  =ROUNDDOWN((CX#/CL#),0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `CZ` · محاسبه مبلغ ماموریت

- **5 سطر**

  ```
  =CY#*CL#
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DA` · مازاد محاسبه یک

- **5 سطر**

  ```
  =CX#-CZ#
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DB` · اضافه کار هر ساعت

- **5 سطر**

  ```
  =ROUND((W#/220*1.4*1),0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DC` · اضافه کار هردقیقه

- **5 سطر**

  ```
  =ROUND((W#/13200*1.4*1),0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DD` · محاسبه اضافه کار دقیقه

- **5 سطر**

  ```
  =ROUNDUP(IF(OR(DH#<DC#),0,IF(OR(DH#>DC#),DH#/DC#)),0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DE` · محاسبه اضافه کار ساعت

- **5 سطر**

  ```
  =ROUNDDOWN((DA#/DB#),0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DF` · مبلغ اضافه کار محاسبه دقیقه

- **5 سطر**

  ```
  =ROUND((W#/13200*1.4*DD#),0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DG` · مبلغ اضافه کار محاسبه ساعت

- **5 سطر**

  ```
  =ROUND((W#/220*1.4*DE#),0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DH` · مازاد محاسبه دو

- **5 سطر**

  ```
  =DA#-DG#
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DI` · مازاد محاسبه سه

- **5 سطر**

  ```
  =DH#-DF#
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DJ` · مانده مرخصی قابل استفاده در آخر سال 1404

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `DK` · مرخصی انتقالی از سال 1404 به روز

- **همهٔ سطرها**

  ```
  =ROUND(IF(OR(DJ#=9,DJ#>9),9,IF(OR(DJ#<9,),DJ#)),0)
  ```

### `DL` · مرخصی استحقاق سال 1405 به روز

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `DM` · جمع مر خصی قابل استفاده در سال 1405 به روز

- **همهٔ سطرها**

  ```
  =DL#+DK#
  ```

### `DN` · مرخصی قابل استفاده سال 1405 به ساعت

- **همهٔ سطرها**

  ```
  =ROUNDDOWN((DM#*440)/60,0)
  ```

### `DO` · جمع مرخصی قابل استفاده سال 1405 به دقیقه

- **همهٔ سطرها**

  ```
  =ROUNDDOWN((DM#*440)-(DN#*60),0)
  ```

### `DP` · مصرفی ماه اعلامی دقیقه

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `DQ` · مصرفی ماه اعلامی ساعت

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `DR` · مصرفی ماه اعلامی روز

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `DS` · مصرفی ماه واقعی دقیقه

- **همهٔ سطرها**

  ```
  =ROUNDDOWN((DR#*440+DQ#*60+DP#-(DU#*440+DT#*60)),0)
  ```

### `DT` · مصرفی ماه واقعی ساعت

- **همهٔ سطرها**

  ```
  =ROUNDDOWN((DR#*440+DQ#*60+DP#-DU#*440)/60,0)
  ```

### `DU` · مصرفی ماه واقعی روز

- **همهٔ سطرها**

  ```
  =ROUNDDOWN((DR#*440+DQ#*60+DP#)/440,0)
  ```

### `DV` · مصرفی اعلامی تا ماه دقیقه

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `DW` · مصرفی اعلامی تا ماه ساعت

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `DX` · مصرفی اعلامی تا ماه روز

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `DY` · مصرفی تا ماه دقیقه

- **همهٔ سطرها**

  ```
  =ROUNDDOWN((DX#*440+DW#*60+DV#-(EA#*440+DZ#*60)),0)
  ```

### `DZ` · مصرفی تا ماه ساعت

- **همهٔ سطرها**

  ```
  =ROUNDDOWN((DX#*440+DW#*60+DV#-EA#*440)/60,0)
  ```

### `EA` · مصرفی تا ماه روز

- **همهٔ سطرها**

  ```
  =ROUNDDOWN((DX#*440+DW#*60+DV#)/440,0)
  ```

### `EB` · استفاده شده کل تا ماه دقیقه

- **همهٔ سطرها**

  ```
  =ROUNDDOWN((EA#*440+DZ#*60+DY#),0)
  ```

### `EC` · مانده تا آخر سال دقیقه

- **همهٔ سطرها**

  ```
  =ROUNDDOWN((DM#*440-EB#-(EE#*440+ED#*60)),0)
  ```

### `ED` · مانده تا آخر سال ساعت

- **همهٔ سطرها**

  ```
  =ROUNDDOWN((DM#*440-EB#-EE#*440)/60,0)
  ```

### `EE` · مانده تا آخر سال روز

- **همهٔ سطرها**

  ```
  =ROUNDDOWN((DM#*440-EB#)/440,0)
  ```


---

## شیت «خرداد 1405 بدون بیمه» — 5 سطر

### `A` · رديف

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `B` · شماره پرسنلی

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `C` · واحد

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `D` · آقا / خانم

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `E` · نام ونام خانوادگي

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `F` · تاریخ استخدام

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `G` · شغل

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `H` · تاريخ تهيه

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `I` · ماه

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `J` · سال

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `K` · روز های ماه

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `L` · غیبت

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `M` · بدون حقوق

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `N` · بیماری

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `O` · کارکرد واقعی ماهانه

- **همهٔ سطرها**

  ```
  =K#-(L#+M#+N#)
  ```

### `P` · تاخیر ساعت

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `Q` · تاخیر دقیقه

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `R` · مزدروزانه

- **4 سطر**

  ```
  =ROUND((CA#*1.45)+(519549),0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `S` · پایه سنوات روزانه

- **3 سطر**

  ```
  =ROUND((5000000/30),0)
  ```

- 2 سطر به‌جای فرمول، عددِ ثابت دارند.

### `T` · جمع مزد وسنوات روزانه

- **همهٔ سطرها**

  ```
  =S#+R#
  ```

### `U` · حقوق ماهانه

- **همهٔ سطرها**

  ```
  =R#*30
  ```

### `V` · پایه سنوات ماهانه

- **همهٔ سطرها**

  ```
  =S#*30
  ```

### `W` · جمع حقوق وسنوات ماهانه

- **همهٔ سطرها**

  ```
  =V#+U#
  ```

### `X` · پایه سنوات تجمیعی روزانه

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `Y` · پایه سنوات تجمیعی ماهانه

- **همهٔ سطرها**

  ```
  =X#*O#
  ```

### `Z` · حقوق قابل پرداخت

- **همهٔ سطرها**

  ```
  =R#*O#
  ```

### `AA` · سنوات قابل پرداخت

- **همهٔ سطرها**

  ```
  =S#*O#
  ```

### `AB` · جمع حقوق وسنوات قابل پرداخت

- **همهٔ سطرها**

  ```
  =AA#+Z#
  ```

### `AC` · مابه التفاوت پایه سنوات تجمیعی قابل پرداخت

- **همهٔ سطرها**

  ```
  =ROUND(IF(OR(Y#>=AA#),Y#-AA#,IF(OR(Y#<=AA#),0)),0)
  ```

### `AD` · پایه سنوات تجمیعی قابل پرداخت

- **همهٔ سطرها**

  ```
  =AC#+AA#
  ```

### `AE` · حق مسكن قابل پرداخت

- **همهٔ سطرها**

  ```
  =ROUND(IF(OR(O#=30,O#=31),CF#,IF(OR(O#<30,O#<31),CF#/31*O#)),0)
  ```

### `AF` · حق اولاد قابل پرداخت

- **همهٔ سطرها**

  ```
  =ROUND(IF(OR(O#=30,O#=31),CH#,IF(OR(O#<30,O#<31),CH#/31*O#)),0)
  ```

### `AG` · حق تاهل قابل پرداخت

- **همهٔ سطرها**

  ```
  =ROUND(IF(OR(O#=30,O#=31),CJ#,IF(OR(O#<30,O#<31),CJ#/31*O#)),0)
  ```

### `AH` · وجه بن قابل پرداخت

- **همهٔ سطرها**

  ```
  =ROUND(IF(OR(O#=30,O#=31),CK#,IF(OR(O#<30,O#<31),CK#/31*O#)),0)
  ```

### `AI` · اضافه کاری عادی دقیقه

- **4 سطر**

  ```
  =DD#
  ```

- **1 سطر**

  ```
  =CT#
  ```

### `AJ` · اضافه کاری عادی ساعت

- **4 سطر**

  ```
  =DE#
  ```

- **1 سطر**

  ```
  =CU#
  ```

### `AK` · مبلغ اضافه كاري کل

- **4 سطر**

  ```
  =ROUND((W#/220*1.4*AJ#)+(W#/13200*1.4*AI#),0)
  ```

- **1 سطر**

  ```
  =ROUND((CS#/220*1.4*AJ#)+(CS#/13200*1.4*AI#),0)
  ```

### `AL` · وجه مرخصی

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `AM` · پایانکار قابل پرداخت

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `AN` · عیدی وپاداش قابل پرداخت

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `AO` · مدت ماموریت

- **4 سطر**

  ```
  =CY#
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `AP` · مبلغ ماموریت

- **4 سطر**

  ```
  =AO#*T#
  ```

- **1 سطر**

  ```
  =AO#*T#*2
  ```

### `AQ` · ذخیره پورسانت فروش

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `AR` · پورسانت یک قابل پرداخت

- **1 سطر**

  ```
  =(CN#+CQ#+AL#)-(AC#+AQ#+CM#+AP#)
  ```

- 4 سطر به‌جای فرمول، عددِ ثابت دارند.

### `AS` · پورسانت قابل پرداخت دو

- **1 سطر**

  ```
  =CO#
  ```

- 4 سطر به‌جای فرمول، عددِ ثابت دارند.

### `AT` · پورسانت قابل پرداخت سه

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `AU` · طلب ماه قبل

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `AV` · جمع پرداختيها فیش

- **همهٔ سطرها**

  ```
  =AU#+AT#+AS#+AR#+AP#+AN#+AM#+AL#+AK#+AH#+AG#+AF#+AE#+AC#+AA#+Z#
  ```

### `AW` · علی الحساب واریزی

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `AX` · علی الحساب نقدی

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `AY` · جمع علی الحساب

- **همهٔ سطرها**

  ```
  =AW#+AX#
  ```

### `AZ` · ماخذ مالیات

- **همهٔ سطرها**

  ```
  =AV#-AP#
  ```

### `BA` · مالیات

- **همهٔ سطرها**

  ```
  =IF(AZ#<=400000000,0, IF(AZ#<=800000000,(AZ#-400000000)*10%, IF(AZ#<=1000000000,(AZ#-800000000)*15%+40000000, IF(AZ#<=1200000000,(AZ#-1000000000)*20%+70000000, IF(AZ#<=1400000000,(AZ#-1200000000)*25%+140000000, IF(AZ#>1400000001,(AZ#-1400000000)*30%+190000000))))))
  ```

### `BB` · روند مالیات

- **همهٔ سطرها**

  ```
  =ROUND((BA#),0)
  ```

### `BC` · ماخذ بیمه

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `BD` · سهم کارگر

- **همهٔ سطرها**

  ```
  =ROUND((BC#*7%),0)
  ```

### `BE` · سهم کارفرما

- **همهٔ سطرها**

  ```
  =ROUND((BC#*20%),0)
  ```

### `BF` · بیمه بیکاری

- **همهٔ سطرها**

  ```
  =ROUND((BC#*3%),0)
  ```

### `BG` · جمع کل بیمه

- **4 سطر**

  ```
  =ROUND((BF#+BE#+BD#),0)
  ```

- **1 سطر**

  ```
  =BF#+BE#+BD#
  ```

### `BH` · مبلغ کل وام

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `BI` · تعدادكل قسط

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `BJ` · مبلغ قسط

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `BK` · شماره قسط

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `BL` · مانده قسط

- **همهٔ سطرها**

  ```
  =BI#-BK#
  ```

### `BM` · مانده وام

- **همهٔ سطرها**

  ```
  =BL#*BJ#
  ```

### `BN` · خرید از شرکت

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `BO` · بدهی از ماه قبل

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `BP` · سایر بدهی

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `BQ` · بیمه تکمیلی

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `BR` · جرائم وخلافی خودروها

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `BS` · آزمایش وطب کار

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `BT` · ماخد کسر کار - دیر کرد

- **همهٔ سطرها**

  ```
  =U#+V#+CF#+CH#+CJ#+CK#
  ```

### `BU` · مبلغ کسر کار - دیر کرد

- **همهٔ سطرها**

  ```
  =ROUND((BT#/220*P#)+(BT#/11440*Q#),0)
  ```

### `BV` · جمع كسورات

- **همهٔ سطرها**

  ```
  =BU#+BS#+BR#+BQ#+BP#+BO#+BN#+BJ#+BD#+BB#+AW#
  ```

### `BW` · خالص پرداخت

- **همهٔ سطرها**

  ```
  =AV#-BV#
  ```

### `BX` · شماره کارت

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `BY` · شماره حساب

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `BZ` · بانك عامل

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `CA` · مزدروزانه 1404

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `CB` · پایه سنوات تجمیعی روزانه سال 1405

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `CC` · پایه سنوات تجمیعی ماهانه سال 1405

- **همهٔ سطرها**

  ```
  =CB#*30
  ```

### `CD` · حداقل دستمزد 1405

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `CE` · پایه سنوات روزانه 1405

- **3 سطر**

  ```
  =ROUND((5000000/30),0)
  ```

- 2 سطر به‌جای فرمول، عددِ ثابت دارند.

### `CF` · حق مسکن

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `CG` · تعداد اولاد

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `CH` · حق اولاد

- **همهٔ سطرها**

  ```
  =CG#*CD#*3
  ```

### `CI` · وضعیت تاهل

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `CJ` · حق تاهل

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `CK` · وجه بن

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `CL` · ماخذ پایانکار وعیدی وپاداش ووجه مرخصی وماموریت

- **4 سطر**

  ```
  =T#
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `CM` · کسر مازاد پرداختی فروش

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `CN` · پورسانت یک

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `CO` · پورسانت دو

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `CP` · پورسانت سه

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `CQ` · مازاد ثابت

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `CR` · مازاد پرداختی ماهانه

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `CS` · ماخذ محاسبه اضافه کاری

- **همهٔ سطرها**

  ```
  =W#
  ```

### `CT` · اضافه کارواقعی دقیقه

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `CU` · اضافه کار واقعی ساعت

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `CV` · مبلغ اضافه کار واقعی

- **همهٔ سطرها**

  ```
  =ROUND((CS#/220*1.4*CU#)+(CS#/13200*1.4*CT#),0)
  ```

### `CW` · جمع مازاد ثابت / مازاد پرداختی ماهانه / اضافه کار واقعی ومرخصی

- **همهٔ سطرها**

  ```
  =CV#+CR#+CQ#+AL#
  ```

### `CX` · قابل پرداخت مازاد ثابت / اضافه کار ثابت / اضافه کار واقعی

- **4 سطر**

  ```
  =CW#-AC#
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `CY` · محاسبه مدت ماموریت

- **4 سطر**

  ```
  =ROUNDDOWN((CX#/CL#),0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `CZ` · محاسبه مبلغ ماموریت

- **4 سطر**

  ```
  =CY#*CL#
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DA` · مازاد محاسبه یک

- **4 سطر**

  ```
  =CX#-CZ#
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DB` · اضافه کار هر ساعت

- **4 سطر**

  ```
  =ROUND((W#/220*1.4*1),0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DC` · اضافه کار هردقیقه

- **4 سطر**

  ```
  =ROUND((W#/13200*1.4*1),0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DD` · محاسبه اضافه کار دقیقه

- **4 سطر**

  ```
  =ROUNDUP(IF(OR(DH#<DC#),0,IF(OR(DH#>DC#),DH#/DC#)),0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DE` · محاسبه اضافه کار ساعت

- **4 سطر**

  ```
  =ROUNDDOWN((DA#/DB#),0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DF` · مبلغ اضافه کار محاسبه دقیقه

- **4 سطر**

  ```
  =ROUND((W#/13200*1.4*DD#),0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DG` · مبلغ اضافه کار محاسبه ساعت

- **4 سطر**

  ```
  =ROUND((W#/220*1.4*DE#),0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DH` · مازاد محاسبه دو

- **4 سطر**

  ```
  =DA#-DG#
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DI` · مازاد محاسبه سه

- **4 سطر**

  ```
  =DH#-DF#
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DJ` · مانده مرخصی قابل استفاده در آخر سال 1404

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `DK` · مرخصی انتقالی از سال 1404 به روز

- **همهٔ سطرها**

  ```
  =ROUND(IF(OR(DJ#=9,DJ#>9),9,IF(OR(DJ#<9,),DJ#)),0)
  ```

### `DL` · مرخصی استحقاق سال 1405 به روز

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `DM` · جمع مر خصی قابل استفاده در سال 1405 به روز

- **همهٔ سطرها**

  ```
  =DL#+DK#
  ```

### `DN` · مرخصی قابل استفاده سال 1405 به ساعت

- **همهٔ سطرها**

  ```
  =ROUNDDOWN((DM#*440)/60,0)
  ```

### `DO` · جمع مرخصی قابل استفاده سال 1405 به دقیقه

- **همهٔ سطرها**

  ```
  =ROUNDDOWN((DM#*440)-(DN#*60),0)
  ```

### `DP` · مصرفی ماه اعلامی دقیقه

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `DQ` · مصرفی ماه اعلامی ساعت

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `DR` · مصرفی ماه اعلامی روز

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `DS` · مصرفی ماه واقعی دقیقه

- **همهٔ سطرها**

  ```
  =ROUNDDOWN((DR#*440+DQ#*60+DP#-(DU#*440+DT#*60)),0)
  ```

### `DT` · مصرفی ماه واقعی ساعت

- **همهٔ سطرها**

  ```
  =ROUNDDOWN((DR#*440+DQ#*60+DP#-DU#*440)/60,0)
  ```

### `DU` · مصرفی ماه واقعی روز

- **همهٔ سطرها**

  ```
  =ROUNDDOWN((DR#*440+DQ#*60+DP#)/440,0)
  ```

### `DV` · مصرفی اعلامی تا ماه دقیقه

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `DW` · مصرفی اعلامی تا ماه ساعت

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `DX` · مصرفی اعلامی تا ماه روز

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `DY` · مصرفی تا ماه دقیقه

- **همهٔ سطرها**

  ```
  =ROUNDDOWN((DX#*440+DW#*60+DV#-(EA#*440+DZ#*60)),0)
  ```

### `DZ` · مصرفی تا ماه ساعت

- **همهٔ سطرها**

  ```
  =ROUNDDOWN((DX#*440+DW#*60+DV#-EA#*440)/60,0)
  ```

### `EA` · مصرفی تا ماه روز

- **همهٔ سطرها**

  ```
  =ROUNDDOWN((DX#*440+DW#*60+DV#)/440,0)
  ```

### `EB` · استفاده شده کل تا ماه دقیقه

- **همهٔ سطرها**

  ```
  =ROUNDDOWN((EA#*440+DZ#*60+DY#),0)
  ```

### `EC` · مانده تا آخر سال دقیقه

- **همهٔ سطرها**

  ```
  =ROUNDDOWN((DM#*440-EB#-(EE#*440+ED#*60)),0)
  ```

### `ED` · مانده تا آخر سال ساعت

- **همهٔ سطرها**

  ```
  =ROUNDDOWN((DM#*440-EB#-EE#*440)/60,0)
  ```

### `EE` · مانده تا آخر سال روز

- **همهٔ سطرها**

  ```
  =ROUNDDOWN((DM#*440-EB#)/440,0)
  ```


---

## شیت «تیر با بیمه 1405» — 61 سطر

### `A` · رديف

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `B` · شماره پرسنلی

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `C` · واحد

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `D` · آقا / خانم

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `E` · نام ونام خانوادگي

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `F` · تاریخ استخدام

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `G` · شغل

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `H` · تاريخ تهيه

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `I` · ماه

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `J` · سال

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `K` · روز های ماه

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `L` · غیبت

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `M` · بدون حقوق

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `N` · بیماری

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `O` · کارکرد واقعی ماهانه

- **همهٔ سطرها**

  ```
  =K#-(L#+M#+N#)
  ```

### `P` · تاخیر ساعت

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `Q` · تاخیر دقیقه

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `R` · مزدروزانه

- **60 سطر**

  ```
  =ROUND((CB#*1.45)+(519549),0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `S` · پایه سنوات روزانه

- **44 سطر**

  ```
  =ROUND((5000000/30),0)
  ```

- 17 سطر به‌جای فرمول، عددِ ثابت دارند.

### `T` · جمع مزد وسنوات روزانه

- **همهٔ سطرها**

  ```
  =S#+R#
  ```

### `U` · حقوق ماهانه

- **همهٔ سطرها**

  ```
  =R#*30
  ```

### `V` · پایه سنوات ماهانه

- **همهٔ سطرها**

  ```
  =S#*30
  ```

### `W` · جمع حقوق وسنوات ماهانه

- **همهٔ سطرها**

  ```
  =V#+U#
  ```

### `X` · پایه سنوات تجمیعی روزانه

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `Y` · پایه سنوات تجمیعی ماهانه

- **همهٔ سطرها**

  ```
  =X#*O#
  ```

### `Z` · حقوق قابل پرداخت

- **همهٔ سطرها**

  ```
  =R#*O#
  ```

### `AA` · سنوات قابل پرداخت

- **همهٔ سطرها**

  ```
  =S#*O#
  ```

### `AB` · جمع حقوق وسنوات قابل پرداخت

- **همهٔ سطرها**

  ```
  =AA#+Z#
  ```

### `AC` · مابه التفاوت پایه سنوات تجمیعی قابل پرداخت

- **همهٔ سطرها**

  ```
  =ROUND(IF(OR(Y#>=AA#),Y#-AA#,IF(OR(Y#<=AA#),0)),0)
  ```

### `AD` · پایه سنوات تجمیعی قابل پرداخت

- **همهٔ سطرها**

  ```
  =AC#+AA#
  ```

### `AE` · حق مسكن قابل پرداخت

- **همهٔ سطرها**

  ```
  =ROUND(IF(OR(O#=30,O#=31),CG#,IF(OR(O#<30,O#<31),CG#/31*O#)),0)
  ```

### `AF` · حق اولاد قابل پرداخت

- **همهٔ سطرها**

  ```
  =ROUND(IF(OR(O#=30,O#=31),CI#,IF(OR(O#<30,O#<31),CI#/31*O#)),0)
  ```

### `AG` · حق تاهل قابل پرداخت

- **همهٔ سطرها**

  ```
  =ROUND(IF(OR(O#=30,O#=31),CK#,IF(OR(O#<30,O#<31),CK#/31*O#)),0)
  ```

### `AH` · وجه بن قابل پرداخت

- **همهٔ سطرها**

  ```
  =ROUND(IF(OR(O#=30,O#=31),CL#,IF(OR(O#<30,O#<31),CL#/31*O#)),0)
  ```

### `AI` · اضافه کاری عادی دقیقه

- **39 سطر**

  ```
  =DE#
  ```

- **22 سطر**

  ```
  =CU#
  ```

### `AJ` · اضافه کاری عادی ساعت

- **39 سطر**

  ```
  =DF#
  ```

- **22 سطر**

  ```
  =CV#
  ```

### `AK` · مبلغ اضافه كاري کل

- **39 سطر**

  ```
  =ROUND((W#/220*1.4*AJ#)+(W#/13200*1.4*AI#),0)
  ```

- **22 سطر**

  ```
  =ROUND((CT#/220*1.4*AJ#)+(CT#/13200*1.4*AI#),0)
  ```

### `AL` · وجه مرخصی

- **1 سطر**

  ```
  =ROUND((CM#/7.333*16/30*DM#),0)
  ```

- 60 سطر به‌جای فرمول، عددِ ثابت دارند.

### `AM` · پایانکار قابل پرداخت

- **1 سطر**

  ```
  =ROUND((T#*2.5/30*30),0)
  ```

- 60 سطر به‌جای فرمول، عددِ ثابت دارند.

### `AN` · عیدی وپاداش قابل پرداخت

- **1 سطر**

  ```
  =ROUND((CM#*5/30*30),0)
  ```

- 60 سطر به‌جای فرمول، عددِ ثابت دارند.

### `AO` · مدت ماموریت

- **37 سطر**

  ```
  =CZ#
  ```

- 24 سطر به‌جای فرمول، عددِ ثابت دارند.

### `AP` · مبلغ ماموریت

- **39 سطر**

  ```
  =AO#*T#
  ```

- **22 سطر**

  ```
  =AO#*T#*2
  ```

### `AQ` · ذخیره پورسانت فروش

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `AR` · پورسانت یک قابل پرداخت

- **21 سطر**

  ```
  =(CO#+CR#+AL#)-(AC#+AQ#+CN#+AP#)
  ```

- **1 سطر**

  ```
  =CO#
  ```

- **1 سطر**

  ```
  =(CO#+CR#+AL#)-(AC#+AQ#+CN#+AP#+CW#)
  ```

- 38 سطر به‌جای فرمول، عددِ ثابت دارند.

### `AS` · پورسانت قابل پرداخت دو

- **20 سطر**

  ```
  =CP#
  ```

- 41 سطر به‌جای فرمول، عددِ ثابت دارند.

### `AT` · پورسانت قابل پرداخت سه

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `AU` · طلب ماه قبل

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `AV` · هزینه تلفن

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `AW` · جمع پرداختيها فیش

- **همهٔ سطرها**

  ```
  =AU#+AT#+AS#+AR#+AP#+AN#+AM#+AL#+AK#+AH#+AG#+AF#+AE#+AC#+AA#+Z#+AV#
  ```

### `AX` · علی الحساب واریزی

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `AY` · علی الحساب نقدی

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `AZ` · جمع علی الحساب

- **60 سطر**

  ```
  =AX#+AY#
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `BA` · ماخذ مالیات

- **همهٔ سطرها**

  ```
  =AW#-AP#
  ```

### `BB` · مالیات

- **همهٔ سطرها**

  ```
  =IF(BA#<=400000000,0, IF(BA#<=800000000,(BA#-400000000)*10%, IF(BA#<=1000000000,(BA#-800000000)*15%+40000000, IF(BA#<=1200000000,(BA#-1000000000)*20%+70000000, IF(BA#<=1400000000,(BA#-1200000000)*25%+140000000, IF(BA#>1400000001,(BA#-1400000000)*30%+190000000))))))
  ```

### `BC` · روند مالیات

- **60 سطر**

  ```
  =ROUND((BB#),0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `BD` · ماخذ بیمه

- **همهٔ سطرها**

  ```
  =AW#-(AP#+AF#)
  ```

### `BE` · سهم کارگر

- **همهٔ سطرها**

  ```
  =ROUND((BD#*7%),0)
  ```

### `BF` · سهم کارفرما

- **همهٔ سطرها**

  ```
  =ROUND((BD#*20%),0)
  ```

### `BG` · بیمه بیکاری

- **همهٔ سطرها**

  ```
  =ROUND((BD#*3%),0)
  ```

### `BH` · جمع کل بیمه

- **43 سطر**

  ```
  =ROUND((BG#+BF#+BE#),0)
  ```

- **18 سطر**

  ```
  =BG#+BF#+BE#
  ```

### `BI` · مبلغ کل وام

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `BJ` · تعدادكل قسط

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `BK` · مبلغ قسط

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `BL` · شماره قسط

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `BM` · مانده قسط

- **58 سطر**

  ```
  =BJ#-BL#
  ```

- 3 سطر به‌جای فرمول، عددِ ثابت دارند.

### `BN` · مانده وام

- **58 سطر**

  ```
  =BM#*BK#
  ```

- 3 سطر به‌جای فرمول، عددِ ثابت دارند.

### `BO` · خرید از شرکت

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `BP` · بدهی از ماه قبل

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `BQ` · سایر بدهی

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `BR` · بیمه تکمیلی

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `BS` · جرائم وخلافی خودروها

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `BT` · آزمایش وطب کار

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `BU` · ماخد کسر کار - دیر کرد

- **همهٔ سطرها**

  ```
  =U#+V#+CG#+CI#+CK#+CL#
  ```

### `BV` · مبلغ کسر کار - دیر کرد

- **60 سطر**

  ```
  =ROUND((BU#/220*P#)+(BU#/11440*Q#),0)
  ```

- **1 سطر**

  ```
  =ROUND((CW#*23%),0)
  ```

### `BW` · جمع كسورات

- **همهٔ سطرها**

  ```
  =BV#+BT#+BS#+BR#+BQ#+BP#+BO#+BK#+BE#+BC#+AX#
  ```

### `BX` · خالص پرداخت

- **همهٔ سطرها**

  ```
  =AW#-BW#
  ```

### `BY` · شماره کارت

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `BZ` · شماره حساب

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `CA` · بانك عامل

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `CB` · مزدروزانه 1404

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `CC` · پایه سنوات تجمیعی روزانه سال 1405

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `CD` · پایه سنوات تجمیعی ماهانه سال 1405

- **همهٔ سطرها**

  ```
  =CC#*30
  ```

### `CE` · حداقل دستمزد 1405

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `CF` · پایه سنوات روزانه 1405

- **39 سطر**

  ```
  =ROUND((5000000/30),0)
  ```

- 22 سطر به‌جای فرمول، عددِ ثابت دارند.

### `CG` · حق مسکن

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `CH` · تعداد اولاد

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `CI` · حق اولاد

- **همهٔ سطرها**

  ```
  =CH#*CE#*3
  ```

### `CJ` · وضعیت تاهل

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `CK` · حق تاهل

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `CL` · وجه بن

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `CM` · ماخذ پایانکار وعیدی وپاداش ووجه مرخصی وماموریت

- **38 سطر**

  ```
  =T#
  ```

- 23 سطر به‌جای فرمول، عددِ ثابت دارند.

### `CN` · کسر مازاد پرداختی فروش

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `CO` · پورسانت یک

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `CP` · پورسانت دو

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `CQ` · پورسانت سه

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `CR` · مازاد ثابت

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `CS` · مازاد پرداختی ماهانه

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `CT` · ماخذ محاسبه اضافه کاری

- **همهٔ سطرها**

  ```
  =W#
  ```

### `CU` · اضافه کارواقعی دقیقه

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `CV` · اضافه کار واقعی ساعت

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `CW` · مبلغ اضافه کار واقعی

- **همهٔ سطرها**

  ```
  =ROUND((CT#/220*1.4*CV#)+(CT#/13200*1.4*CU#),0)
  ```

### `CX` · جمع مازاد ثابت / مازاد پرداختی ماهانه / اضافه کار واقعی ومرخصی

- **59 سطر**

  ```
  =CW#+CS#+CR#+AL#
  ```

- **2 سطر**

  ```
  =CW#+CS#+CR#
  ```

### `CY` · قابل پرداخت مازاد ثابت / اضافه کار ثابت / اضافه کار واقعی

- **37 سطر**

  ```
  =CX#-AC#
  ```

- 24 سطر به‌جای فرمول، عددِ ثابت دارند.

### `CZ` · محاسبه مدت ماموریت

- **37 سطر**

  ```
  =ROUNDDOWN((CY#/CM#),0)
  ```

- 24 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DA` · محاسبه مبلغ ماموریت

- **37 سطر**

  ```
  =CZ#*CM#
  ```

- 24 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DB` · مازاد محاسبه یک

- **38 سطر**

  ```
  =CY#-DA#
  ```

- **1 سطر**

  ```
  =CX#-DA#
  ```

- 22 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DC` · اضافه کار هر ساعت

- **38 سطر**

  ```
  =ROUND((W#/220*1.4*1),0)
  ```

- 23 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DD` · اضافه کار هردقیقه

- **38 سطر**

  ```
  =ROUND((W#/13200*1.4*1),0)
  ```

- 23 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DE` · محاسبه اضافه کار دقیقه

- **38 سطر**

  ```
  =ROUNDUP(IF(OR(DI#<DD#),0,IF(OR(DI#>DD#),DI#/DD#)),0)
  ```

- 23 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DF` · محاسبه اضافه کار ساعت

- **38 سطر**

  ```
  =ROUNDDOWN((DB#/DC#),0)
  ```

- 23 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DG` · مبلغ اضافه کار محاسبه دقیقه

- **37 سطر**

  ```
  =ROUND((W#/13200*1.4*DE#),0)
  ```

- **1 سطر**

  ```
  =ROUND((CT#/13200*1.4*DE#),0)
  ```

- 23 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DH` · مبلغ اضافه کار محاسبه ساعت

- **37 سطر**

  ```
  =ROUND((W#/220*1.4*DF#),0)
  ```

- **1 سطر**

  ```
  =ROUND((CT#/220*1.4*DF#),0)
  ```

- 23 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DI` · مازاد محاسبه دو

- **38 سطر**

  ```
  =DB#-DH#
  ```

- 23 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DJ` · مازاد محاسبه سه

- **38 سطر**

  ```
  =DI#-DG#
  ```

- 23 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DK` · مانده مرخصی قابل استفاده در آخر سال 1404

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `DL` · مرخصی انتقالی از سال 1404 به روز

- **59 سطر**

  ```
  =ROUND(IF(OR(DK#=9,DK#>9),9,IF(OR(DK#<9,),DK#)),0)
  ```

- 2 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DM` · مرخصی استحقاق سال 1405 به روز

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `DN` · جمع مر خصی قابل استفاده در سال 1405 به روز

- **60 سطر**

  ```
  =DM#+DL#
  ```

- **1 سطر**

  ```
  =ROUND(IF(OR(O#=30,O#=31),DM#,IF(OR(O#<30,O#<31),DM#/30*60)),0)
  ```

### `DO` · مرخصی قابل استفاده سال 1405 به ساعت

- **60 سطر**

  ```
  =ROUNDDOWN((DN#*440)/60,0)
  ```

- **1 سطر**

  ```
  =ROUNDDOWN((DN#/60),0)
  ```

### `DP` · جمع مرخصی قابل استفاده سال 1405 به دقیقه

- **60 سطر**

  ```
  =ROUNDDOWN((DN#*440)-(DO#*60),0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DQ` · مصرفی ماه اعلامی دقیقه

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `DR` · مصرفی ماه اعلامی ساعت

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `DS` · مصرفی ماه اعلامی روز

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `DT` · مصرفی ماه واقعی دقیقه

- **60 سطر**

  ```
  =ROUNDDOWN((DS#*440+DR#*60+DQ#-(DV#*440+DU#*60)),0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DU` · مصرفی ماه واقعی ساعت

- **60 سطر**

  ```
  =ROUNDDOWN((DS#*440+DR#*60+DQ#-DV#*440)/60,0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DV` · مصرفی ماه واقعی روز

- **60 سطر**

  ```
  =ROUNDDOWN((DS#*440+DR#*60+DQ#)/440,0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DW` · مصرفی اعلامی تا ماه دقیقه

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `DX` · مصرفی اعلامی تا ماه ساعت

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `DY` · مصرفی اعلامی تا ماه روز

عدد یا متنِ ورودی — 61 سطر مقدار دارد.

### `DZ` · مصرفی تا ماه دقیقه

- **60 سطر**

  ```
  =ROUNDDOWN((DY#*440+DX#*60+DW#-(EB#*440+EA#*60)),0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `EA` · مصرفی تا ماه ساعت

- **60 سطر**

  ```
  =ROUNDDOWN((DY#*440+DX#*60+DW#-EB#*440)/60,0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `EB` · مصرفی تا ماه روز

- **60 سطر**

  ```
  =ROUNDDOWN((DY#*440+DX#*60+DW#)/440,0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `EC` · استفاده شده کل تا ماه دقیقه

- **60 سطر**

  ```
  =ROUNDDOWN((EB#*440+EA#*60+DZ#),0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `ED` · مانده تا آخر سال دقیقه

- **60 سطر**

  ```
  =ROUNDDOWN((DN#*440-EC#-(EF#*440+EE#*60)),0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `EE` · مانده تا آخر سال ساعت

- **60 سطر**

  ```
  =ROUNDDOWN((DN#*440-EC#-EF#*440)/60,0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `EF` · مانده تا آخر سال روز

- **60 سطر**

  ```
  =ROUNDDOWN((DN#*440-EC#)/440,0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.


---

## شیت «تیر بدون بیمه 1405» — 6 سطر

### `A` · رديف

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `B` · شماره پرسنلی

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `C` · واحد

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `D` · آقا / خانم

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `E` · نام ونام خانوادگي

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `F` · تاریخ استخدام

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `G` · شغل

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `H` · تاريخ تهيه

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `I` · ماه

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `J` · سال

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `K` · روز های ماه

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `L` · غیبت

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `M` · بدون حقوق

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `N` · بیماری

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `O` · کارکرد واقعی ماهانه

- **همهٔ سطرها**

  ```
  =K#-(L#+M#+N#)
  ```

### `P` · تاخیر ساعت

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `Q` · تاخیر دقیقه

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `R` · مزدروزانه

- **5 سطر**

  ```
  =ROUND((CB#*1.45)+(519549),0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `S` · پایه سنوات روزانه

- **3 سطر**

  ```
  =ROUND((5000000/30),0)
  ```

- 3 سطر به‌جای فرمول، عددِ ثابت دارند.

### `T` · جمع مزد وسنوات روزانه

- **همهٔ سطرها**

  ```
  =S#+R#
  ```

### `U` · حقوق ماهانه

- **همهٔ سطرها**

  ```
  =R#*30
  ```

### `V` · پایه سنوات ماهانه

- **همهٔ سطرها**

  ```
  =S#*30
  ```

### `W` · جمع حقوق وسنوات ماهانه

- **همهٔ سطرها**

  ```
  =V#+U#
  ```

### `X` · پایه سنوات تجمیعی روزانه

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `Y` · پایه سنوات تجمیعی ماهانه

- **همهٔ سطرها**

  ```
  =X#*O#
  ```

### `Z` · حقوق قابل پرداخت

- **همهٔ سطرها**

  ```
  =R#*O#
  ```

### `AA` · سنوات قابل پرداخت

- **همهٔ سطرها**

  ```
  =S#*O#
  ```

### `AB` · جمع حقوق وسنوات قابل پرداخت

- **همهٔ سطرها**

  ```
  =AA#+Z#
  ```

### `AC` · مابه التفاوت پایه سنوات تجمیعی قابل پرداخت

- **همهٔ سطرها**

  ```
  =ROUND(IF(OR(Y#>=AA#),Y#-AA#,IF(OR(Y#<=AA#),0)),0)
  ```

### `AD` · پایه سنوات تجمیعی قابل پرداخت

- **همهٔ سطرها**

  ```
  =AC#+AA#
  ```

### `AE` · حق مسكن قابل پرداخت

- **همهٔ سطرها**

  ```
  =ROUND(IF(OR(O#=30,O#=31),CG#,IF(OR(O#<30,O#<31),CG#/31*O#)),0)
  ```

### `AF` · حق اولاد قابل پرداخت

- **همهٔ سطرها**

  ```
  =ROUND(IF(OR(O#=30,O#=31),CI#,IF(OR(O#<30,O#<31),CI#/31*O#)),0)
  ```

### `AG` · حق تاهل قابل پرداخت

- **همهٔ سطرها**

  ```
  =ROUND(IF(OR(O#=30,O#=31),CK#,IF(OR(O#<30,O#<31),CK#/31*O#)),0)
  ```

### `AH` · وجه بن قابل پرداخت

- **همهٔ سطرها**

  ```
  =ROUND(IF(OR(O#=30,O#=31),CL#,IF(OR(O#<30,O#<31),CL#/31*O#)),0)
  ```

### `AI` · اضافه کاری عادی دقیقه

- **5 سطر**

  ```
  =DE#
  ```

- **1 سطر**

  ```
  =CU#
  ```

### `AJ` · اضافه کاری عادی ساعت

- **5 سطر**

  ```
  =DF#
  ```

- **1 سطر**

  ```
  =CV#
  ```

### `AK` · مبلغ اضافه كاري کل

- **5 سطر**

  ```
  =ROUND((W#/220*1.4*AJ#)+(W#/13200*1.4*AI#),0)
  ```

- **1 سطر**

  ```
  =ROUND((CT#/220*1.4*AJ#)+(CT#/13200*1.4*AI#),0)
  ```

### `AL` · وجه مرخصی

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `AM` · پایانکار قابل پرداخت

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `AN` · عیدی وپاداش قابل پرداخت

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `AO` · مدت ماموریت

- **5 سطر**

  ```
  =CZ#
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `AP` · مبلغ ماموریت

- **5 سطر**

  ```
  =AO#*T#
  ```

- **1 سطر**

  ```
  =AO#*T#*2
  ```

### `AQ` · ذخیره پورسانت فروش

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `AR` · پورسانت یک قابل پرداخت

- **1 سطر**

  ```
  =(CO#+CR#+AL#)-(AC#+AQ#+CN#+AP#)
  ```

- 5 سطر به‌جای فرمول، عددِ ثابت دارند.

### `AS` · پورسانت قابل پرداخت دو

- **1 سطر**

  ```
  =CP#
  ```

- 5 سطر به‌جای فرمول، عددِ ثابت دارند.

### `AT` · پورسانت قابل پرداخت سه

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `AU` · طلب ماه قبل

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `AV` · هزینه تلفن

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `AW` · جمع پرداختيها فیش

- **همهٔ سطرها**

  ```
  =AU#+AT#+AS#+AR#+AP#+AN#+AM#+AL#+AK#+AH#+AG#+AF#+AE#+AC#+AA#+Z#+AV#
  ```

### `AX` · علی الحساب واریزی

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `AY` · علی الحساب نقدی

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `AZ` · جمع علی الحساب

- **همهٔ سطرها**

  ```
  =AX#+AY#
  ```

### `BA` · ماخذ مالیات

- **همهٔ سطرها**

  ```
  =AW#-AP#
  ```

### `BB` · مالیات

- **همهٔ سطرها**

  ```
  =IF(BA#<=400000000,0, IF(BA#<=800000000,(BA#-400000000)*10%, IF(BA#<=1000000000,(BA#-800000000)*15%+40000000, IF(BA#<=1200000000,(BA#-1000000000)*20%+70000000, IF(BA#<=1400000000,(BA#-1200000000)*25%+140000000, IF(BA#>1400000001,(BA#-1400000000)*30%+190000000))))))
  ```

### `BC` · روند مالیات

- **همهٔ سطرها**

  ```
  =ROUND((BB#),0)
  ```

### `BD` · ماخذ بیمه

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `BE` · سهم کارگر

- **همهٔ سطرها**

  ```
  =ROUND((BD#*7%),0)
  ```

### `BF` · سهم کارفرما

- **همهٔ سطرها**

  ```
  =ROUND((BD#*20%),0)
  ```

### `BG` · بیمه بیکاری

- **همهٔ سطرها**

  ```
  =ROUND((BD#*3%),0)
  ```

### `BH` · جمع کل بیمه

- **5 سطر**

  ```
  =ROUND((BG#+BF#+BE#),0)
  ```

- **1 سطر**

  ```
  =BG#+BF#+BE#
  ```

### `BI` · مبلغ کل وام

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `BJ` · تعدادكل قسط

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `BK` · مبلغ قسط

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `BL` · شماره قسط

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `BM` · مانده قسط

- **همهٔ سطرها**

  ```
  =BJ#-BL#
  ```

### `BN` · مانده وام

- **همهٔ سطرها**

  ```
  =BM#*BK#
  ```

### `BO` · خرید از شرکت

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `BP` · بدهی از ماه قبل

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `BQ` · سایر بدهی

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `BR` · بیمه تکمیلی

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `BS` · جرائم وخلافی خودروها

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `BT` · آزمایش وطب کار

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `BU` · ماخد کسر کار - دیر کرد

- **همهٔ سطرها**

  ```
  =U#+V#+CG#+CI#+CK#+CL#
  ```

### `BV` · مبلغ کسر کار - دیر کرد

- **همهٔ سطرها**

  ```
  =ROUND((BU#/220*P#)+(BU#/11440*Q#),0)
  ```

### `BW` · جمع كسورات

- **همهٔ سطرها**

  ```
  =BV#+BT#+BS#+BR#+BQ#+BP#+BO#+BK#+BE#+BC#+AX#
  ```

### `BX` · خالص پرداخت

- **همهٔ سطرها**

  ```
  =AW#-BW#
  ```

### `BY` · شماره کارت

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `BZ` · شماره حساب

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `CA` · بانك عامل

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `CB` · مزدروزانه 1404

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `CC` · پایه سنوات تجمیعی روزانه سال 1405

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `CD` · پایه سنوات تجمیعی ماهانه سال 1405

- **همهٔ سطرها**

  ```
  =CC#*30
  ```

### `CE` · حداقل دستمزد 1405

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `CF` · پایه سنوات روزانه 1405

- **3 سطر**

  ```
  =ROUND((5000000/30),0)
  ```

- 3 سطر به‌جای فرمول، عددِ ثابت دارند.

### `CG` · حق مسکن

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `CH` · تعداد اولاد

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `CI` · حق اولاد

- **همهٔ سطرها**

  ```
  =CH#*CE#*3
  ```

### `CJ` · وضعیت تاهل

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `CK` · حق تاهل

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `CL` · وجه بن

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `CM` · ماخذ پایانکار وعیدی وپاداش ووجه مرخصی وماموریت

- **5 سطر**

  ```
  =T#
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `CN` · کسر مازاد پرداختی فروش

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `CO` · پورسانت یک

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `CP` · پورسانت دو

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `CQ` · پورسانت سه

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `CR` · مازاد ثابت

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `CS` · مازاد پرداختی ماهانه

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `CT` · ماخذ محاسبه اضافه کاری

- **همهٔ سطرها**

  ```
  =W#
  ```

### `CU` · اضافه کارواقعی دقیقه

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `CV` · اضافه کار واقعی ساعت

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `CW` · مبلغ اضافه کار واقعی

- **همهٔ سطرها**

  ```
  =ROUND((CT#/220*1.4*CV#)+(CT#/13200*1.4*CU#),0)
  ```

### `CX` · جمع مازاد ثابت / مازاد پرداختی ماهانه / اضافه کار واقعی ومرخصی

- **همهٔ سطرها**

  ```
  =CW#+CS#+CR#+AL#
  ```

### `CY` · قابل پرداخت مازاد ثابت / اضافه کار ثابت / اضافه کار واقعی

- **5 سطر**

  ```
  =CX#-AC#
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `CZ` · محاسبه مدت ماموریت

- **5 سطر**

  ```
  =ROUNDDOWN((CY#/CM#),0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DA` · محاسبه مبلغ ماموریت

- **5 سطر**

  ```
  =CZ#*CM#
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DB` · مازاد محاسبه یک

- **5 سطر**

  ```
  =CY#-DA#
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DC` · اضافه کار هر ساعت

- **5 سطر**

  ```
  =ROUND((W#/220*1.4*1),0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DD` · اضافه کار هردقیقه

- **5 سطر**

  ```
  =ROUND((W#/13200*1.4*1),0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DE` · محاسبه اضافه کار دقیقه

- **5 سطر**

  ```
  =ROUNDUP(IF(OR(DI#<DD#),0,IF(OR(DI#>DD#),DI#/DD#)),0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DF` · محاسبه اضافه کار ساعت

- **5 سطر**

  ```
  =ROUNDDOWN((DB#/DC#),0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DG` · مبلغ اضافه کار محاسبه دقیقه

- **5 سطر**

  ```
  =ROUND((W#/13200*1.4*DE#),0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DH` · مبلغ اضافه کار محاسبه ساعت

- **5 سطر**

  ```
  =ROUND((W#/220*1.4*DF#),0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DI` · مازاد محاسبه دو

- **5 سطر**

  ```
  =DB#-DH#
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DJ` · مازاد محاسبه سه

- **5 سطر**

  ```
  =DI#-DG#
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DK` · مانده مرخصی قابل استفاده در آخر سال 1404

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `DL` · مرخصی انتقالی از سال 1404 به روز

- **همهٔ سطرها**

  ```
  =ROUND(IF(OR(DK#=9,DK#>9),9,IF(OR(DK#<9,),DK#)),0)
  ```

### `DM` · مرخصی استحقاق سال 1405 به روز

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `DN` · جمع مر خصی قابل استفاده در سال 1405 به روز

- **همهٔ سطرها**

  ```
  =DM#+DL#
  ```

### `DO` · مرخصی قابل استفاده سال 1405 به ساعت

- **همهٔ سطرها**

  ```
  =ROUNDDOWN((DN#*440)/60,0)
  ```

### `DP` · جمع مرخصی قابل استفاده سال 1405 به دقیقه

- **همهٔ سطرها**

  ```
  =ROUNDDOWN((DN#*440)-(DO#*60),0)
  ```

### `DQ` · مصرفی ماه اعلامی دقیقه

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `DR` · مصرفی ماه اعلامی ساعت

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `DS` · مصرفی ماه اعلامی روز

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `DT` · مصرفی ماه واقعی دقیقه

- **همهٔ سطرها**

  ```
  =ROUNDDOWN((DS#*440+DR#*60+DQ#-(DV#*440+DU#*60)),0)
  ```

### `DU` · مصرفی ماه واقعی ساعت

- **همهٔ سطرها**

  ```
  =ROUNDDOWN((DS#*440+DR#*60+DQ#-DV#*440)/60,0)
  ```

### `DV` · مصرفی ماه واقعی روز

- **همهٔ سطرها**

  ```
  =ROUNDDOWN((DS#*440+DR#*60+DQ#)/440,0)
  ```

### `DW` · مصرفی اعلامی تا ماه دقیقه

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `DX` · مصرفی اعلامی تا ماه ساعت

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `DY` · مصرفی اعلامی تا ماه روز

عدد یا متنِ ورودی — 6 سطر مقدار دارد.

### `DZ` · مصرفی تا ماه دقیقه

- **همهٔ سطرها**

  ```
  =ROUNDDOWN((DY#*440+DX#*60+DW#-(EB#*440+EA#*60)),0)
  ```

### `EA` · مصرفی تا ماه ساعت

- **همهٔ سطرها**

  ```
  =ROUNDDOWN((DY#*440+DX#*60+DW#-EB#*440)/60,0)
  ```

### `EB` · مصرفی تا ماه روز

- **همهٔ سطرها**

  ```
  =ROUNDDOWN((DY#*440+DX#*60+DW#)/440,0)
  ```

### `EC` · استفاده شده کل تا ماه دقیقه

- **همهٔ سطرها**

  ```
  =ROUNDDOWN((EB#*440+EA#*60+DZ#),0)
  ```

### `ED` · مانده تا آخر سال دقیقه

- **همهٔ سطرها**

  ```
  =ROUNDDOWN((DN#*440-EC#-(EF#*440+EE#*60)),0)
  ```

### `EE` · مانده تا آخر سال ساعت

- **همهٔ سطرها**

  ```
  =ROUNDDOWN((DN#*440-EC#-EF#*440)/60,0)
  ```

### `EF` · مانده تا آخر سال روز

- **همهٔ سطرها**

  ```
  =ROUNDDOWN((DN#*440-EC#)/440,0)
  ```


---

## شیت «مرداد با بیمه 1405» — 63 سطر

### `A` · رديف

عدد یا متنِ ورودی — 63 سطر مقدار دارد.

### `B` · شماره پرسنلی

عدد یا متنِ ورودی — 48 سطر مقدار دارد.

### `C` · واحد

عدد یا متنِ ورودی — 63 سطر مقدار دارد.

### `D` · آقا / خانم

عدد یا متنِ ورودی — 63 سطر مقدار دارد.

### `E` · نام ونام خانوادگي

عدد یا متنِ ورودی — 63 سطر مقدار دارد.

### `F` · تاریخ استخدام

عدد یا متنِ ورودی — 63 سطر مقدار دارد.

### `G` · شغل

عدد یا متنِ ورودی — 63 سطر مقدار دارد.

### `H` · تاريخ تهيه

عدد یا متنِ ورودی — 63 سطر مقدار دارد.

### `I` · ماه

عدد یا متنِ ورودی — 63 سطر مقدار دارد.

### `J` · سال

عدد یا متنِ ورودی — 63 سطر مقدار دارد.

### `K` · روز های ماه

عدد یا متنِ ورودی — 63 سطر مقدار دارد.

### `L` · غیبت

عدد یا متنِ ورودی — 63 سطر مقدار دارد.

### `M` · بدون حقوق

عدد یا متنِ ورودی — 63 سطر مقدار دارد.

### `N` · بیماری

عدد یا متنِ ورودی — 63 سطر مقدار دارد.

### `O` · کارکرد واقعی ماهانه

- **همهٔ سطرها**

  ```
  =K#-(L#+M#+N#)
  ```

### `P` · تاخیر ساعت

عدد یا متنِ ورودی — 63 سطر مقدار دارد.

### `Q` · تاخیر دقیقه

عدد یا متنِ ورودی — 63 سطر مقدار دارد.

### `R` · مزدروزانه

- **61 سطر**

  ```
  =ROUND((CB#*1.45)+(519549),0)
  ```

- 2 سطر به‌جای فرمول، عددِ ثابت دارند.

### `S` · پایه سنوات روزانه

- **44 سطر**

  ```
  =ROUND((5000000/30),0)
  ```

- 19 سطر به‌جای فرمول، عددِ ثابت دارند.

### `T` · جمع مزد وسنوات روزانه

- **همهٔ سطرها**

  ```
  =S#+R#
  ```

### `U` · حقوق ماهانه

- **همهٔ سطرها**

  ```
  =R#*30
  ```

### `V` · پایه سنوات ماهانه

- **همهٔ سطرها**

  ```
  =S#*30
  ```

### `W` · جمع حقوق وسنوات ماهانه

- **همهٔ سطرها**

  ```
  =V#+U#
  ```

### `X` · پایه سنوات تجمیعی روزانه

عدد یا متنِ ورودی — 63 سطر مقدار دارد.

### `Y` · پایه سنوات تجمیعی ماهانه

- **همهٔ سطرها**

  ```
  =X#*O#
  ```

### `Z` · حقوق قابل پرداخت

- **همهٔ سطرها**

  ```
  =R#*O#
  ```

### `AA` · سنوات قابل پرداخت

- **همهٔ سطرها**

  ```
  =S#*O#
  ```

### `AB` · جمع حقوق وسنوات قابل پرداخت

- **همهٔ سطرها**

  ```
  =AA#+Z#
  ```

### `AC` · مابه التفاوت پایه سنوات تجمیعی قابل پرداخت

- **همهٔ سطرها**

  ```
  =ROUND(IF(OR(Y#>=AA#),Y#-AA#,IF(OR(Y#<=AA#),0)),0)
  ```

### `AD` · پایه سنوات تجمیعی قابل پرداخت

- **همهٔ سطرها**

  ```
  =AC#+AA#
  ```

### `AE` · حق مسكن قابل پرداخت

- **همهٔ سطرها**

  ```
  =ROUND(IF(OR(O#=30,O#=31),CG#,IF(OR(O#<30,O#<31),CG#/31*O#)),0)
  ```

### `AF` · حق اولاد قابل پرداخت

- **همهٔ سطرها**

  ```
  =ROUND(IF(OR(O#=30,O#=31),CI#,IF(OR(O#<30,O#<31),CI#/31*O#)),0)
  ```

### `AG` · حق تاهل قابل پرداخت

- **همهٔ سطرها**

  ```
  =ROUND(IF(OR(O#=30,O#=31),CK#,IF(OR(O#<30,O#<31),CK#/31*O#)),0)
  ```

### `AH` · وجه بن قابل پرداخت

- **همهٔ سطرها**

  ```
  =ROUND(IF(OR(O#=30,O#=31),CL#,IF(OR(O#<30,O#<31),CL#/31*O#)),0)
  ```

### `AI` · اضافه کاری عادی دقیقه

- **41 سطر**

  ```
  =DE#
  ```

- **22 سطر**

  ```
  =CU#
  ```

### `AJ` · اضافه کاری عادی ساعت

- **41 سطر**

  ```
  =DF#
  ```

- **22 سطر**

  ```
  =CV#
  ```

### `AK` · مبلغ اضافه كاري کل

- **41 سطر**

  ```
  =ROUND((W#/220*1.4*AJ#)+(W#/13200*1.4*AI#),0)
  ```

- **22 سطر**

  ```
  =ROUND((CT#/220*1.4*AJ#)+(CT#/13200*1.4*AI#),0)
  ```

### `AL` · وجه مرخصی

- **1 سطر**

  ```
  =ROUND((CM#/7.333*16/30*DM#),0)
  ```

- 62 سطر به‌جای فرمول، عددِ ثابت دارند.

### `AM` · پایانکار قابل پرداخت

- **1 سطر**

  ```
  =ROUND((T#*2.5/30*30),0)
  ```

- 62 سطر به‌جای فرمول، عددِ ثابت دارند.

### `AN` · عیدی وپاداش قابل پرداخت

- **1 سطر**

  ```
  =ROUND((CM#*5/30*30),0)
  ```

- 62 سطر به‌جای فرمول، عددِ ثابت دارند.

### `AO` · مدت ماموریت

- **38 سطر**

  ```
  =CZ#
  ```

- 25 سطر به‌جای فرمول، عددِ ثابت دارند.

### `AP` · مبلغ ماموریت

- **40 سطر**

  ```
  =AO#*T#
  ```

- **23 سطر**

  ```
  =AO#*T#*2
  ```

### `AQ` · ذخیره پورسانت فروش

عدد یا متنِ ورودی — 63 سطر مقدار دارد.

### `AR` · پورسانت یک قابل پرداخت

- **22 سطر**

  ```
  =(CO#+CR#+AL#)-(AC#+AQ#+CN#+AP#)
  ```

- **1 سطر**

  ```
  =CO#
  ```

- **1 سطر**

  ```
  =(CO#+CR#+AL#)-(AC#+AQ#+CN#+AP#+CW#)
  ```

- 39 سطر به‌جای فرمول، عددِ ثابت دارند.

### `AS` · پورسانت قابل پرداخت دو

- **20 سطر**

  ```
  =CP#
  ```

- 43 سطر به‌جای فرمول، عددِ ثابت دارند.

### `AT` · پورسانت قابل پرداخت سه

عدد یا متنِ ورودی — 63 سطر مقدار دارد.

### `AU` · طلب ماه قبل

عدد یا متنِ ورودی — 63 سطر مقدار دارد.

### `AV` · هزینه تلفن

عدد یا متنِ ورودی — 63 سطر مقدار دارد.

### `AW` · جمع پرداختيها فیش

- **همهٔ سطرها**

  ```
  =AU#+AT#+AS#+AR#+AP#+AN#+AM#+AL#+AK#+AH#+AG#+AF#+AE#+AC#+AA#+Z#+AV#
  ```

### `AX` · علی الحساب واریزی

عدد یا متنِ ورودی — 63 سطر مقدار دارد.

### `AY` · علی الحساب نقدی

عدد یا متنِ ورودی — 63 سطر مقدار دارد.

### `AZ` · جمع علی الحساب

- **62 سطر**

  ```
  =AX#+AY#
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `BA` · ماخذ مالیات

- **همهٔ سطرها**

  ```
  =AW#-AP#
  ```

### `BB` · مالیات

- **همهٔ سطرها**

  ```
  =IF(BA#<=400000000,0, IF(BA#<=800000000,(BA#-400000000)*10%, IF(BA#<=1000000000,(BA#-800000000)*15%+40000000, IF(BA#<=1200000000,(BA#-1000000000)*20%+70000000, IF(BA#<=1400000000,(BA#-1200000000)*25%+140000000, IF(BA#>1400000001,(BA#-1400000000)*30%+190000000))))))
  ```

### `BC` · روند مالیات

- **62 سطر**

  ```
  =ROUND((BB#),0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `BD` · ماخذ بیمه

- **همهٔ سطرها**

  ```
  =AW#-(AP#+AF#)
  ```

### `BE` · سهم کارگر

- **همهٔ سطرها**

  ```
  =ROUND((BD#*7%),0)
  ```

### `BF` · سهم کارفرما

- **همهٔ سطرها**

  ```
  =ROUND((BD#*20%),0)
  ```

### `BG` · بیمه بیکاری

- **همهٔ سطرها**

  ```
  =ROUND((BD#*3%),0)
  ```

### `BH` · جمع کل بیمه

- **45 سطر**

  ```
  =ROUND((BG#+BF#+BE#),0)
  ```

- **18 سطر**

  ```
  =BG#+BF#+BE#
  ```

### `BI` · مبلغ کل وام

عدد یا متنِ ورودی — 63 سطر مقدار دارد.

### `BJ` · تعدادكل قسط

عدد یا متنِ ورودی — 63 سطر مقدار دارد.

### `BK` · مبلغ قسط

عدد یا متنِ ورودی — 63 سطر مقدار دارد.

### `BL` · شماره قسط

عدد یا متنِ ورودی — 63 سطر مقدار دارد.

### `BM` · مانده قسط

- **60 سطر**

  ```
  =BJ#-BL#
  ```

- 3 سطر به‌جای فرمول، عددِ ثابت دارند.

### `BN` · مانده وام

- **60 سطر**

  ```
  =BM#*BK#
  ```

- 3 سطر به‌جای فرمول، عددِ ثابت دارند.

### `BO` · خرید از شرکت

عدد یا متنِ ورودی — 63 سطر مقدار دارد.

### `BP` · بدهی از ماه قبل

عدد یا متنِ ورودی — 63 سطر مقدار دارد.

### `BQ` · سایر بدهی

عدد یا متنِ ورودی — 63 سطر مقدار دارد.

### `BR` · بیمه تکمیلی

عدد یا متنِ ورودی — 63 سطر مقدار دارد.

### `BS` · جرائم وخلافی خودروها

عدد یا متنِ ورودی — 63 سطر مقدار دارد.

### `BT` · آزمایش وطب کار

عدد یا متنِ ورودی — 63 سطر مقدار دارد.

### `BU` · ماخد کسر کار - دیر کرد

- **همهٔ سطرها**

  ```
  =U#+V#+CG#+CI#+CK#+CL#
  ```

### `BV` · مبلغ کسر کار - دیر کرد

- **62 سطر**

  ```
  =ROUND((BU#/220*P#)+(BU#/11440*Q#),0)
  ```

- **1 سطر**

  ```
  =ROUND((CW#*23%),0)
  ```

### `BW` · جمع كسورات

- **همهٔ سطرها**

  ```
  =BV#+BT#+BS#+BR#+BQ#+BP#+BO#+BK#+BE#+BC#+AX#
  ```

### `BX` · خالص پرداخت

- **همهٔ سطرها**

  ```
  =AW#-BW#
  ```

### `BY` · شماره کارت

عدد یا متنِ ورودی — 63 سطر مقدار دارد.

### `BZ` · شماره حساب

عدد یا متنِ ورودی — 48 سطر مقدار دارد.

### `CA` · بانك عامل

عدد یا متنِ ورودی — 50 سطر مقدار دارد.

### `CB` · مزدروزانه 1404

عدد یا متنِ ورودی — 63 سطر مقدار دارد.

### `CC` · پایه سنوات تجمیعی روزانه سال 1405

عدد یا متنِ ورودی — 63 سطر مقدار دارد.

### `CD` · پایه سنوات تجمیعی ماهانه سال 1405

- **همهٔ سطرها**

  ```
  =CC#*30
  ```

### `CE` · حداقل دستمزد 1405

عدد یا متنِ ورودی — 63 سطر مقدار دارد.

### `CF` · پایه سنوات روزانه 1405

- **40 سطر**

  ```
  =ROUND((5000000/30),0)
  ```

- 23 سطر به‌جای فرمول، عددِ ثابت دارند.

### `CG` · حق مسکن

عدد یا متنِ ورودی — 63 سطر مقدار دارد.

### `CH` · تعداد اولاد

عدد یا متنِ ورودی — 63 سطر مقدار دارد.

### `CI` · حق اولاد

- **همهٔ سطرها**

  ```
  =CH#*CE#*3
  ```

### `CJ` · وضعیت تاهل

عدد یا متنِ ورودی — 63 سطر مقدار دارد.

### `CK` · حق تاهل

عدد یا متنِ ورودی — 63 سطر مقدار دارد.

### `CL` · وجه بن

عدد یا متنِ ورودی — 63 سطر مقدار دارد.

### `CM` · ماخذ پایانکار وعیدی وپاداش ووجه مرخصی وماموریت

- **39 سطر**

  ```
  =T#
  ```

- 24 سطر به‌جای فرمول، عددِ ثابت دارند.

### `CN` · کسر مازاد پرداختی فروش

عدد یا متنِ ورودی — 63 سطر مقدار دارد.

### `CO` · پورسانت یک

عدد یا متنِ ورودی — 63 سطر مقدار دارد.

### `CP` · پورسانت دو

عدد یا متنِ ورودی — 63 سطر مقدار دارد.

### `CQ` · پورسانت سه

عدد یا متنِ ورودی — 63 سطر مقدار دارد.

### `CR` · مازاد ثابت

عدد یا متنِ ورودی — 63 سطر مقدار دارد.

### `CS` · مازاد پرداختی ماهانه

عدد یا متنِ ورودی — 63 سطر مقدار دارد.

### `CT` · ماخذ محاسبه اضافه کاری

- **همهٔ سطرها**

  ```
  =W#
  ```

### `CU` · اضافه کارواقعی دقیقه

عدد یا متنِ ورودی — 63 سطر مقدار دارد.

### `CV` · اضافه کار واقعی ساعت

عدد یا متنِ ورودی — 63 سطر مقدار دارد.

### `CW` · مبلغ اضافه کار واقعی

- **همهٔ سطرها**

  ```
  =ROUND((CT#/220*1.4*CV#)+(CT#/13200*1.4*CU#),0)
  ```

### `CX` · جمع مازاد ثابت / مازاد پرداختی ماهانه / اضافه کار واقعی ومرخصی

- **61 سطر**

  ```
  =CW#+CS#+CR#+AL#
  ```

- **2 سطر**

  ```
  =CW#+CS#+CR#
  ```

### `CY` · قابل پرداخت مازاد ثابت / اضافه کار ثابت / اضافه کار واقعی

- **38 سطر**

  ```
  =CX#-AC#
  ```

- 25 سطر به‌جای فرمول، عددِ ثابت دارند.

### `CZ` · محاسبه مدت ماموریت

- **38 سطر**

  ```
  =ROUNDDOWN((CY#/CM#),0)
  ```

- 25 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DA` · محاسبه مبلغ ماموریت

- **38 سطر**

  ```
  =CZ#*CM#
  ```

- 25 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DB` · مازاد محاسبه یک

- **39 سطر**

  ```
  =CY#-DA#
  ```

- **1 سطر**

  ```
  =CX#-DA#
  ```

- 23 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DC` · اضافه کار هر ساعت

- **39 سطر**

  ```
  =ROUND((W#/220*1.4*1),0)
  ```

- 24 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DD` · اضافه کار هردقیقه

- **39 سطر**

  ```
  =ROUND((W#/13200*1.4*1),0)
  ```

- 24 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DE` · محاسبه اضافه کار دقیقه

- **39 سطر**

  ```
  =ROUNDUP(IF(OR(DI#<DD#),0,IF(OR(DI#>DD#),DI#/DD#)),0)
  ```

- 24 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DF` · محاسبه اضافه کار ساعت

- **39 سطر**

  ```
  =ROUNDDOWN((DB#/DC#),0)
  ```

- 24 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DG` · مبلغ اضافه کار محاسبه دقیقه

- **38 سطر**

  ```
  =ROUND((W#/13200*1.4*DE#),0)
  ```

- **1 سطر**

  ```
  =ROUND((CT#/13200*1.4*DE#),0)
  ```

- 24 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DH` · مبلغ اضافه کار محاسبه ساعت

- **38 سطر**

  ```
  =ROUND((W#/220*1.4*DF#),0)
  ```

- **1 سطر**

  ```
  =ROUND((CT#/220*1.4*DF#),0)
  ```

- 24 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DI` · مازاد محاسبه دو

- **39 سطر**

  ```
  =DB#-DH#
  ```

- 24 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DJ` · مازاد محاسبه سه

- **39 سطر**

  ```
  =DI#-DG#
  ```

- 24 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DK` · مانده مرخصی قابل استفاده در آخر سال 1404

عدد یا متنِ ورودی — 63 سطر مقدار دارد.

### `DL` · مرخصی انتقالی از سال 1404 به روز

- **60 سطر**

  ```
  =ROUND(IF(OR(DK#=9,DK#>9),9,IF(OR(DK#<9,),DK#)),0)
  ```

- 3 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DM` · مرخصی استحقاق سال 1405 به روز

عدد یا متنِ ورودی — 63 سطر مقدار دارد.

### `DN` · جمع مر خصی قابل استفاده در سال 1405 به روز

- **62 سطر**

  ```
  =DM#+DL#
  ```

- **1 سطر**

  ```
  =ROUND(IF(OR(O#=30,O#=31),DM#,IF(OR(O#<30,O#<31),DM#/30*60)),0)
  ```

### `DO` · مرخصی قابل استفاده سال 1405 به ساعت

- **62 سطر**

  ```
  =ROUNDDOWN((DN#*440)/60,0)
  ```

- **1 سطر**

  ```
  =ROUNDDOWN((DN#/60),0)
  ```

### `DP` · جمع مرخصی قابل استفاده سال 1405 به دقیقه

- **62 سطر**

  ```
  =ROUNDDOWN((DN#*440)-(DO#*60),0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DQ` · مصرفی ماه اعلامی دقیقه

عدد یا متنِ ورودی — 63 سطر مقدار دارد.

### `DR` · مصرفی ماه اعلامی ساعت

عدد یا متنِ ورودی — 63 سطر مقدار دارد.

### `DS` · مصرفی ماه اعلامی روز

عدد یا متنِ ورودی — 63 سطر مقدار دارد.

### `DT` · مصرفی ماه واقعی دقیقه

- **62 سطر**

  ```
  =ROUNDDOWN((DS#*440+DR#*60+DQ#-(DV#*440+DU#*60)),0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DU` · مصرفی ماه واقعی ساعت

- **62 سطر**

  ```
  =ROUNDDOWN((DS#*440+DR#*60+DQ#-DV#*440)/60,0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DV` · مصرفی ماه واقعی روز

- **62 سطر**

  ```
  =ROUNDDOWN((DS#*440+DR#*60+DQ#)/440,0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DW` · مصرفی اعلامی تا ماه دقیقه

عدد یا متنِ ورودی — 63 سطر مقدار دارد.

### `DX` · مصرفی اعلامی تا ماه ساعت

عدد یا متنِ ورودی — 63 سطر مقدار دارد.

### `DY` · مصرفی اعلامی تا ماه روز

عدد یا متنِ ورودی — 63 سطر مقدار دارد.

### `DZ` · مصرفی تا ماه دقیقه

- **62 سطر**

  ```
  =ROUNDDOWN((DY#*440+DX#*60+DW#-(EB#*440+EA#*60)),0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `EA` · مصرفی تا ماه ساعت

- **62 سطر**

  ```
  =ROUNDDOWN((DY#*440+DX#*60+DW#-EB#*440)/60,0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `EB` · مصرفی تا ماه روز

- **62 سطر**

  ```
  =ROUNDDOWN((DY#*440+DX#*60+DW#)/440,0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `EC` · استفاده شده کل تا ماه دقیقه

- **62 سطر**

  ```
  =ROUNDDOWN((EB#*440+EA#*60+DZ#),0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `ED` · مانده تا آخر سال دقیقه

- **62 سطر**

  ```
  =ROUNDDOWN((DN#*440-EC#-(EF#*440+EE#*60)),0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `EE` · مانده تا آخر سال ساعت

- **62 سطر**

  ```
  =ROUNDDOWN((DN#*440-EC#-EF#*440)/60,0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `EF` · مانده تا آخر سال روز

- **62 سطر**

  ```
  =ROUNDDOWN((DN#*440-EC#)/440,0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.


---

## شیت «مرداد بدون بیمه» — 5 سطر

### `A` · رديف

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `B` · شماره پرسنلی

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `C` · واحد

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `D` · آقا / خانم

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `E` · نام ونام خانوادگي

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `F` · تاریخ استخدام

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `G` · شغل

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `H` · تاريخ تهيه

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `I` · ماه

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `J` · سال

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `K` · روز های ماه

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `L` · غیبت

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `M` · بدون حقوق

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `N` · بیماری

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `O` · کارکرد واقعی ماهانه

- **همهٔ سطرها**

  ```
  =K#-(L#+M#+N#)
  ```

### `P` · تاخیر ساعت

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `Q` · تاخیر دقیقه

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `R` · مزدروزانه

- **4 سطر**

  ```
  =ROUND((CB#*1.45)+(519549),0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `S` · پایه سنوات روزانه

- **3 سطر**

  ```
  =ROUND((5000000/30),0)
  ```

- 2 سطر به‌جای فرمول، عددِ ثابت دارند.

### `T` · جمع مزد وسنوات روزانه

- **همهٔ سطرها**

  ```
  =S#+R#
  ```

### `U` · حقوق ماهانه

- **همهٔ سطرها**

  ```
  =R#*30
  ```

### `V` · پایه سنوات ماهانه

- **همهٔ سطرها**

  ```
  =S#*30
  ```

### `W` · جمع حقوق وسنوات ماهانه

- **همهٔ سطرها**

  ```
  =V#+U#
  ```

### `X` · پایه سنوات تجمیعی روزانه

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `Y` · پایه سنوات تجمیعی ماهانه

- **همهٔ سطرها**

  ```
  =X#*O#
  ```

### `Z` · حقوق قابل پرداخت

- **همهٔ سطرها**

  ```
  =R#*O#
  ```

### `AA` · سنوات قابل پرداخت

- **همهٔ سطرها**

  ```
  =S#*O#
  ```

### `AB` · جمع حقوق وسنوات قابل پرداخت

- **همهٔ سطرها**

  ```
  =AA#+Z#
  ```

### `AC` · مابه التفاوت پایه سنوات تجمیعی قابل پرداخت

- **همهٔ سطرها**

  ```
  =ROUND(IF(OR(Y#>=AA#),Y#-AA#,IF(OR(Y#<=AA#),0)),0)
  ```

### `AD` · پایه سنوات تجمیعی قابل پرداخت

- **همهٔ سطرها**

  ```
  =AC#+AA#
  ```

### `AE` · حق مسكن قابل پرداخت

- **همهٔ سطرها**

  ```
  =ROUND(IF(OR(O#=30,O#=31),CG#,IF(OR(O#<30,O#<31),CG#/31*O#)),0)
  ```

### `AF` · حق اولاد قابل پرداخت

- **همهٔ سطرها**

  ```
  =ROUND(IF(OR(O#=30,O#=31),CI#,IF(OR(O#<30,O#<31),CI#/31*O#)),0)
  ```

### `AG` · حق تاهل قابل پرداخت

- **همهٔ سطرها**

  ```
  =ROUND(IF(OR(O#=30,O#=31),CK#,IF(OR(O#<30,O#<31),CK#/31*O#)),0)
  ```

### `AH` · وجه بن قابل پرداخت

- **همهٔ سطرها**

  ```
  =ROUND(IF(OR(O#=30,O#=31),CL#,IF(OR(O#<30,O#<31),CL#/31*O#)),0)
  ```

### `AI` · اضافه کاری عادی دقیقه

- **4 سطر**

  ```
  =DE#
  ```

- **1 سطر**

  ```
  =CU#
  ```

### `AJ` · اضافه کاری عادی ساعت

- **4 سطر**

  ```
  =DF#
  ```

- **1 سطر**

  ```
  =CV#
  ```

### `AK` · مبلغ اضافه كاري کل

- **4 سطر**

  ```
  =ROUND((W#/220*1.4*AJ#)+(W#/13200*1.4*AI#),0)
  ```

- **1 سطر**

  ```
  =ROUND((CT#/220*1.4*AJ#)+(CT#/13200*1.4*AI#),0)
  ```

### `AL` · وجه مرخصی

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `AM` · پایانکار قابل پرداخت

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `AN` · عیدی وپاداش قابل پرداخت

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `AO` · مدت ماموریت

- **4 سطر**

  ```
  =CZ#
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `AP` · مبلغ ماموریت

- **4 سطر**

  ```
  =AO#*T#
  ```

- **1 سطر**

  ```
  =AO#*T#*2
  ```

### `AQ` · ذخیره پورسانت فروش

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `AR` · پورسانت یک قابل پرداخت

- **1 سطر**

  ```
  =(CO#+CR#+AL#)-(AC#+AQ#+CN#+AP#)
  ```

- 4 سطر به‌جای فرمول، عددِ ثابت دارند.

### `AS` · پورسانت قابل پرداخت دو

- **1 سطر**

  ```
  =CP#
  ```

- 4 سطر به‌جای فرمول، عددِ ثابت دارند.

### `AT` · پورسانت قابل پرداخت سه

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `AU` · طلب ماه قبل

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `AV` · هزینه تلفن

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `AW` · جمع پرداختيها فیش

- **همهٔ سطرها**

  ```
  =AU#+AT#+AS#+AR#+AP#+AN#+AM#+AL#+AK#+AH#+AG#+AF#+AE#+AC#+AA#+Z#+AV#
  ```

### `AX` · علی الحساب واریزی

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `AY` · علی الحساب نقدی

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `AZ` · جمع علی الحساب

- **همهٔ سطرها**

  ```
  =AX#+AY#
  ```

### `BA` · ماخذ مالیات

- **همهٔ سطرها**

  ```
  =AW#-AP#
  ```

### `BB` · مالیات

- **همهٔ سطرها**

  ```
  =IF(BA#<=400000000,0, IF(BA#<=800000000,(BA#-400000000)*10%, IF(BA#<=1000000000,(BA#-800000000)*15%+40000000, IF(BA#<=1200000000,(BA#-1000000000)*20%+70000000, IF(BA#<=1400000000,(BA#-1200000000)*25%+140000000, IF(BA#>1400000001,(BA#-1400000000)*30%+190000000))))))
  ```

### `BC` · روند مالیات

- **همهٔ سطرها**

  ```
  =ROUND((BB#),0)
  ```

### `BD` · ماخذ بیمه

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `BE` · سهم کارگر

- **همهٔ سطرها**

  ```
  =ROUND((BD#*7%),0)
  ```

### `BF` · سهم کارفرما

- **همهٔ سطرها**

  ```
  =ROUND((BD#*20%),0)
  ```

### `BG` · بیمه بیکاری

- **همهٔ سطرها**

  ```
  =ROUND((BD#*3%),0)
  ```

### `BH` · جمع کل بیمه

- **4 سطر**

  ```
  =ROUND((BG#+BF#+BE#),0)
  ```

- **1 سطر**

  ```
  =BG#+BF#+BE#
  ```

### `BI` · مبلغ کل وام

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `BJ` · تعدادكل قسط

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `BK` · مبلغ قسط

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `BL` · شماره قسط

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `BM` · مانده قسط

- **همهٔ سطرها**

  ```
  =BJ#-BL#
  ```

### `BN` · مانده وام

- **همهٔ سطرها**

  ```
  =BM#*BK#
  ```

### `BO` · خرید از شرکت

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `BP` · بدهی از ماه قبل

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `BQ` · سایر بدهی

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `BR` · بیمه تکمیلی

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `BS` · جرائم وخلافی خودروها

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `BT` · آزمایش وطب کار

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `BU` · ماخد کسر کار - دیر کرد

- **همهٔ سطرها**

  ```
  =U#+V#+CG#+CI#+CK#+CL#
  ```

### `BV` · مبلغ کسر کار - دیر کرد

- **همهٔ سطرها**

  ```
  =ROUND((BU#/220*P#)+(BU#/11440*Q#),0)
  ```

### `BW` · جمع كسورات

- **همهٔ سطرها**

  ```
  =BV#+BT#+BS#+BR#+BQ#+BP#+BO#+BK#+BE#+BC#+AX#
  ```

### `BX` · خالص پرداخت

- **همهٔ سطرها**

  ```
  =AW#-BW#
  ```

### `BY` · شماره کارت

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `BZ` · شماره حساب

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `CA` · بانك عامل

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `CB` · مزدروزانه 1404

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `CC` · پایه سنوات تجمیعی روزانه سال 1405

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `CD` · پایه سنوات تجمیعی ماهانه سال 1405

- **همهٔ سطرها**

  ```
  =CC#*30
  ```

### `CE` · حداقل دستمزد 1405

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `CF` · پایه سنوات روزانه 1405

- **3 سطر**

  ```
  =ROUND((5000000/30),0)
  ```

- 2 سطر به‌جای فرمول، عددِ ثابت دارند.

### `CG` · حق مسکن

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `CH` · تعداد اولاد

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `CI` · حق اولاد

- **همهٔ سطرها**

  ```
  =CH#*CE#*3
  ```

### `CJ` · وضعیت تاهل

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `CK` · حق تاهل

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `CL` · وجه بن

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `CM` · ماخذ پایانکار وعیدی وپاداش ووجه مرخصی وماموریت

- **4 سطر**

  ```
  =T#
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `CN` · کسر مازاد پرداختی فروش

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `CO` · پورسانت یک

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `CP` · پورسانت دو

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `CQ` · پورسانت سه

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `CR` · مازاد ثابت

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `CS` · مازاد پرداختی ماهانه

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `CT` · ماخذ محاسبه اضافه کاری

- **همهٔ سطرها**

  ```
  =W#
  ```

### `CU` · اضافه کارواقعی دقیقه

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `CV` · اضافه کار واقعی ساعت

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `CW` · مبلغ اضافه کار واقعی

- **همهٔ سطرها**

  ```
  =ROUND((CT#/220*1.4*CV#)+(CT#/13200*1.4*CU#),0)
  ```

### `CX` · جمع مازاد ثابت / مازاد پرداختی ماهانه / اضافه کار واقعی ومرخصی

- **همهٔ سطرها**

  ```
  =CW#+CS#+CR#+AL#
  ```

### `CY` · قابل پرداخت مازاد ثابت / اضافه کار ثابت / اضافه کار واقعی

- **4 سطر**

  ```
  =CX#-AC#
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `CZ` · محاسبه مدت ماموریت

- **4 سطر**

  ```
  =ROUNDDOWN((CY#/CM#),0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DA` · محاسبه مبلغ ماموریت

- **4 سطر**

  ```
  =CZ#*CM#
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DB` · مازاد محاسبه یک

- **4 سطر**

  ```
  =CY#-DA#
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DC` · اضافه کار هر ساعت

- **4 سطر**

  ```
  =ROUND((W#/220*1.4*1),0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DD` · اضافه کار هردقیقه

- **4 سطر**

  ```
  =ROUND((W#/13200*1.4*1),0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DE` · محاسبه اضافه کار دقیقه

- **4 سطر**

  ```
  =ROUNDUP(IF(OR(DI#<DD#),0,IF(OR(DI#>DD#),DI#/DD#)),0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DF` · محاسبه اضافه کار ساعت

- **4 سطر**

  ```
  =ROUNDDOWN((DB#/DC#),0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DG` · مبلغ اضافه کار محاسبه دقیقه

- **4 سطر**

  ```
  =ROUND((W#/13200*1.4*DE#),0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DH` · مبلغ اضافه کار محاسبه ساعت

- **4 سطر**

  ```
  =ROUND((W#/220*1.4*DF#),0)
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DI` · مازاد محاسبه دو

- **4 سطر**

  ```
  =DB#-DH#
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DJ` · مازاد محاسبه سه

- **4 سطر**

  ```
  =DI#-DG#
  ```

- 1 سطر به‌جای فرمول، عددِ ثابت دارند.

### `DK` · مانده مرخصی قابل استفاده در آخر سال 1404

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `DL` · مرخصی انتقالی از سال 1404 به روز

- **همهٔ سطرها**

  ```
  =ROUND(IF(OR(DK#=9,DK#>9),9,IF(OR(DK#<9,),DK#)),0)
  ```

### `DM` · مرخصی استحقاق سال 1405 به روز

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `DN` · جمع مر خصی قابل استفاده در سال 1405 به روز

- **همهٔ سطرها**

  ```
  =DM#+DL#
  ```

### `DO` · مرخصی قابل استفاده سال 1405 به ساعت

- **همهٔ سطرها**

  ```
  =ROUNDDOWN((DN#*440)/60,0)
  ```

### `DP` · جمع مرخصی قابل استفاده سال 1405 به دقیقه

- **همهٔ سطرها**

  ```
  =ROUNDDOWN((DN#*440)-(DO#*60),0)
  ```

### `DQ` · مصرفی ماه اعلامی دقیقه

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `DR` · مصرفی ماه اعلامی ساعت

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `DS` · مصرفی ماه اعلامی روز

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `DT` · مصرفی ماه واقعی دقیقه

- **همهٔ سطرها**

  ```
  =ROUNDDOWN((DS#*440+DR#*60+DQ#-(DV#*440+DU#*60)),0)
  ```

### `DU` · مصرفی ماه واقعی ساعت

- **همهٔ سطرها**

  ```
  =ROUNDDOWN((DS#*440+DR#*60+DQ#-DV#*440)/60,0)
  ```

### `DV` · مصرفی ماه واقعی روز

- **همهٔ سطرها**

  ```
  =ROUNDDOWN((DS#*440+DR#*60+DQ#)/440,0)
  ```

### `DW` · مصرفی اعلامی تا ماه دقیقه

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `DX` · مصرفی اعلامی تا ماه ساعت

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `DY` · مصرفی اعلامی تا ماه روز

عدد یا متنِ ورودی — 5 سطر مقدار دارد.

### `DZ` · مصرفی تا ماه دقیقه

- **همهٔ سطرها**

  ```
  =ROUNDDOWN((DY#*440+DX#*60+DW#-(EB#*440+EA#*60)),0)
  ```

### `EA` · مصرفی تا ماه ساعت

- **همهٔ سطرها**

  ```
  =ROUNDDOWN((DY#*440+DX#*60+DW#-EB#*440)/60,0)
  ```

### `EB` · مصرفی تا ماه روز

- **همهٔ سطرها**

  ```
  =ROUNDDOWN((DY#*440+DX#*60+DW#)/440,0)
  ```

### `EC` · استفاده شده کل تا ماه دقیقه

- **همهٔ سطرها**

  ```
  =ROUNDDOWN((EB#*440+EA#*60+DZ#),0)
  ```

### `ED` · مانده تا آخر سال دقیقه

- **همهٔ سطرها**

  ```
  =ROUNDDOWN((DN#*440-EC#-(EF#*440+EE#*60)),0)
  ```

### `EE` · مانده تا آخر سال ساعت

- **همهٔ سطرها**

  ```
  =ROUNDDOWN((DN#*440-EC#-EF#*440)/60,0)
  ```

### `EF` · مانده تا آخر سال روز

- **همهٔ سطرها**

  ```
  =ROUNDDOWN((DN#*440-EC#)/440,0)
  ```


---

## شیت «مازاد 1405» — 3 سطر

### `A` · رديف

عدد یا متنِ ورودی — 3 سطر مقدار دارد.

### `B` · نام ونام خانوادگي

عدد یا متنِ ورودی — 3 سطر مقدار دارد.

### `C` · مازاد ثابت 1404

عدد یا متنِ ورودی — 3 سطر مقدار دارد.

### `D` · مازاد ثابت 1405

عدد یا متنِ ورودی — 1 سطر مقدار دارد.

### `E` · حفوق ثابت 1404

عدد یا متنِ ورودی — 3 سطر مقدار دارد.

### `F` · حفوق ثابت 1405

عدد یا متنِ ورودی — 0 سطر مقدار دارد.

